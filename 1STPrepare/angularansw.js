“Hi, my name is A Saikrishna Achary. I’m a Frontend Developer with around 2.8 years of experience 
in developing responsive, scalable, and high-performance web applications.

I primarily work with Angular, React.js, Next.js, TypeScript, JavaScript, Tailwind CSS, and modern frontend technologies.

In Angular, I have experience working with standalone components, reactive forms, RxJS, Signals, routing, lazy loading,
 API integration, authentication, and reusable component architecture. I’ve worked on domain-based projects like Banking/FinTech,
  Grocery E-commerce, and Healthcare applications where I handled frontend development, dashboard modules, state management,
   and performance optimization.

I also have experience collaborating with backend teams for REST API integration and implementing responsive UI designs
 with a focus on clean code and user experience.

Recently, I’ve also been exploring AI integration with Angular applications and modern frontend architecture improvements.

I’m passionate about learning new technologies and building scalable product-level applications


# 1. Banking / FinTech Application

## Interview Explanation

> “I worked on a Banking/FinTech application used for digital banking operations 
like account management, transaction history, payments, and financial dashboards. 
My role was mainly focused on frontend development using Angular, API integration,
 authentication, and building secure reusable UI components.”

---

## Technologies Used

* Angular
* TypeScript
* RxJS
* Angular Material
* REST APIs
* JWT Authentication
* Tailwind CSS
* NgRx
* Chart.js

---

## Features

* User authentication & authorization
* Account dashboard
* Transaction history
* Fund transfer
* Real-time notifications
* Analytics & reports
* Responsive design
* Secure session handling

---

## My Responsibilities

* Developed reusable Angular components
* Integrated banking APIs
* Implemented route guards and JWT authentication
* Worked on dashboard UI and transaction modules
* Handled form validation and error handling
* Optimized application performance

---

## Challenges Faced

### Problem:

Large transaction data slowed down the UI.

### Solution:

> “I implemented lazy loading, pagination, virtual scrolling, and trackBy optimization in Angular to improve performance.”

---

## Security Discussion (Important in Banking)

### Interview Answer:

> “Since it was a fintech application, security was very important. We used JWT authentication,
 HTTP interceptors, route guards, secure API handling, and proper form validation to protect sensitive user data.”

---

## Short Interview Version

> “I worked on a Banking/FinTech application where I handled Angular frontend development,
 API integration, authentication, dashboard modules, and performance optimization. I also worked
  on secure transaction-related features and reusable component architecture.”

---

# 2. Grocery E-Commerce Application

## Interview Explanation

> “I worked on a grocery e-commerce platform where users could browse products, add items to cart, 
place orders, and track deliveries. My role involved frontend development, API integration, state management,
 and responsive UI implementation.”

---

## Technologies Used

* Angular
* TypeScript
* NgRx
* Firebase/Auth
* REST APIs
* Tailwind CSS
* Razorpay/Stripe

---

## Features

* Product listing
* Search & filtering
* Cart & wishlist
* Order management
* Online payments
* Address management
* Delivery tracking
* Mobile responsive UI

---

## My Responsibilities

* Built product listing pages
* Developed cart and checkout flow
* Integrated payment gateway APIs
* Implemented reusable forms
* Worked on responsive UI
* Managed state using NgRx

---

## Challenges Faced

### Problem:

Cart data was getting reset during refresh.

### Solution:

> “I used NgRx store persistence and localStorage synchronization to maintain cart state across refreshes.”

---

## Performance Optimization

### Interview Answer:

> “We optimized image loading, implemented lazy loading for modules, and reduced unnecessary API calls using RxJS operators.”

---

## Short Interview Version

> “I worked on a grocery e-commerce application where I developed product modules, 
cart functionality, payment integration, and responsive UI using Angular and NgRx. 
I also handled API integration and performance optimization.”

---

# 3. Healthcare Application

## Interview Explanation

> “I worked on a healthcare management application used for patient management, 
appointment booking, medical records, and doctor consultation workflows. My role
 focused on frontend architecture, API integration, forms, and secure healthcare dashboards.”

---

## Technologies Used

* Angular
* TypeScript
* RxJS
* Angular Material
* REST APIs
* JWT Authentication
* Tailwind CSS

---

## Features

* Patient registration
* Appointment booking
* Doctor dashboard
* Electronic medical records
* Prescription management
* Notifications & reminders
* Role-based access

---

## My Responsibilities

* Developed appointment scheduling modules
* Built dynamic reactive forms
* Integrated healthcare APIs
* Created reusable UI components
* Implemented validation and error handling
* Worked with backend APIs for patient data

---

## Challenges Faced

### Problem:

Handling large and complex forms with validations.

### Solution:

> “I used Angular Reactive Forms with custom validators and reusable form components to manage complex medical forms efficiently.”

---

## Security Discussion

### Interview Answer:

> “Healthcare applications require secure handling of patient information, 
so we implemented authentication, authorization, secure API communication, and proper access control.”

---

## Short Interview Version

> “I worked on a healthcare application where I developed appointment booking modules, 
patient dashboards, reactive forms, and API integrations using Angular. I also focused 
on secure and responsive UI development.”

---

# Best Way to Explain Any Project in Interview

Follow this structure:

## 1. Project Overview

> What the application does

## 2. Technologies Used

> Angular, APIs, state management, styling

## 3. Your Responsibilities

> What exactly you developed

## 4. Challenges

> Real technical issue

## 5. Solution

> How you solved it

## 6. Performance/Security

> Optimization & authentication

---

# Strong Combined Interview Answer

> “I have worked on multiple domain projects including Banking/FinTech, 
Grocery E-commerce, and Healthcare applications using Angular. My responsibilities
 included frontend development, reusable component architecture, API integration,
  authentication, state management, responsive UI development, and performance optimization. 
  I also worked closely with backend teams to integrate secure and scalable features.”

---

# If Interviewer Asks “Which Project Was Most Challenging?”

## Recommended Answer

> “The Banking/FinTech project was the most challenging because security, performance,
 and handling real-time transaction data were very important. I learned a lot about scalable
  architecture, optimization, and secure frontend development from that project.”



  --------------------------------------------------------------------------------------------


  ## 1. What is Angular?

Angular is a **TypeScript-based open-source frontend framework** developed by Google for building **dynamic web applications** and **Single Page Applications (SPA)**.

It helps developers create:

* Reusable UI components
* Fast web applications
* Scalable enterprise applications
* Responsive frontend systems

### Key Uses

* Banking Applications
* E-commerce Platforms
* Healthcare Dashboards
* Admin Panels
* Enterprise Applications

---

# 2. Difference Between AngularJS and Angular

| Feature              | AngularJS     | Angular               |
| -------------------- | ------------- | --------------------- |
| Language             | JavaScript    | TypeScript            |
| Architecture         | MVC           | Component-Based       |
| Performance          | Slower        | Faster                |
| Mobile Support       | Limited       | Better Mobile Support |
| Dependency Injection | Basic         | Advanced              |
| Data Binding         | Two-way       | One-way & Two-way     |
| Routing              | Less Powerful | Powerful Router       |
| CLI Support          | No            | Yes                   |
| Version              | 1.x           | 2+                    |
| Maintained By        | Google        | Google                |

### Short Interview Answer

AngularJS is the old JavaScript-based framework, while Angular is the modern TypeScript-based framework with better performance, modular architecture, CLI support, and improved scalability.

---

# 3. What are the Features of Angular?

## Main Features

### 1. Component-Based Architecture

Angular applications are built using reusable components.

### 2. Two-Way Data Binding

Automatically synchronizes data between UI and component.

### 3. Dependency Injection

Provides services efficiently across components.

### 4. Routing

Supports navigation between pages without reloading.

### 5. TypeScript Support

Improves code quality and maintainability.

### 6. Directives

Used to manipulate DOM behavior.

Examples:

* `*ngIf`
* `*ngFor`
* `ngClass`

### 7. Reactive Forms

Provides advanced form validation and handling.

### 8. RxJS Support

Handles asynchronous operations using Observables.

### 9. Lazy Loading

Loads modules only when required to improve performance.

### 10. Built-in Testing Support

Supports unit testing and end-to-end testing.

---

# 4. What is TypeScript and Why Angular Uses It?

TypeScript is a **superset of JavaScript** developed by Microsoft that adds:

* Static Typing
* Interfaces
* Classes
* Decorators
* Better Tooling

## Why Angular Uses TypeScript?

### 1. Better Error Detection

Errors are found during development instead of runtime.

### 2. Object-Oriented Features

Supports classes, interfaces, inheritance.

### 3. Better IDE Support

Autocomplete, debugging, navigation.

### 4. Cleaner and Maintainable Code

Useful for large enterprise applications.

### Example

```ts
class User {
  name: string = "Sai";
}
```

---

# 5. What is SPA (Single Page Application)?

A **Single Page Application (SPA)** is a web application that loads a single HTML page and dynamically updates content without refreshing the entire page.

## How SPA Works

* Initial page loads once
* Angular updates only required components
* Faster user experience

## Advantages

* Faster navigation
* Better user experience
* Reduced server load
* Smooth transitions

## Examples

* Gmail
* Facebook
* Netflix

---

# 6. What is a Component in Angular?

A component is the **basic building block** of an Angular application.

It controls:

* HTML Template
* CSS Styles
* Business Logic

## Component Structure

```ts
@Component({
  selector: 'app-user',
  templateUrl: './user.component.html',
  styleUrls: ['./user.component.css']
})
export class UserComponent {
  name = "Sai";
}
```

## Parts of Component

* Template (HTML)
* Style (CSS)
* Class (TypeScript Logic)

---

# 7. What is a Module in Angular?

A module is a container that groups:

* Components
* Directives
* Pipes
* Services

Angular applications are divided into modules for better organization.

---

# 8. What is NgModule?

`@NgModule` is a decorator that defines an Angular module.

## Example

```ts
@NgModule({
  declarations: [AppComponent],
  imports: [BrowserModule],
  bootstrap: [AppComponent]
})
export class AppModule {}
```

## Main Properties

| Property     | Purpose                       |
| ------------ | ----------------------------- |
| declarations | Components, Pipes, Directives |
| imports      | Other modules                 |
| providers    | Services                      |
| bootstrap    | Root component                |

---

# 9. What is Angular CLI?

Angular CLI is a command-line tool used to create and manage Angular projects.

## Common Commands

### Create Project

```bash
ng new my-app
```

### Run Application

```bash
ng serve
```

### Generate Component

```bash
ng generate component home
```

OR

```bash
ng g c home
```

### Build Project

```bash
ng build
```

## Advantages

* Faster development
* Automatic configuration
* Code generation
* Testing support

---

# 10. How Angular Works Internally?

## Step-by-Step Internal Working

### 1. Bootstrap Process

Angular starts from:

```ts
main.ts
```

Bootstraps:

```ts
AppModule
```

OR standalone component in Angular 17+.

---

### 2. Loads Root Component

Angular loads:

```ts
AppComponent
```

using:

```html
<app-root>
```

---

### 3. Component Rendering

Angular converts templates into JavaScript instructions and renders DOM.

---

### 4. Change Detection

Angular checks data changes and updates UI automatically.

Example:

```ts
this.name = "Sai";
```

UI updates automatically.

---

### 5. Dependency Injection

Angular injects required services into components.

Example:

```ts
constructor(private apiService: ApiService) {}
```

---

### 6. Routing

Angular Router changes views without page reload.

---

### 7. RxJS & Observables

Used for:

* API Calls
* Async Data
* Event Handling

---

### 8. Compilation

Angular compiler converts:

* TypeScript → JavaScript
* Templates → Optimized JS Code

---

# Short Interview Summary

Angular is a TypeScript-based frontend framework developed by Google for building scalable SPA applications. It uses component-based architecture, dependency injection, routing, RxJS, and powerful CLI tools to create fast and maintainable enterprise applications.


# 11. What is the Angular Architecture?

Angular architecture is the overall structure used to build Angular applications efficiently and scalably.

## Main Building Blocks of Angular Architecture

```text
Modules
   ↓
Components
   ↓
Templates
   ↓
Services
   ↓
Dependency Injection
   ↓
Routing
```

---

## 1. Modules

Modules organize the application into functional blocks.

Example:

* AppModule
* UserModule
* AdminModule

Purpose:

* Organize code
* Improve maintainability
* Enable lazy loading

---

## 2. Components

Components control UI parts of the application.

Each component contains:

* HTML Template
* CSS Styles
* TypeScript Logic

Example:

```ts id="o1ss8e"
@Component({
  selector: 'app-home',
  templateUrl: './home.component.html'
})
export class HomeComponent {}
```

---

## 3. Templates

Templates define the UI using HTML.

Example:

```html id="6zq83p"
<h1>{{title}}</h1>
```

Supports:

* Data Binding
* Directives
* Pipes

---

## 4. Services

Services contain reusable business logic.

Examples:

* API calls
* Authentication
* Shared data

Example:

```ts id="2wf3cc"
@Injectable()
export class UserService {}
```

---

## 5. Dependency Injection (DI)

Angular automatically provides services where needed.

Example:

```ts id="t06jlk"
constructor(private userService: UserService) {}
```

Advantages:

* Reusability
* Loose coupling
* Better testing

---

## 6. Routing

Routing helps navigate between pages without reloading.

Example:

```ts id="dxx7zx"
const routes: Routes = [
  { path: 'home', component: HomeComponent }
];
```

---

## 7. Directives

Directives change DOM behavior.

Examples:

* `*ngIf`
* `*ngFor`
* `ngClass`

---

## 8. Pipes

Pipes transform data in templates.

Example:

```html id="0j24bh"
{{ name | uppercase }}
```

---

## 9. RxJS & Observables

RxJS is used for asynchronous programming.

Used in:

* API calls
* Event handling
* Real-time data

---

# Short Interview Answer

Angular architecture is component-based and includes modules, components, templates, services, dependency injection, routing, directives, and RxJS for building scalable SPA applications.

---

# 12. What are Decorators in Angular?

Decorators are special TypeScript functions that add metadata to classes, methods, properties, or parameters.

Angular uses decorators to define:

* Components
* Modules
* Services
* Inputs/Outputs

Decorators start with `@`.

---

## Common Angular Decorators

| Decorator     | Purpose                   |
| ------------- | ------------------------- |
| `@Component`  | Defines a component       |
| `@NgModule`   | Defines a module          |
| `@Injectable` | Defines a service         |
| `@Input`      | Receives data from parent |
| `@Output`     | Sends data to parent      |
| `@Directive`  | Creates custom directives |
| `@Pipe`       | Creates custom pipes      |

---

## Example

```ts id="a4if30"
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {}
```

Here, `@Component` tells Angular that this class is a component.

---

# Short Interview Answer

Decorators are special TypeScript annotations used in Angular to provide metadata and configure classes like components, modules, services, directives, and pipes.

---

# 13. What is @Component Decorator?

`@Component` is a decorator used to define a component in Angular.

It tells Angular:

* Which template to use
* Which styles to use
* Component selector
* Component configuration

---

## Syntax

```ts id="vd8nkl"
@Component({
  selector: 'app-user',
  templateUrl: './user.component.html',
  styleUrls: ['./user.component.css']
})
export class UserComponent {}
```

---

## Important Properties

| Property      | Purpose                      |
| ------------- | ---------------------------- |
| `selector`    | HTML tag name                |
| `templateUrl` | External HTML file           |
| `styleUrls`   | External CSS file            |
| `template`    | Inline HTML                  |
| `styles`      | Inline CSS                   |
| `imports`     | Standalone component imports |

---

## Example Usage

```html id="yq6i4t"
<app-user></app-user>
```

Angular renders the component wherever the selector is used.

---

# Short Interview Answer

`@Component` is a decorator that converts a TypeScript class into an Angular component and provides metadata like selector, template, and styles.

---

# 14. What is @NgModule Decorator?

`@NgModule` is a decorator used to define an Angular module.

It groups related:

* Components
* Directives
* Pipes
* Services

---

## Example

```ts id="hf0yrd"
@NgModule({
  declarations: [AppComponent],
  imports: [BrowserModule],
  providers: [],
  bootstrap: [AppComponent]
})
export class AppModule {}
```

---

## Main Properties

| Property       | Purpose                       |
| -------------- | ----------------------------- |
| `declarations` | Components, directives, pipes |
| `imports`      | Other Angular modules         |
| `providers`    | Services                      |
| `bootstrap`    | Root component                |
| `exports`      | Shared components/modules     |

---

## Why Needed?

* Organizes application
* Improves scalability
* Enables lazy loading
* Feature separation

---

## Note (Angular 17+)

Modern Angular also supports:

* Standalone Components
* Module-less architecture

So `NgModule` is optional in many new Angular applications.

---

# Short Interview Answer

`@NgModule` is a decorator used to configure and organize Angular modules by grouping components, services, directives, and dependencies.

---

# 15. What is Metadata in Angular?

Metadata is information used by Angular to understand how a class should behave.

Metadata is added using decorators.

---

## Example

```ts id="zjlwmz"
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
```

Here:

* `selector`
* `templateUrl`

are metadata properties.

---

## Why Metadata is Important?

Metadata tells Angular:

* How to create components
* Which template to render
* Which services to inject
* Which module belongs where

Without metadata, Angular cannot process classes correctly.

---

## Types of Metadata in Angular

| Metadata           | Used In       |
| ------------------ | ------------- |
| Component Metadata | `@Component`  |
| Module Metadata    | `@NgModule`   |
| Service Metadata   | `@Injectable` |
| Pipe Metadata      | `@Pipe`       |

---

# Short Interview Answer

Metadata is additional information provided through decorators that tells Angular how to process and use classes like components, modules, services, and pipes.




# 16. What is Interpolation?

Angular interpolation is a **data binding technique** used to display TypeScript data in the HTML template.

It uses:

```html id="3apvql"
{{ }}
```

syntax.

---

## Example

```ts id="m7w7go"
export class AppComponent {
  name = "Sai Krishna";
}
```

```html id="55s0k8"
<h1>{{ name }}</h1>
```

### Output

```text id="h6c0u3"
Sai Krishna
```

---

## Uses of Interpolation

* Display variables
* Show dynamic data
* Call methods
* Display expressions

Example:

```html id="g8mkjg"
{{ 10 + 20 }}
```

---

## Important Points

* Works from component → template
* Only for displaying data
* Cannot handle events

---

# Short Interview Answer

Interpolation is a one-way data binding technique in Angular used to display dynamic data from the component to the HTML template using double curly braces `{{ }}`.

---

# 17. What is Property Binding?

Property binding is used to bind component data to HTML element properties.

It uses:

```html id="ydg8z5"
[ ]
```

syntax.

---

## Example

```ts id="4s6y59"
export class AppComponent {
  imageUrl = "logo.png";
}
```

```html id="9x75yi"
<img [src]="imageUrl">
```

Angular sets the `src` property dynamically.

---

## Common Property Bindings

| Property | Example                   |
| -------- | ------------------------- |
| src      | `[src]="imgUrl"`          |
| href     | `[href]="link"`           |
| disabled | `[disabled]="isDisabled"` |
| value    | `[value]="username"`      |

---

## Difference from Interpolation

| Interpolation        | Property Binding        |
| -------------------- | ----------------------- |
| Displays text        | Sets element properties |
| `{{ }}`              | `[ ]`                   |
| Mostly string values | Any property values     |

---

# Short Interview Answer

Property binding is a one-way data binding technique used to bind component data to DOM element properties using square brackets `[ ]`.

---

# 18. What is Event Binding?

Event binding is used to handle user actions/events in Angular.

It uses:

```html id="o8i9fd"
( )
```

syntax.

---

## Example

```html id="6xwfgw"
<button (click)="showMessage()">Click</button>
```

```ts id="7f5b6x"
showMessage() {
  alert("Button Clicked");
}
```

---

## Common Events

| Event     | Purpose       |
| --------- | ------------- |
| click     | Button click  |
| input     | Input typing  |
| change    | Value changed |
| submit    | Form submit   |
| mouseover | Mouse hover   |

---

## How It Works

```text id="cnvckn"
User Action → Event Triggered → Component Method Executes
```

---

# Short Interview Answer

Event binding is used to listen to user events like click, input, and submit, and execute component methods using parentheses `( )`.

---

# 19. What is Two-Way Data Binding?

Two-way data binding synchronizes data between:

* Component
* Template

Changes in UI update component data, and changes in component update UI automatically.

Angular uses:

```html id="5tmqg6"
[(ngModel)]
```

syntax.

---

## Example

```html id="vh8ebc"
<input [(ngModel)]="username">

<p>{{ username }}</p>
```

```ts id="w2t6i4"
username = "";
```

---

## How It Works

```text id="b88fht"
Component ↔ Template
```

Both stay synchronized automatically.

---

## Requirements

Need to import:

```ts id="5t6o3w"
FormsModule
```

---

## Internally Combination Of

Two-way binding combines:

* Property Binding
* Event Binding

Equivalent to:

```html id="f55j6n"
[input]="value"
(input)="value = $event.target.value"
```

---

# Short Interview Answer

Two-way data binding allows automatic synchronization between component data and UI using `[(ngModel)]`.

---

# 20. What is Dependency Injection (DI)?

Dependency Injection (DI) is a design pattern used by Angular to provide required dependencies (services) to components automatically.

Instead of creating objects manually, Angular injects them.

---

## Example Without DI

```ts id="c8w0n4"
const service = new UserService();
```

Problem:

* Tight coupling
* Hard to test
* Hard to maintain

---

## Example With DI

```ts id="f4sv4y"
constructor(private userService: UserService) {}
```

Angular automatically creates and provides the service.

---

## Service Example

```ts id="1vf2es"
@Injectable({
  providedIn: 'root'
})
export class UserService {}
```

---

## Advantages of DI

| Advantage       | Description               |
| --------------- | ------------------------- |
| Reusability     | Share services everywhere |
| Loose Coupling  | Components independent    |
| Easy Testing    | Mock services easily      |
| Maintainability | Cleaner architecture      |

---

## How Angular DI Works

```text id="z9h57l"
Injector → Creates Service → Provides to Component
```

---

## Types of Injection

| Type                   | Description            |
| ---------------------- | ---------------------- |
| Constructor Injection  | Most common            |
| Injector Injection     | Manual injection       |
| Hierarchical Injection | Parent-child injectors |

---

# Short Interview Answer

Dependency Injection is a design pattern where Angular automatically provides required services or dependencies to components, improving reusability, testing, and maintainability.








# 21. What is Component Lifecycle in Angular?

Angular component lifecycle is the sequence of stages a component goes through from:

* Creation
* Rendering
* Updating
* Destruction

Angular provides lifecycle hooks to control these stages.

---

## Lifecycle Flow

```text id="1r0m6j"
Component Created
      ↓
ngOnChanges
      ↓
ngOnInit
      ↓
ngDoCheck
      ↓
ngAfterContentInit
      ↓
ngAfterContentChecked
      ↓
ngAfterViewInit
      ↓
ngAfterViewChecked
      ↓
ngOnDestroy
```

---

## Why Lifecycle Hooks Are Important?

They help developers:

* Initialize data
* Handle input changes
* Access DOM elements
* Optimize performance
* Cleanup subscriptions

---

# Short Interview Answer

Component lifecycle in Angular refers to the different phases a component passes through from creation to destruction. Angular provides lifecycle hooks to perform actions during these phases.

---

# 22. Explain Angular Lifecycle Hooks

Lifecycle hooks are special methods provided by Angular that execute automatically at specific stages of a component lifecycle.

---

# Main Lifecycle Hooks

| Hook                    | Purpose                          |
| ----------------------- | -------------------------------- |
| `ngOnChanges`           | Detect input changes             |
| `ngOnInit`              | Initialize component             |
| `ngDoCheck`             | Custom change detection          |
| `ngAfterContentInit`    | After projected content loads    |
| `ngAfterContentChecked` | After checking projected content |
| `ngAfterViewInit`       | After component view loads       |
| `ngAfterViewChecked`    | After checking component view    |
| `ngOnDestroy`           | Cleanup before destroy           |

---

## Example

```ts id="7ovqic"
export class AppComponent implements OnInit {

  ngOnInit() {
    console.log("Component Initialized");
  }
}
```

---

## Lifecycle Order

```text id="9dtnc7"
Constructor
   ↓
ngOnChanges
   ↓
ngOnInit
   ↓
ngDoCheck
   ↓
ngAfterViewInit
   ↓
ngOnDestroy
```

---

# Short Interview Answer

Angular lifecycle hooks are predefined methods that Angular calls automatically during different stages of a component’s lifecycle, such as initialization, change detection, rendering, and destruction.

---

# 23. What is ngOnInit?

`ngOnInit` is a lifecycle hook that executes once after Angular initializes the component.

It is mainly used for:

* API calls
* Initial data loading
* Initialization logic

---

## Syntax

```ts id="h18lbh"
import { OnInit } from '@angular/core';

export class UserComponent implements OnInit {

  ngOnInit(): void {
    console.log("Component Initialized");
  }
}
```

---

## When ngOnInit Executes

```text id="0mkpcl"
Constructor → ngOnInit → View Render
```

---

## Common Uses

* Fetch API data
* Initialize variables
* Setup subscriptions
* Call services

---

## Example

```ts id="tq9r90"
ngOnInit() {
  this.getUsers();
}
```

---

# Short Interview Answer

`ngOnInit` is an Angular lifecycle hook that runs once after component initialization and is commonly used for API calls and initial setup logic.

---

# 24. Difference Between Constructor and ngOnInit

| Constructor                     | ngOnInit                      |
| ------------------------------- | ----------------------------- |
| TypeScript feature              | Angular lifecycle hook        |
| Executes first                  | Executes after constructor    |
| Used for dependency injection   | Used for initialization logic |
| Component not fully initialized | Component fully initialized   |
| Avoid API calls here            | Best place for API calls      |

---

## Constructor Example

```ts id="w2ucjh"
constructor(private userService: UserService) {}
```

Used mainly for:

* Injecting services

---

## ngOnInit Example

```ts id="1gyzh6"
ngOnInit() {
  this.loadUsers();
}
```

Used mainly for:

* Loading data
* API calls
* Initialization

---

## Execution Order

```text id="wj1w6d"
Constructor
    ↓
ngOnInit
```

---

# Best Practice

✅ Constructor:

* Dependency Injection only

✅ ngOnInit:

* Business logic
* API calls
* Initialization

---

# Short Interview Answer

The constructor is a TypeScript feature mainly used for dependency injection, while `ngOnInit` is an Angular lifecycle hook used for component initialization and API calls after the component is fully created.

---

# 25. What is ngOnChanges?

`ngOnChanges` is a lifecycle hook that executes whenever input properties change.

It works with:

```ts id="6laxf6"
@Input()
```

properties.

---

## Syntax

```ts id="9pt5q8"
import { OnChanges, SimpleChanges } from '@angular/core';

export class ChildComponent implements OnChanges {

  @Input() name!: string;

  ngOnChanges(changes: SimpleChanges) {
    console.log(changes);
  }
}
```

---

## When It Executes

* Before `ngOnInit`
* Whenever input values change

---

## Example

### Parent Component

```html id="0b1e9o"
<app-child [name]="userName"></app-child>
```

### Child Component

```ts id="7u4x6t"
@Input() name!: string;
```

If `userName` changes, `ngOnChanges()` executes.

---

## SimpleChanges Object

Provides:

* Previous value
* Current value
* First change status

Example:

```ts id="pohq48"
changes.name.currentValue
changes.name.previousValue
```

---

# Use Cases

* Detect parent data changes
* Update UI dynamically
* Trigger logic on input updates

---

# Short Interview Answer

`ngOnChanges` is an Angular lifecycle hook that runs whenever an `@Input()` property changes and provides previous and current values through the `SimpleChanges` object.






# 26. What is ngDoCheck?

Angular `ngDoCheck` is a lifecycle hook used for **custom change detection**.

Angular calls it during every change detection cycle.

---

## Syntax

```ts id="7uhgqk"
import { DoCheck } from '@angular/core';

export class AppComponent implements DoCheck {

  ngDoCheck(): void {
    console.log("Change Detection Triggered");
  }
}
```

---

## When It Executes

```text id="sx5sc8"
Whenever Angular runs change detection
```

Examples:

* Button click
* Input change
* API response
* Timer updates

---

## Use Cases

* Detect complex object changes
* Custom change tracking
* Performance optimization

---

## Important Note

`ngDoCheck` executes very frequently.

So:

* Avoid heavy logic
* Avoid unnecessary API calls

---

## Difference Between ngOnChanges and ngDoCheck

| ngOnChanges                | ngDoCheck               |
| -------------------------- | ----------------------- |
| Detects `@Input()` changes | Detects all changes     |
| Automatic                  | Manual/custom detection |
| Less frequent              | Executes frequently     |

---

# Short Interview Answer

`ngDoCheck` is an Angular lifecycle hook used for custom change detection and executes during every change detection cycle.

---

# 27. What is ngAfterViewInit?

`ngAfterViewInit` is a lifecycle hook that executes after Angular initializes the component view and child views.

---

## Syntax

```ts id="xjlwmw"
import { AfterViewInit } from '@angular/core';

export class AppComponent implements AfterViewInit {

  ngAfterViewInit(): void {
    console.log("View Initialized");
  }
}
```

---

## When It Executes

```text id="yok5c1"
After component HTML and child components are rendered
```

---

## Common Uses

* Access DOM elements
* Access child components
* Initialize third-party libraries
* ViewChild operations

---

## Example Using ViewChild

```ts id="apc4sn"
@ViewChild('inputRef') inputElement!: ElementRef;

ngAfterViewInit() {
  this.inputElement.nativeElement.focus();
}
```

---

## Why Not Constructor?

DOM is not ready inside:

* constructor
* ngOnInit

DOM becomes fully available in:

```text id="8m4d2u"
ngAfterViewInit
```

---

# Short Interview Answer

`ngAfterViewInit` is a lifecycle hook that runs after Angular fully initializes the component view and child views, mainly used for DOM access and ViewChild operations.

---

# 28. What is ngOnDestroy?

`ngOnDestroy` is a lifecycle hook that executes just before a component is destroyed.

Used for cleanup activities.

---

## Syntax

```ts id="j5l9tw"
import { OnDestroy } from '@angular/core';

export class AppComponent implements OnDestroy {

  ngOnDestroy(): void {
    console.log("Component Destroyed");
  }
}
```

---

## Common Uses

* Unsubscribe Observables
* Clear timers
* Remove event listeners
* Prevent memory leaks

---

## Example

```ts id="r2l3y1"
subscription!: Subscription;

ngOnDestroy() {
  this.subscription.unsubscribe();
}
```

---

## Why Important?

Without cleanup:

* Memory leaks occur
* Performance issues happen

---

## When It Executes

```text id="1rk8u0"
Before component removal/navigation
```

---

# Short Interview Answer

`ngOnDestroy` is an Angular lifecycle hook used for cleanup tasks like unsubscribing Observables and clearing resources before a component is destroyed.

---

# 29. What is Template in Angular?

A template in Angular defines the UI structure of a component using HTML.

Templates display:

* Data
* Events
* Directives
* Bindings

---

## Example

```ts id="5thb1x"
@Component({
  selector: 'app-root',
  template: `<h1>Hello {{name}}</h1>`
})
export class AppComponent {
  name = "Sai";
}
```

---

## Template Features

### 1. Interpolation

```html id="xvjlwm"
{{ title }}
```

### 2. Property Binding

```html id="m5p3q4"
<img [src]="imageUrl">
```

### 3. Event Binding

```html id="98ll3y"
<button (click)="save()">Save</button>
```

### 4. Directives

```html id="59ns3g"
<div *ngIf="isVisible"></div>
```

---

## Types of Templates

| Type              | Description              |
| ----------------- | ------------------------ |
| Inline Template   | Written inside component |
| External Template | Separate HTML file       |

---

# Short Interview Answer

A template in Angular is the HTML view of a component used to display UI elements, data binding, directives, and events.

---

# 30. Difference Between Inline Template and External Template

| Inline Template               | External Template            |
| ----------------------------- | ---------------------------- |
| HTML written inside component | HTML stored in separate file |
| Uses `template` property      | Uses `templateUrl` property  |
| Suitable for small templates  | Suitable for large templates |
| Harder to maintain            | Easier to maintain           |
| Less readable                 | More readable                |

---

# Inline Template Example

```ts id="wbl3r7"
@Component({
  selector: 'app-root',
  template: `<h1>Hello Angular</h1>`
})
export class AppComponent {}
```

---

# External Template Example

```ts id="s4k4sq"
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {}
```

---

## Best Practice

✅ Use Inline Template:

* Small/simple components

✅ Use External Template:

* Large/complex UI

---

# Short Interview Answer

Inline templates define HTML directly inside the component using the `template` property, while external templates use a separate HTML file with `templateUrl`, making code cleaner and easier to maintain.




# 31. What are Directives?

Angular directives are special instructions in Angular used to modify:

* DOM structure
* DOM appearance
* DOM behavior

Directives extend HTML functionality.

---

## Example

```html id="h0i1xp"
<div *ngIf="isVisible">
  Hello Angular
</div>
```

Here:

```text id="b2v6c8"
*ngIf
```

is a directive.

---

## What Directives Can Do

* Show/hide elements
* Change styles
* Add dynamic behavior
* Repeat elements
* Create custom behavior

---

## Common Angular Directives

| Directive | Purpose               |
| --------- | --------------------- |
| `*ngIf`   | Conditional rendering |
| `*ngFor`  | Looping               |
| `ngClass` | Dynamic classes       |
| `ngStyle` | Dynamic styles        |

---

# Short Interview Answer

Directives are special Angular classes that modify the behavior, appearance, or structure of DOM elements.

---

# 32. Types of Directives in Angular

Angular directives are mainly divided into 3 types.

---

# 1. Component Directives

Every Angular component is technically a directive with a template.

Example:

```ts id="lpqjlwm"
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
```

Purpose:

* Define UI components

---

# 2. Structural Directives

Structural directives change the DOM layout by:

* Adding elements
* Removing elements

Examples:

* `*ngIf`
* `*ngFor`
* `*ngSwitch`

---

# 3. Attribute Directives

Attribute directives change:

* Appearance
* Behavior

without changing DOM structure.

Examples:

* `ngClass`
* `ngStyle`

---

# Summary Table

| Type       | Purpose                    | Examples             |
| ---------- | -------------------------- | -------------------- |
| Component  | UI with template           | `@Component`         |
| Structural | Add/remove elements        | `*ngIf`, `*ngFor`    |
| Attribute  | Change appearance/behavior | `ngClass`, `ngStyle` |

---

# Short Interview Answer

Angular has three types of directives:

1. Component directives
2. Structural directives
3. Attribute directives

They are used to control UI structure and behavior.

---

# 33. What is Structural Directive?

Structural directives change the DOM structure by adding or removing elements.

They usually use:

```text id="64uvns"
*
```

symbol.

---

## Common Structural Directives

| Directive   | Purpose               |
| ----------- | --------------------- |
| `*ngIf`     | Conditional rendering |
| `*ngFor`    | Loop through data     |
| `*ngSwitch` | Multiple conditions   |

---

## Example — ngIf

```html id="tmr6ah"
<div *ngIf="isLoggedIn">
  Welcome User
</div>
```

If condition is false:

* Element removed from DOM

---

## Example — ngFor

```html id="i8n3c9"
<li *ngFor="let user of users">
  {{ user }}
</li>
```

Repeats elements dynamically.

---

## Why Called Structural?

Because they modify:

```text id="p0eqlm"
DOM structure
```

---

# Short Interview Answer

Structural directives are Angular directives that add or remove elements from the DOM, such as `*ngIf` and `*ngFor`.

---

# 34. What is Attribute Directive?

Attribute directives change the appearance or behavior of existing DOM elements without changing the DOM structure.

---

## Common Attribute Directives

| Directive | Purpose                  |
| --------- | ------------------------ |
| `ngClass` | Add/remove CSS classes   |
| `ngStyle` | Apply styles dynamically |

---

## Example — ngClass

```html id="6of1gx"
<div [ngClass]="{'active': isActive}">
  Content
</div>
```

---

## Example — ngStyle

```html id="s4wjlwm"
<p [ngStyle]="{'color':'red'}">
  Angular
</p>
```

---

## Important Point

Attribute directives:

* Do NOT create/remove elements
* Only modify existing elements

---

# Short Interview Answer

Attribute directives modify the appearance or behavior of DOM elements without changing the DOM structure, such as `ngClass` and `ngStyle`.

---

# 35. Difference Between ngIf and hidden

| ngIf                       | hidden                      |
| -------------------------- | --------------------------- |
| Structural directive       | HTML attribute              |
| Removes element from DOM   | Hides element using CSS     |
| Better for performance     | Element still exists in DOM |
| Creates/destroys component | Only visibility changes     |
| Supports Angular logic     | Simple HTML hiding          |

---

# Example — ngIf

```html id="jlwm1m"
<div *ngIf="isVisible">
  Hello
</div>
```

If false:

```text id="l6p8ij"
Element removed from DOM
```

---

# Example — hidden

```html id="g1c56u"
<div [hidden]="!isVisible">
  Hello
</div>
```

If false:

```text id="gjc7gx"
Element hidden using display:none
```

---

# Key Difference

## ngIf

```text id="9jlwmq"
DOM element created/destroyed
```

## hidden

```text id="s4k8x0"
DOM element remains
```

---

# When to Use?

✅ Use `ngIf`

* Large components
* Performance optimization
* Conditional rendering

✅ Use `hidden`

* Frequently toggled UI
* Fast visibility switching

---

# Short Interview Answer

`*ngIf` removes or creates elements in the DOM based on conditions, while `hidden` only hides elements using CSS without removing them from the DOM.




# 36. What is ngFor?

Angular `*ngFor` is a **structural directive** used to iterate over collections like arrays and display data dynamically in the template.

---

## Syntax

```html id="z6g2v1"
<div *ngFor="let item of items">
  {{ item }}
</div>
```

---

## Example

### Component

```ts id="3kh3s9"
users = ['Sai', 'Ravi', 'Krishna'];
```

### Template

```html id="v1y8x2"
<li *ngFor="let user of users">
  {{ user }}
</li>
```

---

## Output

```text id="f0m4s7"
Sai
Ravi
Krishna
```

---

## Useful Variables in ngFor

| Variable | Purpose       |
| -------- | ------------- |
| `index`  | Current index |
| `first`  | First element |
| `last`   | Last element  |
| `even`   | Even index    |
| `odd`    | Odd index     |

---

## Example with index

```html id="g9s4c2"
<li *ngFor="let user of users; let i = index">
  {{ i }} - {{ user }}
</li>
```

---

# Short Interview Answer

`*ngFor` is an Angular structural directive used to loop through arrays or collections and display elements dynamically in the template.

---

# 37. What is trackBy in ngFor?

`trackBy` is used with `*ngFor` to improve performance by uniquely identifying list items.

Instead of re-rendering the entire list, Angular updates only changed items.

---

## Why trackBy is Needed?

Without `trackBy`:

* Angular recreates all DOM elements

With `trackBy`:

* Angular updates only modified elements

Better performance for:

* Large lists
* Dynamic tables
* Frequent updates

---

## Syntax

```html id="8tw7o3"
<li *ngFor="let user of users; trackBy: trackById">
  {{ user.name }}
</li>
```

---

## Component Example

```ts id="n3q8r1"
trackById(index: number, user: any): number {
  return user.id;
}
```

---

## Example Data

```ts id="m2x7p5"
users = [
  { id: 1, name: 'Sai' },
  { id: 2, name: 'Ravi' }
];
```

---

## Benefits

* Better performance
* Reduced DOM manipulation
* Faster rendering
* Optimized change detection

---

# Short Interview Answer

`trackBy` is used in `*ngFor` to uniquely identify list items so Angular can optimize DOM updates and improve performance.

---

# 38. What is ngSwitch?

`ngSwitch` is a structural directive used to display different elements based on multiple conditions.

It works like:

```text id="8g1r9v"
switch-case
```

in programming languages.

---

## Syntax

```html id="c5d9q2"
<div [ngSwitch]="role">

  <p *ngSwitchCase="'admin'">Admin User</p>

  <p *ngSwitchCase="'user'">Normal User</p>

  <p *ngSwitchDefault>Guest</p>

</div>
```

---

## Example

### Component

```ts id="u3m6z8"
role = 'admin';
```

### Output

```text id="5y1s7c"
Admin User
```

---

## Directives Used

| Directive         | Purpose        |
| ----------------- | -------------- |
| `ngSwitch`        | Main condition |
| `ngSwitchCase`    | Matching case  |
| `ngSwitchDefault` | Default case   |

---

## Advantages

* Cleaner multiple conditions
* Better readability
* Easier maintenance

---

# Short Interview Answer

`ngSwitch` is an Angular structural directive used to display different templates based on multiple conditions, similar to a switch-case statement.

---

# 39. What are Pipes?

Pipes are used to transform data in Angular templates without changing the original value.

---

## Syntax

```html id="6m2q8w"
{{ value | pipeName }}
```

---

## Example

```html id="t9x4p7"
{{ name | uppercase }}
```

Output:

```text id="v7k3n1"
SAI
```

---

# Common Built-in Pipes

| Pipe        | Purpose                |
| ----------- | ---------------------- |
| `uppercase` | Convert to uppercase   |
| `lowercase` | Convert to lowercase   |
| `titlecase` | First letter capital   |
| `date`      | Format date            |
| `currency`  | Currency formatting    |
| `percent`   | Percentage formatting  |
| `json`      | Convert object to JSON |

---

## Example — Date Pipe

```html id="p4s8y6"
{{ today | date:'dd/MM/yyyy' }}
```

---

## Chaining Pipes

```html id="g2w7u4"
{{ name | lowercase | titlecase }}
```

---

## Custom Pipe Example

```ts id="m9x1q5"
@Pipe({
  name: 'reverse'
})
export class ReversePipe implements PipeTransform {

  transform(value: string): string {
    return value.split('').reverse().join('');
  }
}
```

---

# Short Interview Answer

Pipes are Angular features used to transform and format data in templates, such as uppercase, date, and currency formatting.

---

# 40. Difference Between Pure and Impure Pipes

| Pure Pipe                                  | Impure Pipe                                  |
| ------------------------------------------ | -------------------------------------------- |
| Executes only when input reference changes | Executes during every change detection cycle |
| Better performance                         | Lower performance                            |
| Default behavior                           | Must set `pure:false`                        |
| Suitable for immutable data                | Suitable for mutable data                    |

---

# Pure Pipe Example

```ts id="s7k2m4"
@Pipe({
  name: 'customPipe',
  pure: true
})
```

Runs only when:

* Primitive value changes
* Object reference changes

---

# Impure Pipe Example

```ts id="q5n8v1"
@Pipe({
  name: 'customPipe',
  pure: false
})
```

Runs:

```text id="1y4u7z"
Every change detection cycle
```

---

# When to Use?

✅ Pure Pipes:

* Better performance
* Most applications

✅ Impure Pipes:

* Mutable arrays/objects
* Real-time updates

---

# Performance Note

Impure pipes can slow applications because they execute very frequently.

---

# Short Interview Answer

Pure pipes execute only when input references change and provide better performance, while impure pipes execute on every change detection cycle and are used for mutable data changes.



# 41. What is Custom Pipe?

Angular custom pipe is a user-defined pipe used to create custom data transformations in Angular.

When built-in pipes are not enough, we create custom pipes.

---

# Steps to Create Custom Pipe

## 1. Create Pipe

```bash id="m4k8z1"
ng generate pipe reverse
```

OR

```bash id="x7p3q9"
ng g p reverse
```

---

## 2. Implement PipeTransform

```ts id="v6m2s8"
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'reverse'
})
export class ReversePipe implements PipeTransform {

  transform(value: string): string {
    return value.split('').reverse().join('');
  }
}
```

---

## 3. Use in Template

```html id="t1q7n5"
{{ 'Angular' | reverse }}
```

---

## Output

```text id="r5w9k2"
ralugnA
```

---

# Common Use Cases

* Text formatting
* Search filtering
* Currency formatting
* Data transformation
* Date customization

---

# Advantages

* Reusable
* Cleaner templates
* Better maintainability

---

# Short Interview Answer

A custom pipe is a user-defined Angular pipe used to create custom data transformations by implementing the `PipeTransform` interface.

---

# 42. What are Template Reference Variables?

Template reference variables are variables used to reference:

* DOM elements
* Angular components
* Directives

inside the template.

They are created using:

```text id="8n2v4x"
#
```

symbol.

---

# Example

```html id="m7x3p8"
<input #nameInput type="text">

<button (click)="show(nameInput.value)">
  Submit
</button>
```

---

# How It Works

```text id="y4p9w1"
#nameInput
```

references the input element.

---

# Component

```ts id="k5s8q3"
show(value: string) {
  console.log(value);
}
```

---

# Uses

* Access input values
* Access DOM elements
* Form validation
* Pass template references

---

# Example with Video

```html id="v1q7m9"
<video #videoPlayer></video>

<button (click)="videoPlayer.play()">
  Play
</button>
```

---

# Short Interview Answer

Template reference variables are local template variables created using `#` that reference DOM elements, components, or directives inside Angular templates.

---

# 43. What is Content Projection?

Content projection is a technique used to insert external content inside a component.

Angular uses:

```html id="f2m6x8"
<ng-content>
```

for content projection.

Similar to:

```text id="c7w1r4"
slots
```

in other frameworks.

---

# Example

## Child Component

```html id="g9q3v2"
<div class="card">
  <ng-content></ng-content>
</div>
```

---

## Parent Component

```html id="p4x8s1"
<app-card>
  <h2>Hello Angular</h2>
</app-card>
```

---

## Output

```html id="s6m2k7"
<div class="card">
  <h2>Hello Angular</h2>
</div>
```

---

# Types of Content Projection

| Type        | Description                  |
| ----------- | ---------------------------- |
| Single-slot | One `ng-content`             |
| Multi-slot  | Multiple `ng-content select` |

---

# Multi-slot Example

```html id="q7v4p1"
<ng-content select=".title"></ng-content>

<ng-content select=".body"></ng-content>
```

---

# Advantages

* Reusable UI
* Flexible layouts
* Better component design

---

# Short Interview Answer

Content projection is an Angular feature that allows external content to be inserted inside a component using `<ng-content>`.

---

# 44. What is ViewChild?

`@ViewChild` is a decorator used to access:

* Child components
* DOM elements
* Directives

from the component class.

---

# Syntax

```ts id="u8m3q6"
@ViewChild('inputRef') input!: ElementRef;
```

---

# Example

## Template

```html id="d2v7x9"
<input #inputRef type="text">
```

---

## Component

```ts id="e4m1s8"
@ViewChild('inputRef') input!: ElementRef;

ngAfterViewInit() {
  this.input.nativeElement.focus();
}
```

---

# Common Uses

* DOM access
* Focus input fields
* Access child component methods
* Third-party libraries

---

# When Available?

`ViewChild` becomes available after:

```text id="r1p6w4"
ngAfterViewInit
```

---

# Types of ViewChild Targets

| Target      | Example            |
| ----------- | ------------------ |
| DOM Element | `ElementRef`       |
| Component   | Child component    |
| Directive   | Directive instance |

---

# Short Interview Answer

`@ViewChild` is an Angular decorator used to access child components, directives, or DOM elements from the component class after the view initializes.

---

# 45. What is ContentChild?

`@ContentChild` is a decorator used to access projected content passed through:

```html id="k4m8v2"
<ng-content>
```

---

# Difference from ViewChild

| ViewChild               | ContentChild                |
| ----------------------- | --------------------------- |
| Accesses component view | Accesses projected content  |
| Internal template       | External projected template |

---

# Example

## Child Component

```html id="v5s1m9"
<ng-content></ng-content>
```

---

## Parent Component

```html id="c8q4x7"
<app-card>
  <p #contentRef>Hello</p>
</app-card>
```

---

## Child TS

```ts id="t7m2v5"
@ContentChild('contentRef') content!: ElementRef;
```

---

# When Available?

Available after:

```text id="q3w8n1"
ngAfterContentInit
```

---

# Common Uses

* Access projected templates
* Reusable components
* Dynamic content handling

---

# Short Interview Answer

`@ContentChild` is an Angular decorator used to access projected content passed into a component through `<ng-content>`.




# 46. What is One-Way Data Binding?

Angular one-way data binding means data flows in only one direction.

Usually:

```text id="u1x4m8"
Component → Template
```

Angular updates the UI when component data changes.

---

# Types of One-Way Binding

| Type             | Syntax  |
| ---------------- | ------- |
| Interpolation    | `{{ }}` |
| Property Binding | `[ ]`   |
| Event Binding    | `( )`   |

---

# Example — Interpolation

```ts id="m3q7v1"
name = "Sai";
```

```html id="w8p2x5"
<h1>{{ name }}</h1>
```

---

# Example — Property Binding

```html id="s4m9k2"
<img [src]="imageUrl">
```

---

# Advantages

* Better performance
* Easier debugging
* Predictable data flow

---

# Short Interview Answer

One-way data binding is a data flow mechanism where data moves in a single direction, usually from the component to the template.

---

# 47. What is Two-Way Data Binding?

Two-way data binding synchronizes data between:

* Component
* Template

Changes in UI update component data automatically, and component changes update UI automatically.

Angular uses:

```html id="v7m1q4"
[(ngModel)]
```

syntax.

---

# Example

## Component

```ts id="t9p5x2"
username = "";
```

---

## Template

```html id="n2v8s1"
<input [(ngModel)]="username">

<p>{{ username }}</p>
```

---

# How It Works

```text id="g6m3w9"
Component ↔ Template
```

Both remain synchronized.

---

# Requirements

Import:

```ts id="c4x7q1"
FormsModule
```

---

# Internally Combines

* Property Binding
* Event Binding

Equivalent to:

```html id="k8p2m5"
[value]="username"
(input)="username = $event.target.value"
```

---

# Advantages

* Simplifies forms
* Automatic synchronization
* Less code

---

# Short Interview Answer

Two-way data binding automatically synchronizes data between the component and template using `[(ngModel)]`.

---

# 48. Difference Between Property Binding and Interpolation

| Property Binding                       | Interpolation               |
| -------------------------------------- | --------------------------- |
| Uses `[ ]`                             | Uses `{{ }}`                |
| Binds to DOM properties                | Displays text values        |
| Supports non-string values             | Converts values to string   |
| Used for attributes like src, disabled | Used for displaying content |

---

# Interpolation Example

```html id="q7m4v2"
<h1>{{ title }}</h1>
```

Used for:

```text id="r9x1p6"
Displaying text
```

---

# Property Binding Example

```html id="m5w8q3"
<button [disabled]="isDisabled">
  Save
</button>
```

Used for:

```text id="s2v7k9"
Setting DOM properties
```

---

# Important Difference

## Interpolation

Works mainly with:

```text id="x1m4p8"
Text content
```

## Property Binding

Works with:

```text id="w6q2n5"
DOM element properties
```

---

# Short Interview Answer

Interpolation is used to display data in templates using `{{ }}`, while property binding binds component data to DOM element properties using `[ ]`.

---

# 49. What is EventEmitter?

`EventEmitter` is used for communication from:

```text id="j8m3v1"
Child Component → Parent Component
```

It works together with:

```ts id="n5x7p4"
@Output()
```

decorator.

---

# Example

## Child Component

```ts id="f2q8m6"
import { Output, EventEmitter } from '@angular/core';

@Output() messageEvent = new EventEmitter<string>();

sendMessage() {
  this.messageEvent.emit("Hello Parent");
}
```

---

## Child Template

```html id="u4v9x2"
<button (click)="sendMessage()">
  Send
</button>
```

---

## Parent Template

```html id="g7m1q5"
<app-child
  (messageEvent)="receiveMessage($event)">
</app-child>
```

---

## Parent Component

```ts id="s8x2p6"
receiveMessage(data: string) {
  console.log(data);
}
```

---

# Uses

* Parent-child communication
* Sending events/data upward
* Custom events

---

# Short Interview Answer

`EventEmitter` is an Angular class used with `@Output()` to send data and events from a child component to a parent component.

---

# 50. What is @Input Decorator?

`@Input()` is a decorator used to receive data from:

```text id="c5m9x3"
Parent Component → Child Component
```

---

# Example

## Parent Component

```html id="v1q8m4"
<app-child [name]="userName"></app-child>
```

---

## Child Component

```ts id="p7x3q6"
import { Input } from '@angular/core';

@Input() name!: string;
```

---

# How It Works

```text id="w4m7p2"
Parent sends data → Child receives data
```

---

# Example Display

```html id="y2q5v8"
<p>{{ name }}</p>
```

---

# Advantages

* Reusable components
* Dynamic child components
* Parent-child communication

---

# Important Note

`@Input()` works with:

* Property Binding
* Parent-to-child communication

---

# Short Interview Answer

`@Input()` is an Angular decorator used to pass data from a parent component to a child component.





# 51. What is @Output Decorator?

Angular `@Output()` is a decorator used to send data/events from:

```text
Child → Parent
```

It works together with **EventEmitter**.

---

## Example

### Child Component

```ts
import { Output, EventEmitter } from '@angular/core';

@Output() notify = new EventEmitter<string>();

sendData() {
  this.notify.emit("Hello Parent");
}
```

---

### Child Template

```html
<button (click)="sendData()">Send</button>
```

---

### Parent Template

```html
<app-child (notify)="receiveData($event)"></app-child>
```

---

### Parent Component

```ts
receiveData(data: string) {
  console.log(data);
}
```

---

## Key Points

* Used for child → parent communication
* Emits custom events
* Works with EventEmitter

---

# Short Interview Answer

`@Output()` is an Angular decorator used to emit events from a child component to a parent component using EventEmitter.

---

# 52. How Parent-Child Communication Works?

In Angular, parent-child communication happens in two directions.

---

## 1. Parent → Child (Using @Input)

### Parent

```html
<app-child [name]="userName"></app-child>
```

### Child

```ts
@Input() name!: string;
```

---

## 2. Child → Parent (Using @Output)

### Child

```ts
@Output() event = new EventEmitter<string>();
```

### Parent

```html
<app-child (event)="handle($event)"></app-child>
```

---

## Flow

```text
Parent → Child  (Input)
Child → Parent  (Output)
```

---

## Summary

| Direction      | Method                 |
| -------------- | ---------------------- |
| Parent → Child | @Input                 |
| Child → Parent | @Output + EventEmitter |

---

# Short Interview Answer

Parent-child communication in Angular uses `@Input()` for passing data from parent to child and `@Output()` with EventEmitter for sending data from child to parent.

---

# 53. How Sibling Component Communication Works?

Sibling components do NOT communicate directly.

They communicate through:

```text
Common Parent OR Shared Service
```

---

## Method 1: Using Parent

### Flow

```text
Sibling A → Parent → Sibling B
```

---

## Method 2: Using Shared Service (Recommended)

Both components use the same service to share data.

---

## Example Using Service

### Service

```ts
data = new BehaviorSubject<string>("");

sendData(value: string) {
  this.data.next(value);
}
```

---

### Component A

```ts
this.service.sendData("Hello");
```

---

### Component B

```ts
this.service.data.subscribe(res => {
  console.log(res);
});
```

---

# Short Interview Answer

Sibling components communicate either through a common parent using Input/Output or through a shared service using RxJS.

---

# 54. What is Shared Service?

A shared service is an Angular service used to share data and logic between multiple components.

---

## Why Use Shared Service?

* Component communication
* Reusable logic
* Centralized data management

---

## Example

```ts
import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class DataService {

  private dataSource = new BehaviorSubject<string>("");

  currentData$ = this.dataSource.asObservable();

  updateData(value: string) {
    this.dataSource.next(value);
  }
}
```

---

## Usage

### Component A

```ts
this.dataService.updateData("Hello");
```

---

### Component B

```ts
this.dataService.currentData$.subscribe(data => {
  console.log(data);
});
```

---

## Benefits

* Loose coupling
* Real-time updates
* Easy state sharing

---

# Short Interview Answer

A shared service is an Angular service used to share data and functionality between components, commonly using RxJS Observables.

---

# 55. What is RxJS?

RxJS (Reactive Extensions for JavaScript) is a library used for handling:

* Asynchronous data
* Event streams
* Reactive programming

Angular heavily uses RxJS.

---

## Core Concepts

| Concept      | Description         |
| ------------ | ------------------- |
| Observable   | Data stream         |
| Observer     | Receives data       |
| Subscription | Executes observable |
| Operators    | Transform data      |

---

## Example

```ts
import { Observable } from 'rxjs';

const obs = new Observable(observer => {
  observer.next("Hello");
});

obs.subscribe(data => console.log(data));
```

---

## Real Use Cases

* API calls (HttpClient)
* Form value changes
* Event handling
* State management

---

## Common RxJS Classes

| Class           | Purpose                 |
| --------------- | ----------------------- |
| Observable      | Stream of data          |
| Subject         | Multicast data          |
| BehaviorSubject | Holds latest value      |
| ReplaySubject   | Replays previous values |

---

## Advantages

* Handles async operations
* Reactive programming
* Powerful operators
* Better control over streams

---

# Short Interview Answer

RxJS is a reactive programming library used in Angular to handle asynchronous data streams using Observables, Subjects, and operators.





# 56. What is Observable?

RxJS **Observable** is a stream of data that can emit multiple values over time (synchronously or asynchronously).

It is widely used in Angular for:

* API calls
* Event handling
* Real-time data

---

## Example

```ts id="x1a2b3"
import { Observable } from 'rxjs';

const obs = new Observable(observer => {
  observer.next("Hello");
  observer.next("World");
  observer.complete();
});

obs.subscribe(data => console.log(data));
```

---

## Output

```text id="o1p2q3"
Hello
World
```

---

## Key Features

* Can emit multiple values
* Lazy (runs only on subscribe)
* Supports operators
* Can be cancelled (unsubscribe)

---

# Short Interview Answer

An Observable is a data stream that emits multiple values over time and is used in Angular for handling asynchronous operations.

---

# 57. Difference Between Observable and Promise

| Observable            | Promise              |
| --------------------- | -------------------- |
| Emits multiple values | Emits only one value |
| Lazy execution        | Executes immediately |
| Can be cancelled      | Cannot be cancelled  |
| Uses subscribe()      | Uses then()          |
| Supports operators    | Limited features     |
| Used in RxJS          | Native JavaScript    |

---

## Example — Promise

```ts id="p1q2r3"
const promise = new Promise(resolve => {
  resolve("Hello");
});

promise.then(res => console.log(res));
```

---

## Example — Observable

```ts id="s4t5u6"
obs.subscribe(res => console.log(res));
```

---

## Key Difference

```text id="v7w8x9"
Observable → Multiple values + cancelable  
Promise → Single value + not cancelable
```

---

# Short Interview Answer

Observable emits multiple values over time and can be cancelled, while Promise emits a single value and cannot be cancelled.

---

# 58. What is BehaviorSubject?

`BehaviorSubject` is a special type of Subject in RxJS that:

* Stores the **latest value**
* Emits the **current value immediately** to new subscribers

---

## Example

```ts id="b1c2d3"
import { BehaviorSubject } from 'rxjs';

const subject = new BehaviorSubject<string>("Initial");

subject.subscribe(data => console.log("A:", data));

subject.next("Hello");

subject.subscribe(data => console.log("B:", data));
```

---

## Output

```text id="e4f5g6"
A: Initial
A: Hello
B: Hello
```

---

## Key Features

* Requires initial value
* Stores last emitted value
* Useful for state management

---

## Use Cases

* Shared services
* State management
* Form data sharing

---

# Short Interview Answer

BehaviorSubject is an RxJS Subject that stores the latest value and immediately emits it to new subscribers.

---

# 59. What is Subject in RxJS?

`Subject` is both:

* Observable (can subscribe)
* Observer (can emit values)

It allows multicasting (same data to multiple subscribers).

---

## Example

```ts id="h1i2j3"
import { Subject } from 'rxjs';

const subject = new Subject<string>();

subject.subscribe(data => console.log("A:", data));
subject.subscribe(data => console.log("B:", data));

subject.next("Hello");
```

---

## Output

```text id="k4l5m6"
A: Hello
B: Hello
```

---

## Key Features

* No initial value
* Does NOT store previous values
* Emits only new values

---

# Short Interview Answer

Subject is an RxJS type that acts as both an Observable and Observer, allowing multiple subscribers to receive the same emitted values.

---

# 60. Difference Between Subject and BehaviorSubject

| Subject                     | BehaviorSubject                |
| --------------------------- | ------------------------------ |
| No initial value            | Requires initial value         |
| Does not store last value   | Stores latest value            |
| New subscribers get nothing | New subscribers get last value |
| Emits only new values       | Emits current + new values     |

---

## Example Difference

### Subject

```ts id="n1o2p3"
const subject = new Subject();

subject.next("Hello");

subject.subscribe(data => console.log(data)); // No output
```

---

### BehaviorSubject

```ts id="q4r5s6"
const subject = new BehaviorSubject("Initial");

subject.next("Hello");

subject.subscribe(data => console.log(data)); // Hello
```

---

## When to Use?

✅ Use Subject:

* Event-based communication

✅ Use BehaviorSubject:

* State management
* Shared data

---

# Short Interview Answer

Subject does not store previous values and only emits new ones, while BehaviorSubject stores the latest value and sends it immediately to new subscribers.




# 61. What is a Service in Angular?

Angular **service** is a class used to handle:

* Business logic
* Data sharing
* API calls
* Reusable functionality

Services are typically decorated with `@Injectable()`.

---

## Example

```ts
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class UserService {

  getUsers() {
    return ["Sai", "Ravi"];
  }
}
```

---

## Usage in Component

```ts
constructor(private userService: UserService) {}

ngOnInit() {
  console.log(this.userService.getUsers());
}
```

---

## Key Idea

```text
Keep logic in services, UI in components
```

---

# Short Interview Answer

A service in Angular is a class used to handle business logic, API calls, and shared data, making applications modular and reusable.

---

# 62. Why Services Are Used?

Services solve multiple problems in Angular applications.

---

## Main Reasons

### 1. Code Reusability

Write once, use in multiple components.

---

### 2. Separation of Concerns

* Components → UI
* Services → Logic

---

### 3. Data Sharing

Share data across components.

---

### 4. API Handling

Centralize HTTP calls.

---

### 5. Maintainability

Cleaner and scalable code structure.

---

## Example

Instead of writing API logic in every component:

```text
Use one service → reuse everywhere
```

---

# Short Interview Answer

Services are used to share data, handle business logic, reuse code, and keep components clean and maintainable.

---

# 63. What is Singleton Service?

A **singleton service** means:

```text
Only ONE instance of the service exists in the entire application
```

---

## How to Create Singleton Service?

### Method 1 (Recommended)

```ts
@Injectable({
  providedIn: 'root'
})
```

---

### Method 2

Provide service in `AppModule`.

---

## Why Use Singleton?

* Shared data across app
* Consistent state
* Better performance

---

## Example

```ts
@Injectable({
  providedIn: 'root'
})
export class AuthService {}
```

---

## Key Point

All components use the **same instance**.

---

# Short Interview Answer

A singleton service is a service that has only one instance throughout the Angular application, typically created using `providedIn: 'root'`.

---

# 64. What is Provider?

A **provider** tells Angular:

```text
How to create and supply a service
```

---

## Example

```ts
@Component({
  selector: 'app-root',
  providers: [UserService]
})
```

---

## Types of Providers

| Type               | Description          |
| ------------------ | -------------------- |
| Root Provider      | `providedIn: 'root'` |
| Component Provider | Inside component     |
| Module Provider    | Inside module        |

---

## What Provider Does?

* Registers service
* Creates instance
* Injects dependency

---

## Example Flow

```text
Provider → Creates Service → Injects into Component
```

---

# Short Interview Answer

A provider is a configuration that tells Angular how to create and deliver a service instance for dependency injection.

---

# 65. What is Hierarchical Dependency Injection?

Angular uses a **hierarchical injector system**.

This means:

```text
Different levels can have different instances of services
```

---

## Hierarchy Levels

```text
Root Injector
   ↓
Module Injector
   ↓
Component Injector
```

---

## How It Works

* Angular searches for a service:

  1. Component level
  2. Parent component
  3. Module level
  4. Root level

---

## Example

```ts
@Component({
  providers: [UserService]
})
```

This creates a **new instance** only for that component.

---

## Behavior

| Level     | Instance                   |
| --------- | -------------------------- |
| Root      | Global singleton           |
| Component | New instance per component |

---

## Benefits

* Flexible service scope
* Better control
* Optimized memory

---

# Short Interview Answer

Hierarchical dependency injection in Angular means services are organized in a tree of injectors, allowing different parts of the application to have their own instances or share a global instance.





# 66. What is Injector?

Angular **Injector** is the core part of Angular’s Dependency Injection system responsible for:

* Creating service instances
* Managing dependencies
* Providing them to components

---

## How It Works

```text
Injector → Creates Service → Injects into Component
```

---

## Example

```ts
constructor(private userService: UserService) {}
```

Here:

* Angular injector creates `UserService`
* Injects it into the component

---

## Types of Injectors

| Injector           | Scope              |
| ------------------ | ------------------ |
| Root Injector      | Whole application  |
| Module Injector    | Specific module    |
| Component Injector | Specific component |

---

## Key Point

Angular automatically resolves dependencies using injectors.

---

# Short Interview Answer

An injector is a mechanism in Angular that creates and provides service instances to components using dependency injection.

---

# 67. What is Constructor Injection?

Constructor Injection is a way of injecting dependencies into a class using its constructor.

---

## Example

```ts
constructor(private userService: UserService) {}
```

---

## How It Works

* Angular sees the dependency in constructor
* Injector creates the service
* Passes it to the component

---

## Benefits

* Clean code
* Easy testing
* Loose coupling

---

## Example Usage

```ts
ngOnInit() {
  this.userService.getUsers();
}
```

---

# Short Interview Answer

Constructor injection is a method of injecting dependencies into a class through its constructor, allowing Angular to automatically provide required services.

---

# 68. What is providedIn: 'root'?

`providedIn: 'root'` is used in `@Injectable()` to register a service at the root level.

---

## Example

```ts
@Injectable({
  providedIn: 'root'
})
export class AuthService {}
```

---

## What It Means

```text
Service is available globally across the application
```

---

## Benefits

* Creates singleton service
* No need to add in providers array
* Tree-shakable (removed if unused)

---

## Key Point

One instance shared across all components.

---

# Short Interview Answer

`providedIn: 'root'` registers a service at the application root level, making it a singleton and available throughout the app.

---

# 69. What is InjectionToken?

`InjectionToken` is used when Angular cannot identify a dependency by type.

Used for:

* Non-class dependencies
* Interfaces
* Configuration values

---

## Example

```ts
import { InjectionToken } from '@angular/core';

export const API_URL = new InjectionToken<string>('apiUrl');
```

---

## Provide Value

```ts
providers: [
  { provide: API_URL, useValue: 'https://api.example.com' }
]
```

---

## Inject Value

```ts
constructor(@Inject(API_URL) private apiUrl: string) {}
```

---

## Use Cases

* App configuration
* Environment variables
* Constants

---

# Short Interview Answer

InjectionToken is used to create a unique identifier for dependencies that are not classes, such as configuration values or interfaces.

---

# 70. Difference Between Service and Component

| Service                | Component                    |
| ---------------------- | ---------------------------- |
| Handles business logic | Handles UI                   |
| No HTML template       | Has HTML template            |
| Reusable logic         | UI building block            |
| Uses `@Injectable`     | Uses `@Component`            |
| Injected via DI        | Declared in module/component |

---

## Service Example

```ts
@Injectable({
  providedIn: 'root'
})
export class DataService {}
```

---

## Component Example

```ts
@Component({
  selector: 'app-root',
  template: `<h1>Hello</h1>`
})
export class AppComponent {}
```

---

## Key Difference

```text
Service → Logic
Component → UI
```

---

# Short Interview Answer

A service is used for business logic and data handling, while a component is used to build and control the UI in Angular.





# 71. What is Lazy Loaded Service?

Angular **lazy loaded service** is a service that is created only when a **lazy-loaded module** is loaded.

---

## What is Lazy Loading?

Lazy loading means:

```text
Load module only when required
```

---

## How Lazy Loaded Service Works

If a service is provided inside a lazy-loaded module:

```ts
@NgModule({
  providers: [UserService]
})
export class UserModule {}
```

👉 Angular creates a **separate instance** of the service for that module.

---

## Key Points

* Loaded only when module loads
* Not part of root injector
* Multiple instances possible
* Improves performance

---

## Example

```ts
{
  path: 'user',
  loadChildren: () => import('./user.module')
}
```

When route loads → service instance created.

---

# Short Interview Answer

A lazy loaded service is a service that is instantiated only when its associated lazy-loaded module is loaded, improving performance and scope control.

---

# 72. What is Dependency Injection Hierarchy?

Angular uses a **hierarchical structure of injectors**.

---

## Hierarchy

```text
Root Injector
   ↓
Module Injector
   ↓
Component Injector
```

---

## How It Works

When Angular needs a service:

1. Checks component injector
2. Moves to parent component
3. Checks module injector
4. Finally checks root injector

---

## Example

```ts
@Component({
  providers: [UserService]
})
```

👉 Creates a **new instance for that component only**

---

## Behavior

| Level     | Instance                   |
| --------- | -------------------------- |
| Root      | Global singleton           |
| Module    | Shared in module           |
| Component | New instance per component |

---

## Benefits

* Flexible scope
* Better control
* Optimized memory

---

# Short Interview Answer

Dependency injection hierarchy in Angular means services are resolved through a tree of injectors, allowing different scopes like root, module, and component levels.

---

# 73. What are Interceptors?

Interceptors are Angular services used to:

* Intercept HTTP requests
* Modify requests/responses
* Add common logic

They act like:

```text
Middleware for HTTP calls
```

---

## Why Use Interceptors?

* Add authentication token
* Handle errors
* Log requests
* Modify headers

---

## Example Use Cases

* JWT token injection
* Global error handling
* API logging

---

# Short Interview Answer

Interceptors are Angular services that intercept and modify HTTP requests and responses globally.

---

# 74. What is HTTP Interceptor?

HTTP Interceptor is a specific type of interceptor used with Angular’s HTTP client.

---

## How It Works

```text
Request → Interceptor → Server → Interceptor → Response
```

---

## Example

```ts
import { HttpInterceptor } from '@angular/common/http';

export class AuthInterceptor implements HttpInterceptor {

  intercept(req: any, next: any) {
    const modifiedReq = req.clone({
      headers: req.headers.set('Authorization', 'Bearer token')
    });

    return next.handle(modifiedReq);
  }
}
```

---

## Register Interceptor

```ts
providers: [
  { provide: HTTP_INTERCEPTORS, useClass: AuthInterceptor, multi: true }
]
```

---

## Features

* Modify request
* Modify response
* Handle errors
* Chain multiple interceptors

---

# Short Interview Answer

HTTP Interceptor is an Angular feature that intercepts HTTP requests and responses to modify them globally, such as adding headers or handling errors.

---

# 75. How to Handle Global Errors in Angular?

Angular provides multiple ways to handle global errors.

---

# 1. Using HTTP Interceptor (Recommended)

### Example

```ts
intercept(req: any, next: any) {
  return next.handle(req).pipe(
    catchError(error => {
      console.error(error);
      return throwError(() => error);
    })
  );
}
```

---

# 2. Using Global Error Handler

Create custom error handler:

```ts
import { ErrorHandler } from '@angular/core';

export class GlobalErrorHandler implements ErrorHandler {

  handleError(error: any): void {
    console.error('Global Error:', error);
  }
}
```

---

## Register It

```ts
providers: [
  { provide: ErrorHandler, useClass: GlobalErrorHandler }
]
```

---

# 3. Try-Catch (Local Handling)

```ts
try {
  // logic
} catch (error) {
  console.log(error);
}
```

---

# Best Practices

* Use interceptor for API errors
* Use ErrorHandler for global errors
* Log errors to monitoring tools
* Show user-friendly messages

---

# Short Interview Answer

Global errors in Angular can be handled using HTTP interceptors for API errors and a custom ErrorHandler for application-wide error handling.




# 76. What is Angular Routing?

Angular **routing** is a feature that allows navigation between different views (components) without reloading the page.

It is mainly used in **Single Page Applications (SPA)**.

---

## Example

```ts
const routes: Routes = [
  { path: 'home', component: HomeComponent },
  { path: 'about', component: AboutComponent }
];
```

---

## Template

```html
<a routerLink="/home">Home</a>
<a routerLink="/about">About</a>

<router-outlet></router-outlet>
```

---

## How It Works

```text
URL Change → Router → Loads Component → Updates View
```

---

## Key Features

* Navigation without reload
* URL-based navigation
* Supports lazy loading
* Route guards for security

---

# Short Interview Answer

Angular routing is used to navigate between components in a single-page application without reloading the page.

---

# 77. What is RouterModule?

`RouterModule` is an Angular module that provides routing functionality.

---

## Import Example

```ts
import { RouterModule, Routes } from '@angular/router';
```

---

## Configure Routes

```ts
const routes: Routes = [
  { path: 'home', component: HomeComponent }
];

@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule]
})
export class AppRoutingModule {}
```

---

## Key Methods

| Method       | Purpose                         |
| ------------ | ------------------------------- |
| `forRoot()`  | Configure root routes           |
| `forChild()` | Configure feature module routes |

---

## Purpose

* Registers routes
* Enables navigation
* Provides router directives

---

# Short Interview Answer

RouterModule is an Angular module used to configure and manage routing in an application.

---

# 78. What is Lazy Loading in Angular?

Lazy loading is a technique where modules are loaded **only when required**, instead of loading everything at once.

---

## Example

```ts
const routes: Routes = [
  {
    path: 'admin',
    loadChildren: () =>
      import('./admin/admin.module').then(m => m.AdminModule)
  }
];
```

---

## How It Works

```text
User clicks route → Module loads → Component renders
```

---

## Key Idea

```text
Load on demand
```

---

## Why Needed?

* Reduce initial load time
* Improve performance
* Optimize large applications

---

# Short Interview Answer

Lazy loading is a technique in Angular where modules are loaded only when needed, improving application performance.

---

# 79. Benefits of Lazy Loading

Lazy loading provides several advantages for large-scale applications.

---

## Main Benefits

### 1. Faster Initial Load

Only essential modules are loaded at startup.

---

### 2. Better Performance

Reduces bundle size and improves speed.

---

### 3. Optimized Resource Usage

Loads modules only when required.

---

### 4. Scalable Architecture

Useful for large enterprise applications.

---

### 5. Improved User Experience

Faster navigation after initial load.

---

## Example Scenario

```text
Login page loads first → Dashboard loads later
```

---

# Short Interview Answer

Lazy loading improves performance by loading modules only when needed, reducing initial load time and optimizing resource usage.

---

# 80. What is Route Guard?

A **route guard** is used to control access to routes in Angular.

It decides whether a user can:

* Access a route
* Leave a route
* Load a module

---

## Types of Route Guards

| Guard              | Purpose                 |
| ------------------ | ----------------------- |
| `CanActivate`      | Allow/deny route access |
| `CanDeactivate`    | Prevent leaving a route |
| `CanLoad`          | Control lazy loading    |
| `CanActivateChild` | Protect child routes    |

---

## Example

```ts
import { CanActivate } from '@angular/router';

export class AuthGuard implements CanActivate {

  canActivate(): boolean {
    return isLoggedIn;
  }
}
```

---

## Apply Guard

```ts
{
  path: 'dashboard',
  component: DashboardComponent,
  canActivate: [AuthGuard]
}
```

---

## Use Cases

* Authentication
* Authorization
* Prevent unsaved changes
* Secure admin routes

---

## Flow

```text
User tries to access route → Guard checks → Allow or block
```

---

# Short Interview Answer

A route guard is an Angular feature used to control access to routes based on conditions like authentication or authorization.




# 81. Types of Route Guards

Angular provides multiple **route guards** to control navigation and access.

---

## Main Types of Route Guards

| Guard              | Purpose                            |
| ------------------ | ---------------------------------- |
| `CanActivate`      | Allow/deny access to a route       |
| `CanActivateChild` | Protect child routes               |
| `CanDeactivate`    | Prevent leaving a route            |
| `CanLoad`          | Prevent lazy-loaded module loading |
| `Resolve`          | Pre-fetch data before route loads  |

---

## Summary Flow

```text
User Navigation → Guard Check → Allow / Block / Load Data
```

---

# Short Interview Answer

Angular provides route guards like CanActivate, CanDeactivate, CanLoad, CanActivateChild, and Resolve to control navigation and access.

---

# 82. What is CanActivate?

`CanActivate` is a guard used to control whether a route can be accessed.

---

## Example

```ts
import { CanActivate } from '@angular/router';

export class AuthGuard implements CanActivate {

  canActivate(): boolean {
    return isLoggedIn; // true → allow, false → block
  }
}
```

---

## Apply Guard

```ts
{
  path: 'dashboard',
  component: DashboardComponent,
  canActivate: [AuthGuard]
}
```

---

## Use Cases

* Authentication check
* Role-based access
* Secure routes

---

## Flow

```text
User tries → Guard checks → Access granted/denied
```

---

# Short Interview Answer

CanActivate is a route guard that determines whether a user can access a particular route.

---

# 83. What is CanDeactivate?

`CanDeactivate` is used to prevent users from leaving a route.

---

## Example

```ts
import { CanDeactivate } from '@angular/router';

export class ConfirmGuard implements CanDeactivate<any> {

  canDeactivate(component: any): boolean {
    return confirm("Are you sure you want to leave?");
  }
}
```

---

## Apply Guard

```ts
{
  path: 'form',
  component: FormComponent,
  canDeactivate: [ConfirmGuard]
}
```

---

## Use Cases

* Unsaved form data
* Confirmation dialogs
* Prevent accidental navigation

---

## Flow

```text
User tries to leave → Guard asks → Allow / Block
```

---

# Short Interview Answer

CanDeactivate is a route guard used to prevent navigation away from a component, typically to avoid losing unsaved data.

---

# 84. What is Resolve Guard?

`Resolve` guard is used to fetch data **before a route is activated**.

---

## Why Use Resolve?

```text
Load data → Then load component
```

---

## Example

```ts
import { Resolve } from '@angular/router';

export class UserResolver implements Resolve<any> {

  resolve() {
    return this.userService.getUsers();
  }
}
```

---

## Apply Resolver

```ts
{
  path: 'users',
  component: UserComponent,
  resolve: { users: UserResolver }
}
```

---

## Access Data

```ts
constructor(private route: ActivatedRoute) {}

ngOnInit() {
  console.log(this.route.snapshot.data['users']);
}
```

---

## Benefits

* Data ready before UI loads
* Better UX
* No loading flicker

---

## Flow

```text
Route → Resolve → Data Loaded → Component Loaded
```

---

# Short Interview Answer

Resolve guard pre-fetches data before a route loads, ensuring the component has required data before rendering.

---

# 85. What is Child Routing?

Child routing is used to define **nested routes inside a parent route**.

---

## Example

```ts
const routes: Routes = [
  {
    path: 'dashboard',
    component: DashboardComponent,
    children: [
      { path: 'profile', component: ProfileComponent },
      { path: 'settings', component: SettingsComponent }
    ]
  }
];
```

---

## Template

```html
<h1>Dashboard</h1>

<router-outlet></router-outlet>
```

---

## URL Example

```text
/dashboard/profile
/dashboard/settings
```

---

## Benefits

* Better structure
* Modular design
* Nested UI layouts

---

## How It Works

```text
Parent Component → Child Routes → Nested Views
```

---

# Short Interview Answer

Child routing allows defining nested routes within a parent route, enabling hierarchical navigation and layout in Angular applications.




# 86. What is Router Outlet?

Angular **`<router-outlet>`** is a directive that acts as a **placeholder** where Angular loads routed components.

---

## Example

```html id="2b5f3n"
<router-outlet></router-outlet>
```

---

## How It Works

```text id="7q1m8x"
Route changes → Angular loads component → Displays inside router-outlet
```

---

## Example Flow

```ts id="x3n9k1"
{ path: 'home', component: HomeComponent }
```

When user navigates to `/home`, Angular loads `HomeComponent` inside `<router-outlet>`.

---

## Multiple Router Outlets

Angular also supports named outlets:

```html id="j8l2v4"
<router-outlet name="sidebar"></router-outlet>
```

---

# Short Interview Answer

`<router-outlet>` is a directive that acts as a placeholder in which Angular dynamically loads components based on the current route.

---

# 87. Difference Between routerLink and href

| routerLink                 | href                   |
| -------------------------- | ---------------------- |
| Angular directive          | HTML attribute         |
| Works with Angular routing | Reloads full page      |
| SPA navigation             | Traditional navigation |
| No page refresh            | Page refresh occurs    |
| Uses Angular Router        | Uses browser default   |

---

## routerLink Example

```html id="c5v8x2"
<a routerLink="/home">Home</a>
```

---

## href Example

```html id="q1n7m4"
<a href="/home">Home</a>
```

---

## Key Difference

```text id="k9x2v5"
routerLink → No reload (SPA)
href → Full page reload
```

---

# Short Interview Answer

`routerLink` is used for Angular navigation without page reload, while `href` performs a full page reload using standard HTML navigation.

---

# 88. What is Dynamic Routing?

Dynamic routing means navigating to routes with **dynamic values (parameters)**.

---

## Example

```ts id="z8m3p6"
{ path: 'user/:id', component: UserComponent }
```

---

## URL Example

```text id="r2v7x1"
/user/101
/user/202
```

---

## Use Cases

* User profile pages
* Product details
* Blog posts

---

## Access Parameter

```ts id="y6n4k9"
this.route.snapshot.params['id'];
```

---

# Short Interview Answer

Dynamic routing allows passing dynamic values in the URL using route parameters like `/user/:id`.

---

# 89. How to Pass Route Parameters?

There are two main ways to pass data in routes.

---

# 1. Route Params (Path Parameters)

## Define Route

```ts id="v1k7m2"
{ path: 'user/:id', component: UserComponent }
```

---

## Navigate

```ts id="s8x4q6"
this.router.navigate(['/user', 101]);
```

---

## Get Value

```ts id="p3n9v1"
this.route.snapshot.params['id'];
```

---

# 2. Query Params

## Navigate

```ts id="t6m2k8"
this.router.navigate(['/users'], {
  queryParams: { page: 1 }
});
```

---

## URL

```text id="y5x1n3"
/users?page=1
```

---

## Get Value

```ts id="h9k3p7"
this.route.snapshot.queryParams['page'];
```

---

# Short Interview Answer

Route parameters can be passed using path parameters (`/user/:id`) or query parameters (`?page=1`) and accessed via ActivatedRoute.

---

# 90. Difference Between Query Params and Route Params

| Route Params            | Query Params            |
| ----------------------- | ----------------------- |
| Part of URL path        | Part of query string    |
| Mandatory (usually)     | Optional                |
| Defined in route config | Not defined in route    |
| Example: `/user/101`    | Example: `/user?id=101` |

---

## Example

### Route Params

```text id="m4x7p2"
/user/101
```

---

### Query Params

```text id="z1n6k9"
/user?id=101
```

---

## Access

```ts id="v7p3m8"
// Route param
this.route.snapshot.params['id'];

// Query param
this.route.snapshot.queryParams['id'];
```

---

## Key Difference

```text id="c2k8x4"
Route Params → Required identifier  
Query Params → Optional filters/settings
```

---

# Short Interview Answer

Route params are part of the URL path and used for required values, while query params are optional key-value pairs used for filters and additional data.




# 91. What is Template-Driven Form?

Angular **template-driven form** is a form approach where most of the logic is handled in the **HTML template** using directives.

It uses:

* `ngModel`
* `FormsModule`

---

## Example

```html id="2d8m4k"
<form #userForm="ngForm">
  
  <input name="username" ngModel required>

  <button type="submit">Submit</button>

</form>
```

---

## Features

* Simple to use
* Less code in TypeScript
* Uses two-way binding `[(ngModel)]`
* Good for small forms

---

## When to Use

* Simple forms
* Basic validation
* Quick development

---

# Short Interview Answer

Template-driven forms are Angular forms where form logic and validation are handled in the template using directives like `ngModel`.

---

# 92. What is Reactive Form?

Reactive forms are form controls created and managed in the **component class (TypeScript)**.

They use:

* `FormGroup`
* `FormControl`
* `ReactiveFormsModule`

---

## Example

```ts id="9f3k7p"
import { FormGroup, FormControl } from '@angular/forms';

form = new FormGroup({
  username: new FormControl('')
});
```

---

## Template

```html id="8n2q5x"
<form [formGroup]="form">
  <input formControlName="username">
</form>
```

---

## Features

* More control
* Better validation
* Scalable
* Reactive programming style

---

## When to Use

* Complex forms
* Dynamic forms
* Advanced validation

---

# Short Interview Answer

Reactive forms are Angular forms where form logic is handled in the component using FormGroup and FormControl, providing better control and scalability.

---

# 93. Difference Between Template-Driven and Reactive Forms

| Template-Driven    | Reactive Forms             |
| ------------------ | -------------------------- |
| Logic in HTML      | Logic in TypeScript        |
| Uses `ngModel`     | Uses FormGroup/FormControl |
| Simple to use      | More powerful              |
| Less scalable      | Highly scalable            |
| Limited validation | Advanced validation        |

---

## Key Difference

```text id="9p4x2k"
Template-driven → Template-based  
Reactive → Code-based
```

---

# Short Interview Answer

Template-driven forms rely on the template for logic, while reactive forms manage form logic in the component, making them more scalable and testable.

---

# 94. What is FormGroup?

`FormGroup` is a collection of form controls.

It represents a **form or a section of a form**.

---

## Example

```ts id="q7m2p9"
form = new FormGroup({
  username: new FormControl(''),
  email: new FormControl('')
});
```

---

## Template

```html id="x8k5n3"
<form [formGroup]="form">
  <input formControlName="username">
  <input formControlName="email">
</form>
```

---

## Purpose

* Group multiple fields
* Manage form state
* Handle validation

---

## Access Value

```ts id="u4p7k1"
this.form.value
```

---

# Short Interview Answer

FormGroup is a collection of FormControls used to manage a group of form fields in reactive forms.

---

# 95. What is FormControl?

`FormControl` represents a single form field.

---

## Example

```ts id="z5k2p8"
username = new FormControl('');
```

---

## Template

```html id="k3p9m1"
<input [formControl]="username">
```

---

## With Validation

```ts id="t6p1x4"
username = new FormControl('', Validators.required);
```

---

## Features

* Tracks value
* Tracks validation status
* Tracks user interaction

---

## Access Value

```ts id="v1k8p3"
this.username.value
```

---

# Short Interview Answer

FormControl represents a single form input field in reactive forms and is used to track its value and validation state.





# 96. What is FormArray?

Angular **FormArray** is used to manage a **dynamic list of form controls**.

It is useful when you don’t know how many fields will be needed.

---

## Example

```ts id="q1w2e3"
import { FormArray, FormControl } from '@angular/forms';

skills = new FormArray([
  new FormControl('Angular'),
  new FormControl('React')
]);
```

---

## Template

```html id="w4e5r6"
<div *ngFor="let skill of skills.controls; let i = index">
  <input [formControl]="skill">
</div>
```

---

## Add/Remove Controls

```ts id="r7t8y9"
// Add
this.skills.push(new FormControl(''));

// Remove
this.skills.removeAt(0);
```

---

## Use Cases

* Dynamic forms
* Multiple inputs (skills, hobbies)
* Repeating fields

---

# Short Interview Answer

FormArray is used in reactive forms to manage a dynamic collection of form controls.

---

# 97. What are Validators?

Validators are functions used to validate form inputs.

They ensure user input is correct and meets required conditions.

---

## Built-in Validators

```ts id="a1s2d3"
import { Validators } from '@angular/forms';

username = new FormControl('', [
  Validators.required,
  Validators.minLength(3)
]);
```

---

## Common Validators

| Validator | Purpose                 |
| --------- | ----------------------- |
| required  | Field must not be empty |
| minLength | Minimum length          |
| maxLength | Maximum length          |
| pattern   | Match regex             |
| email     | Valid email             |

---

## Example

```html id="f4g5h6"
<input formControlName="username">
```

---

## Validation Check

```ts id="j7k8l9"
this.form.get('username')?.valid
```

---

# Short Interview Answer

Validators are functions used to validate form inputs and ensure correct data entry in Angular forms.

---

# 98. What is Custom Validator?

A custom validator is a user-defined validation function.

Used when built-in validators are not sufficient.

---

## Example

```ts id="z1x2c3"
import { AbstractControl } from '@angular/forms';

function noSpaceValidator(control: AbstractControl) {
  return control.value.includes(' ')
    ? { noSpace: true }
    : null;
}
```

---

## Apply Validator

```ts id="v4b5n6"
username = new FormControl('', [noSpaceValidator]);
```

---

## Error Handling

```html id="m7n8b9"
<div *ngIf="username.errors?.noSpace">
  No spaces allowed
</div>
```

---

## Use Cases

* Password validation
* Custom business rules
* Complex conditions

---

# Short Interview Answer

A custom validator is a user-defined function used to implement custom validation logic in Angular forms.

---

# 99. Difference Between setValue and patchValue

| setValue                      | patchValue                |
| ----------------------------- | ------------------------- |
| Must update all fields        | Can update partial fields |
| Strict matching required      | Flexible                  |
| Throws error if missing field | No error                  |

---

## Example — setValue

```ts id="q2w3e4"
this.form.setValue({
  username: 'Sai',
  email: 'test@mail.com'
});
```

❌ Error if any field is missing

---

## Example — patchValue

```ts id="r5t6y7"
this.form.patchValue({
  username: 'Sai'
});
```

✔ Only updates provided field

---

## Key Difference

```text id="u8i9o0"
setValue → Strict  
patchValue → Flexible
```

---

# Short Interview Answer

setValue updates all form fields and requires exact structure, while patchValue updates only specified fields and is more flexible.

---

# 100. How to Handle Dynamic Forms?

Dynamic forms are forms where fields can be added/removed at runtime.

---

## Steps to Handle Dynamic Forms

### 1. Use FormArray

```ts id="p1o2i3"
form = new FormGroup({
  skills: new FormArray([])
});
```

---

### 2. Add Controls Dynamically

```ts id="u4y5t6"
addSkill() {
  this.skills.push(new FormControl(''));
}
```

---

### 3. Remove Controls

```ts id="r7e8w9"
removeSkill(index: number) {
  this.skills.removeAt(index);
}
```

---

### 4. Template

```html id="q0w9e8"
<div formArrayName="skills">
  <div *ngFor="let skill of skills.controls; let i = index">
    <input [formControlName]="i">
    <button (click)="removeSkill(i)">Remove</button>
  </div>
</div>

<button (click)="addSkill()">Add Skill</button>
```

---

## Use Cases

* Survey forms
* Dynamic questionnaires
* Add/remove fields

---

## Best Practices

* Use Reactive Forms
* Use FormArray
* Validate dynamically

---

# Short Interview Answer

Dynamic forms in Angular are handled using FormArray, allowing fields to be added or removed at runtime dynamically.





# 101. What is FormBuilder?

Angular **FormBuilder** is a service that helps create reactive forms **quickly and with less code**.

It provides shortcuts for creating:

* `FormGroup`
* `FormControl`
* `FormArray`

---

## Without FormBuilder

```ts
form = new FormGroup({
  username: new FormControl(''),
  email: new FormControl('')
});
```

---

## With FormBuilder

```ts
import { FormBuilder } from '@angular/forms';

constructor(private fb: FormBuilder) {}

form = this.fb.group({
  username: [''],
  email: ['']
});
```

---

## Advantages

* Less boilerplate code
* Cleaner syntax
* Faster development

---

# Short Interview Answer

FormBuilder is an Angular service that simplifies the creation of reactive forms by reducing boilerplate code.

---

# 102. How to Reset Forms?

You can reset forms using the `reset()` method.

---

## Reactive Form

```ts
this.form.reset();
```

---

## With Default Values

```ts
this.form.reset({
  username: 'Sai'
});
```

---

## Template-Driven Form

```html
<form #formRef="ngForm">
```

```ts
formRef.reset();
```

---

## What reset() Does

* Clears values
* Resets validation
* Marks fields as pristine

---

# Short Interview Answer

Forms can be reset in Angular using the `reset()` method, which clears values and resets validation states.

---

# 103. How to Show Validation Messages?

Validation messages are shown using:

* Form control state
* Error conditions

---

## Example

```html
<input formControlName="username">

<div *ngIf="form.get('username')?.touched && form.get('username')?.invalid">
  
  <small *ngIf="form.get('username')?.errors?.required">
    Username is required
  </small>

  <small *ngIf="form.get('username')?.errors?.minlength">
    Minimum 3 characters required
  </small>

</div>
```

---

## Key Conditions

| Condition | Meaning           |
| --------- | ----------------- |
| `touched` | User interacted   |
| `dirty`   | Value changed     |
| `invalid` | Validation failed |

---

## Best Practice

```text
Show errors only after user interaction (touched/dirty)
```

---

# Short Interview Answer

Validation messages are displayed using form control states like touched, dirty, and invalid along with error conditions.

---

# 104. What is Async Validator?

An **async validator** is used for validation that requires:

* Server calls
* Delayed responses
* Asynchronous operations

---

## Example Use Case

* Check username availability
* Validate email from server

---

## Example

```ts
import { AbstractControl } from '@angular/forms';
import { of } from 'rxjs';
import { delay, map } from 'rxjs/operators';

function usernameValidator(control: AbstractControl) {
  return of(control.value).pipe(
    delay(1000),
    map(value => value === 'admin' ? { notAvailable: true } : null)
  );
}
```

---

## Apply Async Validator

```ts
username = new FormControl('', [], [usernameValidator]);
```

---

## Key Point

```text
Runs asynchronously (returns Observable or Promise)
```

---

# Short Interview Answer

Async validators are used to perform validation that involves asynchronous operations like API calls, returning Observables or Promises.

---

# 105. How to Disable Form Controls Dynamically?

You can enable/disable form controls using methods.

---

## Disable Control

```ts
this.form.get('username')?.disable();
```

---

## Enable Control

```ts
this.form.get('username')?.enable();
```

---

## Disable While Creating

```ts
username = new FormControl({ value: '', disabled: true });
```

---

## Template Example

```html
<input formControlName="username">
<button (click)="form.get('username')?.disable()">Disable</button>
```

---

## Disable Entire Form

```ts
this.form.disable();
```

---

## Use Cases

* Role-based access
* Conditional fields
* Step-by-step forms

---

# Short Interview Answer

Form controls can be dynamically disabled or enabled in Angular using `disable()` and `enable()` methods.




# 106. What is HttpClient?

Angular **HttpClient** is a service used to make HTTP requests to backend APIs.

It is part of:

```ts
@angular/common/http
```

---

## Import

```ts
import { HttpClient } from '@angular/common/http';
```

---

## Example

```ts
constructor(private http: HttpClient) {}

getUsers() {
  return this.http.get('https://api.example.com/users');
}
```

---

## Features

* Supports GET, POST, PUT, DELETE
* Returns Observables
* Handles JSON automatically
* Supports interceptors

---

# Short Interview Answer

HttpClient is an Angular service used to make HTTP requests to backend APIs and handle responses using Observables.

---

# 107. How to Make API Calls in Angular?

API calls are made using **HttpClient**.

---

## Step 1: Import Module

```ts
import { HttpClientModule } from '@angular/common/http';
```

---

## Step 2: Inject HttpClient

```ts
constructor(private http: HttpClient) {}
```

---

## Step 3: Make API Call

### GET Example

```ts
this.http.get('https://api.example.com/users')
  .subscribe(res => console.log(res));
```

---

### POST Example

```ts
this.http.post('https://api.example.com/users', {
  name: 'Sai'
}).subscribe(res => console.log(res));
```

---

## Best Practice

✔ Use services for API calls

```ts
getUsers() {
  return this.http.get('/api/users');
}
```

---

# Short Interview Answer

API calls in Angular are made using HttpClient by injecting it into components or services and calling methods like get() or post().

---

# 108. Difference Between GET and POST

| GET               | POST               |
| ----------------- | ------------------ |
| Fetch data        | Send data          |
| Data in URL       | Data in body       |
| No side effects   | Can modify server  |
| Limited data size | Large data allowed |
| Cached            | Not cached         |

---

## Example — GET

```ts
this.http.get('/api/users');
```

---

## Example — POST

```ts
this.http.post('/api/users', { name: 'Sai' });
```

---

## Key Difference

```text
GET → Retrieve data  
POST → Send/Create data
```

---

# Short Interview Answer

GET is used to retrieve data from a server, while POST is used to send data to a server for creation or processing.

---

# 109. How to Handle API Errors?

Errors can be handled using RxJS operators like:

```ts
catchError
```

---

## Example

```ts
import { catchError } from 'rxjs/operators';
import { throwError } from 'rxjs';

this.http.get('/api/users').pipe(
  catchError(error => {
    console.error('Error:', error);
    return throwError(() => error);
  })
).subscribe();
```

---

## Types of Errors

* Network errors
* Server errors (500)
* Client errors (400)

---

## Best Practices

* Show user-friendly messages
* Log errors
* Use interceptors for global handling

---

# Short Interview Answer

API errors in Angular are handled using RxJS operators like catchError and can also be managed globally using interceptors.

---

# 110. What is Interceptor in API Calls?

An **interceptor** is a middleware that intercepts HTTP requests and responses globally.

---

## Flow

```text
Request → Interceptor → Server → Interceptor → Response
```

---

## Example

```ts
import { HttpInterceptor } from '@angular/common/http';

export class AuthInterceptor implements HttpInterceptor {

  intercept(req: any, next: any) {

    const modifiedReq = req.clone({
      headers: req.headers.set('Authorization', 'Bearer token')
    });

    return next.handle(modifiedReq);
  }
}
```

---

## Register Interceptor

```ts
providers: [
  { provide: HTTP_INTERCEPTORS, useClass: AuthInterceptor, multi: true }
]
```

---

## Use Cases

* Add auth token
* Handle errors
* Logging
* Modify requests

---

# Short Interview Answer

An interceptor is an Angular feature that intercepts HTTP requests and responses globally to modify them or handle tasks like authentication and error handling.




# 111. How to Add Auth Token in Headers?

Angular the best way to add an auth token is using an **HTTP Interceptor**.

---

## Using Interceptor (Recommended)

```ts id="a1b2c3"
import { HttpInterceptor } from '@angular/common/http';

export class AuthInterceptor implements HttpInterceptor {

  intercept(req: any, next: any) {

    const token = "your-token";

    const modifiedReq = req.clone({
      headers: req.headers.set('Authorization', `Bearer ${token}`)
    });

    return next.handle(modifiedReq);
  }
}
```

---

## Register Interceptor

```ts id="d4e5f6"
providers: [
  { provide: HTTP_INTERCEPTORS, useClass: AuthInterceptor, multi: true }
]
```

---

## Alternative (Manual Way)

```ts id="g7h8i9"
this.http.get('/api/users', {
  headers: {
    Authorization: 'Bearer token'
  }
});
```

---

## Best Practice

✔ Use interceptor for global handling
✔ Avoid repeating headers in every API call

---

# Short Interview Answer

Auth tokens are added using HTTP interceptors by modifying request headers globally before sending them to the server.

---

# 112. What is retry Operator in RxJS?

RxJS **retry** operator is used to automatically retry a failed observable.

---

## Example

```ts id="j1k2l3"
import { retry } from 'rxjs/operators';

this.http.get('/api/data')
  .pipe(retry(3)) // retry 3 times
  .subscribe();
```

---

## How It Works

```text id="m4n5o6"
Error occurs → Retry again → Repeat until limit
```

---

## Use Cases

* Network failures
* Temporary server issues
* API retry logic

---

## Important Note

```text id="p7q8r9"
Retries only on error
```

---

# Short Interview Answer

The retry operator in RxJS automatically retries a failed observable a specified number of times.

---

# 113. What is switchMap?

`switchMap` is an RxJS operator that:

* Cancels previous request
* Switches to a new observable

---

## Example

```ts id="s1t2u3"
import { switchMap } from 'rxjs/operators';

searchInput.valueChanges.pipe(
  switchMap(value => this.http.get(`/api/search?q=${value}`))
).subscribe();
```

---

## How It Works

```text id="v4w5x6"
New request → Cancel old request → Execute latest
```

---

## Use Cases

* Search autocomplete
* Live API calls
* Avoid duplicate requests

---

# Short Interview Answer

switchMap cancels the previous observable and switches to a new one, making it ideal for handling rapid user inputs like search.

---

# 114. What is mergeMap?

`mergeMap` is an RxJS operator that:

* Executes multiple observables simultaneously
* Does NOT cancel previous requests

---

## Example

```ts id="y7z8a9"
import { mergeMap } from 'rxjs/operators';

from([1, 2, 3]).pipe(
  mergeMap(id => this.http.get(`/api/user/${id}`))
).subscribe();
```

---

## How It Works

```text id="b1c2d3"
Multiple requests → Run in parallel → All results returned
```

---

## Use Cases

* Multiple API calls
* Parallel processing
* Batch operations

---

# Short Interview Answer

mergeMap maps values to multiple observables and executes them concurrently without cancelling previous ones.

---

# 115. Difference Between switchMap and mergeMap

| switchMap                  | mergeMap                  |
| -------------------------- | ------------------------- |
| Cancels previous request   | Keeps all requests        |
| Only latest result matters | All results matter        |
| Used for search            | Used for parallel calls   |
| Prevents race conditions   | May cause race conditions |

---

## Example Scenario

### switchMap

```text id="e4f5g6"
User typing → Only latest API call executed
```

---

### mergeMap

```text id="h7i8j9"
Multiple clicks → All API calls executed
```

---

## Key Difference

```text id="k1l2m3"
switchMap → Cancel previous  
mergeMap → Run all
```

---

# Short Interview Answer

switchMap cancels previous observables and processes only the latest one, while mergeMap processes all observables concurrently without cancellation.




# 116. What is forkJoin?

RxJS **`forkJoin`** is used to execute multiple observables **in parallel** and get the result **only when all complete**.

---

## Example

```ts id="a1f2g3"
import { forkJoin } from 'rxjs';

forkJoin({
  users: this.http.get('/api/users'),
  posts: this.http.get('/api/posts')
}).subscribe(res => {
  console.log(res.users);
  console.log(res.posts);
});
```

---

## How It Works

```text id="b4h5i6"
Run all APIs → Wait for all → Return combined result
```

---

## Key Points

* Executes in parallel
* Emits once (after all complete)
* Fails if any request fails

---

## Use Cases

* Dashboard data loading
* Multiple API calls together

---

# Short Interview Answer

forkJoin executes multiple observables in parallel and returns the final result when all observables complete.

---

# 117. What is debounceTime?

`debounceTime` delays emitting values until a specified time has passed without new input.

---

## Example

```ts id="c7j8k9"
import { debounceTime } from 'rxjs/operators';

searchInput.valueChanges.pipe(
  debounceTime(500)
).subscribe(value => console.log(value));
```

---

## How It Works

```text id="d1l2m3"
User types → Wait 500ms → Emit value
```

---

## Use Cases

* Search input
* API optimization
* Avoid unnecessary calls

---

## Key Benefit

```text id="e4n5o6"
Reduces API calls
```

---

# Short Interview Answer

debounceTime delays emitting values until a specified time has passed, helping reduce unnecessary operations like API calls.

---

# 118. What is catchError?

`catchError` is an RxJS operator used to handle errors in observables.

---

## Example

```ts id="f7p8q9"
import { catchError } from 'rxjs/operators';
import { throwError } from 'rxjs';

this.http.get('/api/users').pipe(
  catchError(error => {
    console.error(error);
    return throwError(() => error);
  })
).subscribe();
```

---

## How It Works

```text id="g1r2s3"
Error occurs → catchError handles → Return fallback/error
```

---

## Use Cases

* API error handling
* Fallback values
* Logging errors

---

# Short Interview Answer

catchError is used to handle errors in an observable stream and return a fallback value or error.

---

# 119. What is tap Operator?

`tap` is used for **side effects** without modifying the data.

---

## Example

```ts id="h4t5u6"
import { tap } from 'rxjs/operators';

this.http.get('/api/users').pipe(
  tap(data => console.log('Response:', data))
).subscribe();
```

---

## How It Works

```text id="i7v8w9"
Data flows → tap logs/acts → Data continues unchanged
```

---

## Use Cases

* Logging
* Debugging
* Trigger side effects

---

## Important Note

```text id="j1x2y3"
Does NOT change data
```

---

# Short Interview Answer

tap is an RxJS operator used to perform side effects like logging without altering the data stream.

---

# 120. What is Unsubscribe and Why Important?

**Unsubscribe** means stopping an observable from emitting values.

---

## Example

```ts id="k4z5a6"
subscription = this.http.get('/api/users')
  .subscribe();

ngOnDestroy() {
  this.subscription.unsubscribe();
}
```

---

## Why Important?

### 1. Prevent Memory Leaks

Observables keep running if not unsubscribed.

---

### 2. Improve Performance

Stops unnecessary processing.

---

### 3. Avoid Duplicate Calls

Prevents multiple active subscriptions.

---

## When to Unsubscribe?

* Long-lived observables
* Interval/timer
* ValueChanges

---

## Not Needed For

* HTTP calls (auto-complete)
* Async pipe (auto unsubscribe)

---

## Alternative

```html id="l7b8c9"
{{ data$ | async }}
```

---

# Short Interview Answer

Unsubscribe is used to stop an observable stream, preventing memory leaks and improving performance in Angular applications.




# 121. What is State Management?

Angular **state management** is the process of managing and sharing application data (state) across components in a predictable way.

---

## What is State?

State = Application data

Examples:

* User data
* API responses
* UI state (loading, error)

---

## Why Needed?

* Share data across components
* Avoid complex data flow
* Maintain consistency

---

## Example

```text id="v1s2t3"
Login → Store user data → Access anywhere
```

---

## Approaches in Angular

* Services (simple apps)
* NgRx (large apps)

---

# Short Interview Answer

State management is the process of managing and sharing application data across components in a consistent and predictable way.

---

# 122. What is NgRx?

NgRx is a state management library for Angular based on **Redux pattern** and **RxJS**.

---

## Key Idea

```text id="x4y5z6"
Centralized store → Single source of truth
```

---

## Core Concepts

| Concept  | Purpose             |
| -------- | ------------------- |
| Store    | Holds state         |
| Action   | Describes event     |
| Reducer  | Updates state       |
| Effect   | Handles async tasks |
| Selector | Gets data           |

---

## Flow

```text id="a7b8c9"
Component → Action → Reducer → Store → Component
```

---

## Use Cases

* Large applications
* Complex state
* Predictable data flow

---

# Short Interview Answer

NgRx is a state management library for Angular that uses a centralized store and follows the Redux pattern with RxJS.

---

# 123. Difference Between NgRx and Services

| NgRx                          | Services            |
| ----------------------------- | ------------------- |
| Centralized state             | Distributed state   |
| Uses store, actions, reducers | Simple classes      |
| Predictable data flow         | Less structured     |
| Best for large apps           | Best for small apps |
| More boilerplate              | Less code           |

---

## Key Difference

```text id="d1e2f3"
NgRx → Structured & scalable  
Service → Simple & flexible
```

---

## When to Use

* Use Services → Small apps
* Use NgRx → Large apps

---

# Short Interview Answer

NgRx provides structured, centralized state management, while services offer simpler and more flexible data sharing.

---

# 124. What is Store in NgRx?

The **Store** is the central place where the application state is stored.

---

## Key Points

* Single source of truth
* Immutable state
* Read-only

---

## Example

```ts id="g4h5i6"
this.store.select('users').subscribe(data => {
  console.log(data);
});
```

---

## Flow

```text id="j7k8l9"
Store → Provides data → Component
```

---

## Benefits

* Centralized data
* Easy debugging
* Predictable updates

---

# Short Interview Answer

Store in NgRx is a centralized place that holds the entire application state as a single source of truth.

---

# 125. What is Action in NgRx?

An **action** is an event that describes **what happened** in the application.

---

## Example

```ts id="m1n2o3"
import { createAction } from '@ngrx/store';

export const loadUsers = createAction('[User] Load Users');
```

---

## Action with Payload

```ts id="p4q5r6"
export const addUser = createAction(
  '[User] Add User',
  props<{ user: any }>()
);
```

---

## Key Points

* Describes event
* Sent to reducer
* Triggers state change

---

## Flow

```text id="s7t8u9"
Component → Dispatch Action → Reducer updates state
```

---

# Short Interview Answer

An action in NgRx is an object that represents an event or change in the application and is used to trigger state updates.




# 126. What is Reducer in NgRx?

NgRx **reducer** is a pure function that updates the state based on an action.

---

## Key Idea

```text
Current State + Action → New State
```

---

## Example

```ts
import { createReducer, on } from '@ngrx/store';
import { addUser } from './user.actions';

export const initialState = [];

export const userReducer = createReducer(
  initialState,
  on(addUser, (state, { user }) => [...state, user])
);
```

---

## Characteristics

* Pure function (no side effects)
* Immutable (does not modify original state)
* Returns new state

---

## Flow

```text
Action → Reducer → Updated State → Store
```

---

# Short Interview Answer

A reducer is a pure function that takes the current state and an action, and returns a new updated state.

---

# 127. What is Effect in NgRx?

**Effect** is used to handle **side effects** like:

* API calls
* Async operations
* External interactions

---

## Why Effects?

Reducers must be pure, so async logic is handled in effects.

---

## Example

```ts
import { createEffect, ofType } from '@ngrx/effects';
import { loadUsers } from './user.actions';

loadUsers$ = createEffect(() =>
  this.actions$.pipe(
    ofType(loadUsers),
    switchMap(() => this.http.get('/api/users')
      .pipe(
        map(users => loadUsersSuccess({ users }))
      )
    )
  )
);
```

---

## Flow

```text
Action → Effect → API Call → New Action → Reducer
```

---

## Use Cases

* API calls
* Logging
* Navigation

---

# Short Interview Answer

An effect in NgRx handles side effects like API calls and dispatches new actions after completing asynchronous tasks.

---

# 128. What is Selector in NgRx?

A **selector** is used to get specific data from the store.

---

## Example

```ts
import { createSelector } from '@ngrx/store';

export const selectUsers = createSelector(
  (state: any) => state.users,
  (users) => users
);
```

---

## Usage

```ts
this.store.select(selectUsers).subscribe(data => {
  console.log(data);
});
```

---

## Benefits

* Reusable
* Efficient (memoized)
* Clean code

---

## Key Idea

```text
Store → Selector → Component
```

---

# Short Interview Answer

A selector is a function used to retrieve specific pieces of state from the NgRx store.

---

# 129. What is Entity Adapter?

**Entity Adapter** is a utility in NgRx used to manage collections of data efficiently.

---

## Why Use It?

* Simplifies CRUD operations
* Improves performance
* Reduces boilerplate

---

## Example

```ts
import { createEntityAdapter } from '@ngrx/entity';

const adapter = createEntityAdapter();

const initialState = adapter.getInitialState();
```

---

## Features

* Auto CRUD methods (`addOne`, `updateOne`, etc.)
* Normalized state structure
* Better performance

---

## State Structure

```text
{
  ids: [],
  entities: {}
}
```

---

# Short Interview Answer

Entity Adapter is a utility in NgRx that helps manage collections of data efficiently with built-in CRUD operations.

---

# 130. What is Redux Pattern?

The **Redux pattern** is a state management pattern based on:

* Single source of truth
* Immutable state
* Unidirectional data flow

---

## Flow

```text
Component → Action → Reducer → Store → Component
```

---

## Core Principles

### 1. Single Source of Truth

All state is stored in one place (store)

---

### 2. Immutable State

State cannot be changed directly

---

### 3. Unidirectional Flow

Data flows in one direction

---

## Used In

* NgRx
* Redux (React)

---

## Benefits

* Predictable state
* Easy debugging
* Scalable architecture

---

# Short Interview Answer

Redux pattern is a state management approach that uses a single store, immutable state, and unidirectional data flow for predictable application behavior.




# 131. What is Immutable State?

NgRx **immutable state** means the state **cannot be changed directly**.
Instead, you always create a **new copy of the state** when updating it.

---

## Example

❌ Wrong (mutable)

```ts
state.users.push(newUser);
```

✅ Correct (immutable)

```ts
return {
  ...state,
  users: [...state.users, newUser]
};
```

---

## Why Important?

* Predictable updates
* Easy debugging
* Works well with NgRx & change detection

---

## Key Idea

```text
Old State → Copy → Modify → New State
```

---

# Short Interview Answer

Immutable state means the original state is never modified directly; instead, a new updated state is returned.

---

# 132. What is Signal in Angular?

Angular **Signal** is a reactive primitive introduced in Angular (v16+) for managing state.

It holds a value and automatically updates the UI when the value changes.

---

## Example

```ts
import { signal } from '@angular/core';

count = signal(0);

// update
this.count.set(1);

// read
this.count();
```

---

## Key Features

* Reactive state
* Auto UI updates
* Simpler than RxJS
* No subscription needed

---

## How It Works

```text
Value changes → Signal updates → UI updates automatically
```

---

# Short Interview Answer

A signal is a reactive state container in Angular that automatically updates the UI when its value changes.

---

# 133. Difference Between Signals and Observables

| Signals                 | Observables            |
| ----------------------- | ---------------------- |
| Simple state management | Complex async streams  |
| No subscription needed  | Requires subscribe()   |
| Synchronous by default  | Asynchronous           |
| Easy to use             | More powerful          |
| Used for UI state       | Used for API & streams |

---

## Key Difference

```text
Signals → Simple reactive state  
Observables → Async data streams
```

---

## When to Use

* Signals → UI state
* Observables → API calls, events

---

# Short Interview Answer

Signals are simple reactive state containers, while observables handle asynchronous data streams and require subscriptions.

---

# 134. What is Computed Signal?

A **computed signal** is a derived signal whose value depends on other signals.

---

## Example

```ts
import { signal, computed } from '@angular/core';

price = signal(100);
quantity = signal(2);

total = computed(() => this.price() * this.quantity());
```

---

## How It Works

```text
Dependency changes → Computed recalculates → UI updates
```

---

## Features

* Auto recalculation
* Read-only
* Efficient

---

# Short Interview Answer

A computed signal is a derived reactive value that automatically updates when its dependent signals change.

---

# 135. What is Effect in Signals?

An **effect** is used to run side effects when signals change.

---

## Example

```ts
import { effect } from '@angular/core';

effect(() => {
  console.log(this.count());
});
```

---

## How It Works

```text
Signal changes → Effect runs automatically
```

---

## Use Cases

* Logging
* API calls
* Local storage updates

---

## Important Note

```text
Used for side effects, not for deriving values
```

---

# Short Interview Answer

An effect in Angular signals runs side-effect logic automatically whenever a signal value changes.

---

# 136. When Should You Use NgRx?

Use NgRx when your application becomes complex.

---

## Use NgRx When

### 1. Large Application

Multiple modules and components

---

### 2. Complex State

Shared data across many components

---

### 3. Predictable State Needed

Strict and consistent data flow

---

### 4. Advanced Features

* Undo/redo
* Debugging
* State tracking

---

### 5. Multiple API Calls

Complex async logic

---

## Avoid NgRx When

* Small apps
* Simple state
* Few components

---

## Alternative

```text
Small app → Services / Signals  
Large app → NgRx
```

---

# Short Interview Answer

NgRx should be used in large and complex Angular applications where centralized, predictable state management is required.





# 137. What is Change Detection?

Angular **change detection** is the process by which Angular **updates the UI when application data changes**.

---

## Key Idea

```text
Data changes → Angular detects → UI updates
```

---

## Example

```ts
name = "Sai";

updateName() {
  this.name = "Krishna";
}
```

```html
<h1>{{ name }}</h1>
```

👉 When `name` changes → UI updates automatically

---

## Why Important?

* Keeps UI in sync with data
* Automatic updates
* Improves developer productivity

---

# Short Interview Answer

Change detection is the mechanism Angular uses to update the DOM whenever the application state changes.

---

# 138. Types of Change Detection Strategies

Angular provides two main strategies:

---

## 1. Default Strategy

```ts
changeDetection: ChangeDetectionStrategy.Default
```

### Features

* Checks all components
* Runs frequently
* Automatic updates

---

## 2. OnPush Strategy

```ts
changeDetection: ChangeDetectionStrategy.OnPush
```

### Features

* Checks only when needed
* Better performance
* Requires immutable data

---

## Comparison

| Default                      | OnPush                     |
| ---------------------------- | -------------------------- |
| Checks entire component tree | Checks only specific cases |
| Less performance             | High performance           |
| Automatic                    | Optimized/manual control   |

---

# Short Interview Answer

Angular has two change detection strategies: Default (checks all components) and OnPush (checks only when necessary for better performance).

---

# 139. What is OnPush Strategy?

**OnPush** is an optimized change detection strategy.

Angular updates the component only when:

---

## Conditions

1. Input reference changes
2. Event occurs (click, input)
3. Observable emits new value
4. Manually triggered

---

## Example

```ts
import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-demo',
  template: `{{ data.name }}`,
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class DemoComponent {
  data = { name: 'Sai' };
}
```

---

## Important Note

❌ Won’t detect:

```ts
this.data.name = "Krishna"; // mutation
```

✅ Works with:

```ts
this.data = { name: "Krishna" }; // new reference
```

---

## Benefits

* Faster performance
* Less unnecessary checks
* Better scalability

---

# Short Interview Answer

OnPush is a change detection strategy that updates the component only when input references change or specific triggers occur, improving performance.

---

# 140. How Angular Change Detection Works?

Angular uses **Zone.js + component tree checking**.

---

## Step-by-Step Flow

```text
1. Event occurs (click, API, timer)
2. Zone.js detects change
3. Angular runs change detection
4. Compares old vs new values
5. Updates DOM if needed
```

---

## Example Flow

```text
Button click → Value changes → Angular checks → UI updates
```

---

## Important Concepts

### 1. Zone.js

Tracks async operations

---

### 2. Dirty Checking

Compares values

---

### 3. Component Tree

Checks from root to child components

---

## Optimization

* Use OnPush
* Use trackBy in ngFor
* Avoid unnecessary bindings

---

# Short Interview Answer

Angular change detection works by tracking asynchronous events using Zone.js, then checking component values and updating the DOM when changes are detected.




# 141. What Causes Unnecessary Re-rendering?

Angular unnecessary re-rendering happens when Angular runs change detection more often than needed.

---

## Common Causes

### 1. Mutable Data Changes

```ts
this.users.push(newUser); // mutation
```

👉 Angular may re-render entire list

---

### 2. No trackBy in *ngFor

Without `trackBy`, Angular recreates all DOM elements.

---

### 3. Frequent Change Detection (Default Strategy)

* Every event triggers full component tree check

---

### 4. Functions in Template

```html
<p>{{ getValue() }}</p>
```

👉 Function runs on every change detection cycle

---

### 5. Large Component Tree

* Deep nested components
* Many bindings

---

## How to Avoid

✔ Use `OnPush` strategy
✔ Use `trackBy` in `ngFor`
✔ Avoid functions in templates
✔ Use immutable updates

---

# Short Interview Answer

Unnecessary re-rendering is caused by frequent change detection, mutable data, missing trackBy in ngFor, and heavy template logic.

---

# 142. What is Zone.js?

Zone.js is a library used by Angular to **track asynchronous operations**.

---

## What It Tracks

* setTimeout
* HTTP requests
* Events (click, input)
* Promises

---

## How It Works

```text
Async event → Zone.js detects → Angular runs change detection
```

---

## Example

```ts
setTimeout(() => {
  this.name = "Sai";
}, 1000);
```

👉 Zone.js detects timeout → Angular updates UI

---

## Why Needed?

* Automatic UI updates
* No manual change detection required

---

## Without Zone.js

You would need:

```ts
ChangeDetectorRef.detectChanges()
```

---

# Short Interview Answer

Zone.js is a library that tracks asynchronous operations and triggers Angular change detection automatically.

---

# 143. What is trackBy Function?

`trackBy` is used with `*ngFor` to improve performance by tracking items using a unique identifier.

---

## Without trackBy

```html
<div *ngFor="let user of users">
  {{ user.name }}
</div>
```

👉 Angular recreates all DOM elements on change

---

## With trackBy

```html
<div *ngFor="let user of users; trackBy: trackById">
  {{ user.name }}
</div>
```

---

## Component

```ts
trackById(index: number, item: any) {
  return item.id;
}
```

---

## How It Works

```text
Old list vs New list → Compare IDs → Update only changed items
```

---

## Benefits

* Better performance
* Avoid DOM re-creation
* Efficient rendering

---

## Use Case

* Large lists
* Frequent updates

---

# Short Interview Answer

trackBy is a function used with ngFor to uniquely identify items, allowing Angular to update only changed elements instead of re-rendering the entire list.




# 144. How to Optimize Large Lists?

Angular handling large lists efficiently is critical for performance.

---

## Best Techniques

### 1. Use `trackBy` in *ngFor

```html
<div *ngFor="let item of items; trackBy: trackById">
  {{ item.name }}
</div>
```

✔ Prevents full DOM re-render

---

### 2. Use OnPush Strategy

```ts
changeDetection: ChangeDetectionStrategy.OnPush
```

✔ Reduces unnecessary checks

---

### 3. Virtual Scrolling (Best for Large Data)

Use Angular CDK:

```html
<cdk-virtual-scroll-viewport itemSize="50">
  <div *cdkVirtualFor="let item of items">
    {{ item.name }}
  </div>
</cdk-virtual-scroll-viewport>
```

✔ Renders only visible items

---

### 4. Pagination

✔ Load data in chunks instead of all at once

---

### 5. Avoid Heavy Logic in Template

❌ Bad:

```html
{{ calculate(item) }}
```

✔ Move logic to component

---

### 6. Immutable Data Updates

```ts
this.items = [...this.items, newItem];
```

---

## Key Idea

```text
Render only what is needed
```

---

# Short Interview Answer

Large lists can be optimized using trackBy, OnPush change detection, virtual scrolling, pagination, and avoiding heavy template logic.

---

# 145. What is Memoization?

Memoization is an optimization technique where **results of expensive function calls are cached** and reused.

---

## Example

```ts
function memoize(fn: Function) {
  const cache: any = {};

  return function(value: number) {
    if (cache[value]) {
      return cache[value];
    }
    const result = fn(value);
    cache[value] = result;
    return result;
  };
}
```

---

## Simple Example

```ts
const square = memoize((n: number) => n * n);

square(5); // calculated
square(5); // from cache
```

---

## Benefits

* Improves performance
* Avoids repeated calculations
* Useful in heavy computations

---

## Use Cases

* Expensive calculations
* Derived data
* Selectors (NgRx uses memoization)

---

# Short Interview Answer

Memoization is a technique of caching function results to avoid repeated calculations and improve performance.

---

# 146. What is Lazy Component Loading?

Lazy component loading means loading a component **only when it is required**, instead of loading it at application startup.

---

## How It Works

```text
User action → Load component → Render
```

---

## Example (Standalone Component)

```ts
{
  path: 'profile',
  loadComponent: () =>
    import('./profile.component').then(m => m.ProfileComponent)
}
```

---

## Difference from Module Lazy Loading

| Module Lazy Loading  | Component Lazy Loading |
| -------------------- | ---------------------- |
| Loads entire module  | Loads single component |
| Traditional approach | Modern (Angular 14+)   |

---

## Benefits

* Faster initial load
* Smaller bundle size
* Better performance

---

## Use Cases

* Large apps
* Feature-based loading
* Rarely used components

---

# Short Interview Answer

Lazy component loading is a technique where components are loaded only when needed, improving performance and reducing initial load time.




# 147. How to Improve Angular Performance?

Angular performance optimization is a common interview topic—focus on practical techniques.

---

## Key Techniques

### 1. Use OnPush Change Detection

```ts
changeDetection: ChangeDetectionStrategy.OnPush
```

✔ Reduces unnecessary checks

---

### 2. Use trackBy in *ngFor

✔ Prevents full DOM re-render

---

### 3. Lazy Loading

✔ Load modules/components only when needed

---

### 4. Use Virtual Scrolling

✔ Render only visible items

---

### 5. Avoid Heavy Logic in Templates

❌ Bad:

```html
{{ calculate() }}
```

✔ Move logic to component

---

### 6. Use Pure Pipes

✔ Prevent unnecessary recalculations

---

### 7. Optimize API Calls

* Use `debounceTime`
* Use caching
* Avoid duplicate requests

---

### 8. Unsubscribe from Observables

✔ Prevent memory leaks

---

### 9. Use Signals (Modern Angular)

✔ Efficient reactivity without subscriptions

---

### 10. Enable Production Mode

```ts
enableProdMode();
```

✔ Removes debugging overhead

---

## Key Idea

```text
Minimize DOM updates + Reduce unnecessary work
```

---

# Short Interview Answer

Angular performance can be improved using OnPush, trackBy, lazy loading, pure pipes, optimized API calls, and minimizing unnecessary change detection.

---

# 148. What are Pure Pipes?

A **pure pipe** executes only when its **input value changes**.

---

## Default Behavior

All pipes in Angular are **pure by default**.

---

## Example

```ts
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'double',
  pure: true
})
export class DoublePipe implements PipeTransform {
  transform(value: number) {
    return value * 2;
  }
}
```

---

## How It Works

```text
Input changes → Pipe runs  
No change → Pipe NOT executed
```

---

## Example Usage

```html
{{ 10 | double }}
```

---

## Benefits

* Better performance
* Avoid unnecessary recalculation

---

## Important Note

❌ Won’t run for:

```ts
array.push() // mutation
```

✔ Works for:

```ts
array = [...array] // new reference
```

---

# Short Interview Answer

Pure pipes are Angular pipes that run only when input values change, improving performance by avoiding unnecessary recalculations.

---

# 149. How to Reduce Bundle Size?

Reducing bundle size improves load time and performance.

---

## Best Practices

### 1. Lazy Loading

✔ Load modules/components only when needed

---

### 2. Tree Shaking

✔ Remove unused code (automatic in Angular)

---

### 3. Use Production Build

```bash
ng build --configuration production
```

✔ Minifies and optimizes code

---

### 4. Avoid Large Libraries

✔ Use lightweight alternatives

---

### 5. Use Standalone Components

✔ Avoid unnecessary modules

---

### 6. Code Splitting

✔ Split app into smaller chunks

---

### 7. Optimize Assets

* Compress images
* Use modern formats (WebP)

---

### 8. Remove Unused Imports

✔ Keep code clean

---

### 9. Enable Compression (Gzip/Brotli)

✔ Reduce file size during delivery

---

## Key Idea

```text
Load less code → Faster app
```

---

# Short Interview Answer

Bundle size can be reduced using lazy loading, tree shaking, production builds, removing unused code, and optimizing assets.





# 150. What is Ahead-of-Time Compilation (AOT)?

Angular **AOT (Ahead-of-Time) compilation** compiles Angular templates and components **at build time (before the browser loads the app)**.

---

## How It Works

```text
Build time → Compile HTML + TS → Optimized JS → Browser loads fast
```

---

## Benefits

* Faster rendering
* Smaller bundle size
* Early error detection
* Better security

---

## Example

```bash
ng build --configuration production
```

👉 AOT is enabled by default in production builds

---

# Short Interview Answer

AOT compiles Angular templates at build time, resulting in faster rendering and smaller bundle sizes.

---

# 151. Difference Between AOT and JIT

| AOT                   | JIT                 |
| --------------------- | ------------------- |
| Compile at build time | Compile at runtime  |
| Faster performance    | Slower              |
| Smaller bundle        | Larger bundle       |
| Errors at build time  | Errors at runtime   |
| Used in production    | Used in development |

---

## Key Difference

```text
AOT → Before browser loads  
JIT → Inside browser
```

---

# Short Interview Answer

AOT compiles code during build time for better performance, while JIT compiles code in the browser at runtime.

---

# 152. What is Tree Shaking?

Tree shaking is a process of **removing unused code** from the final bundle.

---

## Example

```ts
import { Component } from '@angular/core';
// unused imports removed automatically
```

---

## How It Works

```text
Analyze code → Remove unused parts → Smaller bundle
```

---

## Benefits

* Reduced bundle size
* Faster load time
* Better performance

---

## Tools

* Webpack
* Angular CLI

---

# Short Interview Answer

Tree shaking removes unused code from the final bundle, reducing size and improving performance.

---

# 153. What is Differential Loading?

Differential loading means serving **different bundles for different browsers**.

---

## How It Works

* Modern browsers → ES2015+ bundle
* Older browsers → ES5 bundle

---

## Example

```text
Modern browser → Faster bundle  
Old browser → Compatible bundle
```

---

## Benefits

* Better compatibility
* Optimized performance

---

## Note

* Used in older Angular versions
* Modern Angular focuses less on this (ES2015+ default)

---

# Short Interview Answer

Differential loading serves different bundles for modern and older browsers to balance performance and compatibility.

---

# 154. What is Standalone Component?

A **standalone component** is a component that does **not require NgModule**.

---

## Example

```ts
import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'app-demo',
  template: `<h1>Hello</h1>`
})
export class DemoComponent {}
```

---

## Key Feature

```text
No NgModule required
```

---

## Imports Inside Component

```ts
@Component({
  standalone: true,
  imports: [CommonModule]
})
```

---

# Short Interview Answer

A standalone component is an Angular component that works independently without being declared in an NgModule.

---

# 155. Benefits of Standalone Components

## Advantages

### 1. Less Boilerplate

No need for NgModule

---

### 2. Faster Development

Simpler structure

---

### 3. Better Lazy Loading

Direct component loading

---

### 4. Improved Readability

Cleaner architecture

---

### 5. Better Performance

Smaller bundles

---

## Example Use

```ts
loadComponent: () =>
  import('./demo.component').then(m => m.DemoComponent)
```

---

## Key Idea

```text
Standalone → Simpler + Modern Angular
```

---

# Short Interview Answer

Standalone components reduce boilerplate, simplify development, and improve performance by eliminating the need for NgModules.



# 156. What are Standalone APIs?

Angular **Standalone APIs** are modern Angular APIs that allow you to build applications **without using NgModules**.

They are introduced to simplify Angular development.

---

## Key Standalone APIs

### 1. `bootstrapApplication`

```ts
import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app.component';

bootstrapApplication(AppComponent);
```

---

### 2. Standalone Components

```ts
@Component({
  standalone: true
})
```

---

### 3. provideRouter

```ts
import { provideRouter } from '@angular/router';

bootstrapApplication(AppComponent, {
  providers: [provideRouter(routes)]
});
```

---

### 4. provideHttpClient

```ts
import { provideHttpClient } from '@angular/common/http';

bootstrapApplication(AppComponent, {
  providers: [provideHttpClient()]
});
```

---

## Benefits

* No NgModules required
* Cleaner code
* Better tree-shaking
* Easier lazy loading

---

## Key Idea

```text
Standalone APIs → Module-less Angular
```

---

# Short Interview Answer

Standalone APIs are Angular APIs that allow building applications without NgModules, simplifying structure and improving performance.

---

# 157. What is Dynamic Component Loading?

Dynamic component loading means creating and rendering components **at runtime** instead of defining them in templates.

---

## Why Use It?

* Load components conditionally
* Build dynamic UI
* Reduce initial load

---

## Example (Modern Angular)

```ts
import { ViewContainerRef } from '@angular/core';

constructor(private vcr: ViewContainerRef) {}

loadComponent() {
  this.vcr.createComponent(DemoComponent);
}
```

---

## How It Works

```text
User action → Create component → Render dynamically
```

---

## Use Cases

* Modals
* Dialogs
* Dynamic dashboards
* Plugin systems

---

# Short Interview Answer

Dynamic component loading is the process of creating and rendering components at runtime instead of statically in templates.

---

# 158. What is Renderer2?

**Renderer2** is an Angular service used to safely manipulate the DOM.

---

## Why Not Direct DOM?

❌ Avoid:

```ts
document.getElementById('id');
```

✔ Use Renderer2 for:

* Security
* Cross-platform support
* SSR compatibility

---

## Example

```ts
import { Renderer2, ElementRef } from '@angular/core';

constructor(private renderer: Renderer2, private el: ElementRef) {}

ngOnInit() {
  this.renderer.setStyle(this.el.nativeElement, 'color', 'red');
}
```

---

## Common Methods

| Method       | Purpose            |
| ------------ | ------------------ |
| setStyle     | Apply CSS          |
| addClass     | Add class          |
| removeClass  | Remove class       |
| setAttribute | Set attribute      |
| listen       | Add event listener |

---

## Use Cases

* DOM manipulation
* Dynamic styling
* Event handling

---

## Key Idea

```text
Renderer2 → Safe DOM manipulation
```

---

# Short Interview Answer

Renderer2 is an Angular service used to safely manipulate the DOM in a platform-independent and secure way.




# 159. What is ElementRef?

Angular **ElementRef** is a wrapper around a DOM element that gives direct access to the native element.

---

## Example

```ts
import { ElementRef } from '@angular/core';

constructor(private el: ElementRef) {}

ngOnInit() {
  console.log(this.el.nativeElement);
}
```

---

## Access DOM

```ts
this.el.nativeElement.style.color = 'red';
```

---

## ⚠️ Important Note

Direct DOM manipulation using ElementRef is **not recommended**.

✔ Instead use:

* `Renderer2` (safer)

---

## Why Avoid Direct Access?

* Security issues (XSS risk)
* Not compatible with SSR
* Breaks Angular abstraction

---

## Key Idea

```text
ElementRef → Direct DOM access (use carefully)
```

---

# Short Interview Answer

ElementRef provides direct access to a DOM element, but it should be used carefully as direct DOM manipulation is not recommended.

---

# 160. What is HostListener?

**HostListener** is a decorator used to listen to events on the host element.

---

## Example

```ts
import { HostListener } from '@angular/core';

@HostListener('click')
onClick() {
  console.log('Element clicked');
}
```

---

## Listen to Window Events

```ts
@HostListener('window:resize', ['$event'])
onResize(event: any) {
  console.log(event);
}
```

---

## Use Cases

* Click events
* Keyboard events
* Window resize/scroll

---

## How It Works

```text
Event occurs → HostListener catches → Method executes
```

---

# Short Interview Answer

HostListener is a decorator used to listen to events on the host element and execute methods when those events occur.

---

# 161. What is HostBinding?

**HostBinding** is a decorator used to bind properties of the host element.

---

## Example

```ts
import { HostBinding } from '@angular/core';

@HostBinding('style.color') color = 'red';
```

---

## Dynamic Example

```ts
@HostBinding('class.active') isActive = true;
```

---

## Use Cases

* Dynamic styles
* Conditional classes
* Attribute binding

---

## How It Works

```text
Property changes → HostBinding updates DOM
```

---

## Key Idea

```text
HostBinding → Bind data to host element
```

---

# Short Interview Answer

HostBinding is used to bind component or directive properties to the host element’s properties like styles, classes, or attributes.




# 162. What is ViewEncapsulation?

Angular **ViewEncapsulation** controls how styles are applied to components and whether they are **scoped or global**.

---

## Key Idea

```text
Component styles → Apply only to that component (or not)
```

---

## Example

```ts
import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'app-demo',
  template: `<h1>Hello</h1>`,
  styles: [`h1 { color: red; }`],
  encapsulation: ViewEncapsulation.Emulated
})
export class DemoComponent {}
```

---

## Why Needed?

* Prevent style conflicts
* Keep styles modular
* Control CSS behavior

---

# Short Interview Answer

ViewEncapsulation defines how Angular applies component styles and whether they are scoped or global.

---

# 163. Types of ViewEncapsulation

Angular provides 3 types:

---

## 1. Emulated (Default)

```ts
encapsulation: ViewEncapsulation.Emulated
```

### Features

* Styles scoped to component
* Uses unique attributes (e.g., `_ngcontent`)
* No style leakage

✔ Most commonly used

---

## 2. None

```ts
encapsulation: ViewEncapsulation.None
```

### Features

* Styles are global
* Affect entire application

❌ Risk of conflicts

---

## 3. ShadowDom

```ts
encapsulation: ViewEncapsulation.ShadowDom
```

### Features

* Uses native Shadow DOM
* True style isolation

✔ Best isolation
❌ Limited browser support (older browsers)

---

## Comparison

| Type      | Scope     | Isolation |
| --------- | --------- | --------- |
| Emulated  | Component | Medium    |
| None      | Global    | None      |
| ShadowDom | Component | Strong    |

---

## Key Idea

```text
Emulated → Scoped (default)  
None → Global  
ShadowDom → Fully isolated
```

---

# Short Interview Answer

Angular supports Emulated (default scoped styles), None (global styles), and ShadowDom (true encapsulation using Shadow DOM).

---

# 164. What is Content Projection?

Content projection allows you to **pass content from a parent component into a child component**.

---

## Similar To

👉 Like `<slot>` in Web Components

---

## Example

### Child Component

```html
<ng-content></ng-content>
```

---

### Parent Component

```html
<app-card>
  <h2>Title</h2>
  <p>Description</p>
</app-card>
```

---

## Output

```text
Child component displays parent content inside ng-content
```

---

## Multiple Slots

```html
<ng-content select="h2"></ng-content>
<ng-content select="p"></ng-content>
```

---

## Use Cases

* Reusable UI components
* Layout wrappers (cards, modals)
* Customizable components

---

## Key Idea

```text
Parent content → Passed → Child template
```

---

# Short Interview Answer

Content projection is a technique in Angular that allows inserting content from a parent component into a child component using ng-content.







# 165. What is ng-content?

Angular **`<ng-content>`** is a directive used for **content projection**.

It allows you to insert **parent content into a child component template**.

---

## Example

### Child Component

```html
<div class="card">
  <ng-content></ng-content>
</div>
```

---

### Parent Component

```html
<app-card>
  <h2>Title</h2>
  <p>Description</p>
</app-card>
```

---

## Output

```text
Card component displays Title and Description inside it
```

---

## Multiple Slots

```html
<ng-content select="h2"></ng-content>
<ng-content select="p"></ng-content>
```

---

## Key Idea

```text
ng-content → Insert external content into component
```

---

# Short Interview Answer

ng-content is used in Angular for content projection, allowing parent content to be displayed inside a child component.

---

# 166. What is Custom Structural Directive?

A **custom structural directive** is a directive that changes the **structure of the DOM** by adding/removing elements.

---

## Structural Directive Symbol

```text
*
```

Examples:

* `*ngIf`
* `*ngFor`

---

## Create Custom Structural Directive

```ts
import { Directive, TemplateRef, ViewContainerRef } from '@angular/core';

@Directive({
  selector: '[appIf]'
})
export class IfDirective {

  constructor(
    private templateRef: TemplateRef<any>,
    private viewContainer: ViewContainerRef
  ) {}

  @Input() set appIf(condition: boolean) {
    if (condition) {
      this.viewContainer.createEmbeddedView(this.templateRef);
    } else {
      this.viewContainer.clear();
    }
  }
}
```

---

## Usage

```html
<div *appIf="isVisible">
  Show this content
</div>
```

---

## How It Works

```text
Condition true → Add DOM  
Condition false → Remove DOM
```

---

## Use Cases

* Conditional rendering
* Custom loops
* Permission-based UI

---

# Short Interview Answer

A custom structural directive is a directive that modifies the DOM structure by adding or removing elements dynamically.

---

# 167. What is Resolver?

A **resolver** is used to **fetch data before navigating to a route**.

---

## Why Use Resolver?

```text
Load data first → Then open page
```

---

## Example

```ts
import { Resolve } from '@angular/router';

export class UserResolver implements Resolve<any> {

  resolve() {
    return this.userService.getUsers();
  }
}
```

---

## Apply in Route

```ts
{
  path: 'users',
  component: UserComponent,
  resolve: { users: UserResolver }
}
```

---

## Access Data

```ts
constructor(private route: ActivatedRoute) {}

ngOnInit() {
  console.log(this.route.snapshot.data['users']);
}
```

---

## Benefits

* Data ready before UI loads
* Better user experience
* No loading spinner needed

---

## Key Idea

```text
Route → Resolver → Data → Component
```

---

# Short Interview Answer

A resolver is used in Angular routing to fetch data before a route is activated, ensuring the component loads with required data.






# 168. What is APP_INITIALIZER?

Angular **`APP_INITIALIZER`** is a special Angular token used to run **code before the application initializes**.

---

## Why Use It?

* Load configuration from API
* Initialize app settings
* Authenticate user before app start

---

## Example

```ts
import { APP_INITIALIZER } from '@angular/core';

export function loadConfig() {
  return () => fetch('/config.json')
    .then(res => res.json())
    .then(data => console.log(data));
}

providers: [
  {
    provide: APP_INITIALIZER,
    useFactory: loadConfig,
    multi: true
  }
]
```

---

## Key Point

```text
App waits → Initialization completes → App starts
```

---

## Important Notes

* Must return a **function**
* Can return **Promise or Observable**
* Supports multiple initializers (`multi: true`)

---

# Short Interview Answer

APP_INITIALIZER is used to execute functions during app startup before Angular initializes the application.

---

# 169. What is platformBrowserDynamic?

`platformBrowserDynamic` is a function used to **bootstrap Angular applications in the browser**.

---

## Example

```ts
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
import { AppModule } from './app.module';

platformBrowserDynamic().bootstrapModule(AppModule);
```

---

## Key Role

```text
Start Angular app → Bootstrap root module
```

---

## When Used?

* JIT compilation
* Development mode

---

## Modern Alternative

✔ With standalone APIs:

```ts
bootstrapApplication(AppComponent);
```

---

# Short Interview Answer

platformBrowserDynamic is used to bootstrap Angular applications in the browser, typically in JIT mode.

---

# 170. What is Angular Universal?

Angular Universal is used for **Server-Side Rendering (SSR)** in Angular.

---

## What is SSR?

Rendering Angular app on the **server instead of browser**.

---

## How It Works

```text
Server → Render HTML → Send to browser → Faster load
```

---

## Benefits

### 1. Faster Initial Load

✔ HTML already rendered

---

### 2. SEO Friendly

✔ Search engines can read content

---

### 3. Better Performance

✔ Especially for slow devices

---

## Example Flow

```text
Request → Server renders → HTML sent → App loads
```

---

## Use Cases

* SEO-heavy apps
* E-commerce websites
* Content platforms

---

## Key Idea

```text
Angular Universal → Server-side Angular rendering
```

---

# Short Interview Answer

Angular Universal enables server-side rendering in Angular, improving performance and SEO by rendering pages on the server before sending them to the browser.





# 171. What is Server-Side Rendering (SSR)?

Angular Universal **Server-Side Rendering (SSR)** means rendering your Angular application **on the server** and sending **fully generated HTML** to the browser.

---

## How It Works

```text
Client request → Server renders HTML → Browser receives ready page → App becomes interactive
```

---

## Example Flow

* User opens page
* Server generates HTML
* Browser displays instantly
* Angular hydrates and becomes interactive

---

## Key Difference

| CSR (Client-Side Rendering) | SSR               |
| --------------------------- | ----------------- |
| Render in browser           | Render on server  |
| Slower first load           | Faster first load |

---

# Short Interview Answer

SSR is rendering Angular applications on the server and sending pre-rendered HTML to the client for faster initial load.

---

# 172. Benefits of SSR

## 1. Faster Initial Load

✔ Page is already rendered

---

## 2. Better SEO

✔ Search engines can crawl content easily

---

## 3. Improved Performance

✔ Especially on slow networks/devices

---

## 4. Better User Experience

✔ Content visible immediately

---

## 5. Social Media Sharing

✔ Proper metadata (Open Graph tags)

---

## Summary

```text
SSR → Faster + SEO + Better UX
```

---

# Short Interview Answer

SSR improves performance, SEO, and user experience by rendering content on the server before sending it to the browser.

---

# 173. What is Hydration in Angular?

Angular **hydration** is the process where Angular **attaches event listeners and reuses server-rendered HTML** instead of re-rendering it.

---

## How It Works

```text
SSR HTML → Angular loads → Hydrates → Becomes interactive
```

---

## Without Hydration

❌ Re-render entire UI

---

## With Hydration

✔ Reuse existing DOM
✔ Faster interaction

---

## Benefits

* Better performance
* No flicker
* Faster startup

---

# Short Interview Answer

Hydration is the process of making server-rendered HTML interactive by attaching Angular logic on the client side.

---

# 174. What is Micro Frontend?

A **micro frontend** is an architecture where a frontend app is divided into **smaller independent applications**.

---

## Concept

```text
Large app → Split into small apps → Each developed independently
```

---

## Example

* Header → App 1
* Dashboard → App 2
* Profile → App 3

---

## Benefits

* Independent deployment
* Scalable teams
* Technology flexibility

---

## Tools Used

* Module Federation (Webpack)
* Single-SPA

---

## Use Cases

* Large enterprise apps
* Multi-team projects

---

# Short Interview Answer

Micro frontend is an architecture where a large frontend application is divided into smaller, independent applications that can be developed and deployed separately.

---

# 175. How Angular Supports PWA?

Angular supports **Progressive Web Apps (PWA)** using built-in tools.

---

## Setup

```bash
ng add @angular/pwa
```

---

## Key Features

### 1. Service Worker

✔ Caches assets and API responses

---

### 2. Offline Support

✔ Works without internet

---

### 3. App-like Experience

✔ Installable on device

---

### 4. Push Notifications

✔ Engage users

---

## How It Works

```text
App loads → Service worker caches → Offline usage enabled
```

---

## Files Generated

* `ngsw-config.json`
* Service worker setup

---

## Benefits

* Fast loading
* Offline capability
* Better UX

---

# Short Interview Answer

Angular supports PWA by using service workers to enable offline access, caching, and app-like features for better performance and user experience.




Testing

# 176. What is Unit Testing in Angular?

Angular **unit testing** is the process of testing **individual components, services, or functions** in isolation.

---

## Purpose

* Ensure each unit works correctly
* Catch bugs early
* Improve code quality

---

## Example

```ts
it('should add two numbers', () => {
  expect(2 + 2).toBe(4);
});
```

---

## What Can Be Tested?

* Components
* Services
* Pipes
* Directives

---

## Key Idea

```text
Test small parts → Ensure correctness
```

---

# Short Interview Answer

Unit testing in Angular is the practice of testing individual components or services independently to ensure they work correctly.

---

# 177. What is Jasmine?

Jasmine is a **behavior-driven development (BDD) testing framework** used in Angular.

---

## Features

* No external dependencies
* Simple syntax
* Supports test cases and expectations

---

## Example

```ts
describe('Math Test', () => {
  it('should add numbers', () => {
    expect(2 + 2).toBe(4);
  });
});
```

---

## Key Terms

| Term     | Meaning    |
| -------- | ---------- |
| describe | Test suite |
| it       | Test case  |
| expect   | Assertion  |

---

# Short Interview Answer

Jasmine is a testing framework used in Angular for writing unit tests using a readable BDD syntax.

---

# 178. What is Karma?

Karma is a **test runner** that executes tests in a browser.

---

## Role

```text
Run tests → Show results → Report output
```

---

## Features

* Runs tests in real browsers
* Watches file changes
* Provides live results

---

## Workflow

```text
Write test (Jasmine) → Run (Karma) → See results
```

---

# Short Interview Answer

Karma is a test runner that executes Angular tests in browsers and displays the results.

---

# 179. What is TestBed?

**TestBed** is a utility provided by Angular to configure and create a testing environment.

---

## Purpose

* Create component instances
* Inject dependencies
* Configure testing module

---

## Example

```ts
import { TestBed } from '@angular/core/testing';

beforeEach(() => {
  TestBed.configureTestingModule({
    declarations: [AppComponent]
  });
});
```

---

## Create Component

```ts
const fixture = TestBed.createComponent(AppComponent);
const component = fixture.componentInstance;
```

---

## Key Idea

```text
TestBed → Simulate Angular environment for testing
```

---

# Short Interview Answer

TestBed is Angular’s primary testing utility used to configure and initialize components and services in a test environment.

---

# 180. What is Component Testing?

Component testing is testing the **behavior, template, and interaction** of Angular components.

---

## What to Test?

* Template rendering
* User interactions (click, input)
* Data binding
* DOM changes

---

## Example

```ts
it('should display title', () => {
  const fixture = TestBed.createComponent(AppComponent);
  fixture.detectChanges();

  const compiled = fixture.nativeElement;
  expect(compiled.querySelector('h1').textContent).toContain('Hello');
});
```

---

## Tools Used

* Jasmine
* Karma
* TestBed

---

## Key Idea

```text
Component + Template + Interaction → Test together
```

---

# Short Interview Answer

Component testing in Angular verifies that a component’s template, logic, and interactions work correctly.





# 181. What is Service Testing?

Angular **service testing** is the process of testing Angular services independently.

---

## Why Test Services?

* Validate business logic
* Test API calls
* Ensure correct data handling

---

## Example

```ts id="s1a2b3"
import { TestBed } from '@angular/core/testing';
import { DataService } from './data.service';

describe('DataService', () => {
  let service: DataService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DataService);
  });

  it('should return data', () => {
    expect(service.getData()).toEqual([1, 2, 3]);
  });
});
```

---

## What to Test?

* Methods
* API responses
* Logic

---

# Short Interview Answer

Service testing verifies the logic and functionality of Angular services independently.

---

# 182. What is spyOn in Jasmine?

Jasmine **`spyOn`** is used to **mock or track function calls** in tests.

---

## Example

```ts id="s4d5f6"
spyOn(service, 'getData').and.returnValue([1, 2, 3]);

expect(service.getData()).toEqual([1, 2, 3]);
```

---

## Track Calls

```ts id="g7h8j9"
spyOn(service, 'getData');

service.getData();

expect(service.getData).toHaveBeenCalled();
```

---

## Features

* Mock return values
* Track method calls
* Avoid real API calls

---

# Short Interview Answer

spyOn is used in Jasmine to mock functions and verify whether they were called during testing.

---

# 183. What is fakeAsync?

**fakeAsync** is a testing utility used to simulate asynchronous code in a **synchronous way**.

---

## Why Use It?

* Test async operations easily
* Control timing

---

## Example

```ts id="k1l2m3"
import { fakeAsync, tick } from '@angular/core/testing';

it('should test async code', fakeAsync(() => {
  let value = false;

  setTimeout(() => {
    value = true;
  }, 1000);

  tick(1000);

  expect(value).toBeTrue();
}));
```

---

## Key Idea

```text id="n4o5p6"
Async code → Controlled manually
```

---

# Short Interview Answer

fakeAsync allows testing asynchronous code in a synchronous manner by controlling time manually.

---

# 184. What is tick()?

`tick()` is used with **fakeAsync** to simulate the passage of time.

---

## Example

```ts id="q7r8s9"
tick(1000); // simulate 1 second
```

---

## How It Works

```text id="t1u2v3"
Delay → tick() → Execute async code
```

---

## Use Cases

* setTimeout
* setInterval
* Delayed operations

---

# Short Interview Answer

tick() simulates the passage of time in fakeAsync tests to execute asynchronous code.

---

# 185. What is Async Testing?

Async testing is testing code that involves:

* Promises
* Observables
* setTimeout

---

## Methods

### 1. async/await

```ts id="w4x5y6"
it('should test async', async () => {
  const data = await service.getDataAsync();
  expect(data).toBeTruthy();
});
```

---

### 2. done()

```ts id="z7a8b9"
it('should test async', (done) => {
  service.getData().subscribe(data => {
    expect(data).toBeTruthy();
    done();
  });
});
```

---

### 3. fakeAsync + tick

✔ Controlled async testing

---

## Key Idea

```text id="c1d2e3"
Wait for async → Then test result
```

---

# Short Interview Answer

Async testing is used to test asynchronous operations like promises and observables using async/await, done(), or fakeAsync.




# 186. What is End-to-End Testing (E2E)?

Angular **end-to-end (E2E) testing** tests the **entire application flow** from the user’s perspective.

---

## What It Covers

* UI interaction
* Navigation
* API integration
* Full user journey

---

## Example

```text
User login → Dashboard opens → Data loads → Logout
```

---

## Tools

* Cypress
* Playwright
* Selenium

---

## Key Idea

```text
Test complete app like a real user
```

---

# Short Interview Answer

E2E testing verifies the complete application flow by simulating real user interactions.

---

# 187. What is Cypress?

Cypress is a modern **E2E testing framework** for web applications.

---

## Features

* Runs in browser
* Fast execution
* Automatic waiting
* Easy debugging

---

## Example

```ts
describe('Login Test', () => {
  it('should login', () => {
    cy.visit('/login');
    cy.get('input[name="email"]').type('test@mail.com');
    cy.get('input[name="password"]').type('123456');
    cy.get('button').click();
    cy.url().should('include', '/dashboard');
  });
});
```

---

## Benefits

* Developer-friendly
* Real-time reload
* Screenshots & videos

---

# Short Interview Answer

Cypress is a JavaScript-based end-to-end testing framework used to test web applications in real browsers.

---

# 188. What is Mocking?

**Mocking** means creating **fake data or fake services** to simulate real behavior during testing.

---

## Why Use Mocking?

* Avoid real API calls
* Control test data
* Improve test speed

---

## Example

```ts
spyOn(service, 'getData').and.returnValue(of([1, 2, 3]));
```

---

## Types

* Mock services
* Mock API responses
* Mock functions

---

## Key Idea

```text
Replace real dependency → Use fake version
```

---

# Short Interview Answer

Mocking is the practice of replacing real dependencies with fake ones to control behavior during testing.

---

# 189. How to Test Observables?

Observables are tested by **subscribing and asserting emitted values**.

---

## Example

```ts
import { of } from 'rxjs';

it('should test observable', () => {
  const data$ = of([1, 2, 3]);

  data$.subscribe(value => {
    expect(value).toEqual([1, 2, 3]);
  });
});
```

---

## With Async

```ts
it('should test async observable', (done) => {
  service.getData().subscribe(data => {
    expect(data).toBeTruthy();
    done();
  });
});
```

---

## With fakeAsync

```ts
it('should test observable', fakeAsync(() => {
  let result;

  service.getData().subscribe(data => result = data);

  tick();
  expect(result).toBeDefined();
}));
```

---

## Key Idea

```text
Subscribe → Get value → Assert
```

---

# Short Interview Answer

Observables are tested by subscribing to them and verifying the emitted values using assertions.

---

# 190. How to Test HTTP Calls?

Angular provides **HttpClientTestingModule** for testing HTTP calls.

---

## Setup

```ts
import { HttpClientTestingModule, HttpTestingController } from '@angular/common/http/testing';
```

---

## Example

```ts
describe('API Test', () => {
  let httpMock: HttpTestingController;
  let service: DataService;

  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [HttpClientTestingModule],
      providers: [DataService]
    });

    service = TestBed.inject(DataService);
    httpMock = TestBed.inject(HttpTestingController);
  });

  it('should fetch data', () => {
    service.getData().subscribe(data => {
      expect(data).toEqual([1, 2, 3]);
    });

    const req = httpMock.expectOne('/api/data');
    req.flush([1, 2, 3]);
  });
});
```

---

## Key Steps

1. Call API method
2. Expect request
3. Provide mock response
4. Assert result

---

## Key Idea

```text
Intercept request → Return mock → Test result
```

---

# Short Interview Answer

HTTP calls are tested using HttpClientTestingModule and HttpTestingController to mock API requests and verify responses.




# 191. What is XSS Attack?

**XSS (Cross-Site Scripting)** is a security vulnerability where attackers inject **malicious scripts** into a web application.

---

## Example

```html
<input value="<script>alert('Hacked')</script>">
```

👉 Script executes in the browser

---

## Types of XSS

* Stored XSS
* Reflected XSS
* DOM-based XSS

---

## Impact

* Steal cookies/session
* Redirect users
* Inject malicious content

---

## Key Idea

```text
Attacker injects script → Browser executes → Data compromised
```

---

# Short Interview Answer

XSS is a security attack where malicious scripts are injected into web pages and executed in the user's browser.

---

# 192. How Angular Prevents XSS?

Angular has **built-in protection** against XSS.

---

## 1. Automatic Escaping

```html
<p>{{ userInput }}</p>
```

✔ Angular escapes HTML → prevents script execution

---

## 2. Sanitization

Angular cleans dangerous content before rendering.

---

## 3. Safe DOM APIs

Avoids direct DOM manipulation

---

## 4. Security Contexts

* HTML
* URL
* Resource URL

---

## Example

```html
<div [innerHTML]="content"></div>
```

👉 Angular sanitizes content automatically

---

## Important Note

❌ Dangerous:

```ts
bypassSecurityTrustHtml()
```

👉 Use only when necessary

---

# Short Interview Answer

Angular prevents XSS using automatic escaping, sanitization, and secure DOM handling.

---

# 193. What is Sanitization?

Sanitization is the process of **removing unsafe or malicious content** from user input.

---

## Example

```html
<div [innerHTML]="userInput"></div>
```

👉 Angular removes `<script>` tags automatically

---

## Why Needed?

* Prevent XSS attacks
* Ensure safe rendering

---

## Types in Angular

* HTML sanitization
* URL sanitization
* Style sanitization

---

## Key Idea

```text
Unsafe content → Cleaned → Safe to display
```

---

# Short Interview Answer

Sanitization is the process of cleaning unsafe input to prevent security vulnerabilities like XSS.

---

# 194. What is CSRF?

**CSRF (Cross-Site Request Forgery)** is an attack where a malicious site tricks a user into performing actions on another site.

---

## Example

* User logged into bank
* Malicious site sends request
* Money transfer happens without user consent

---

## How It Works

```text
User logged in → Attacker sends request → Server accepts → Action performed
```

---

## Impact

* Unauthorized transactions
* Data modification

---

# Short Interview Answer

CSRF is an attack where unauthorized actions are performed on behalf of an authenticated user without their consent.

---

# 195. How to Secure Angular Applications?

Angular security is critical in real-world apps.

---

## Best Practices

### 1. Prevent XSS

✔ Use Angular binding (`{{ }}`)
✔ Avoid direct DOM access

---

### 2. Use HTTPS

✔ Secure data transmission

---

### 3. Implement Authentication & Authorization

✔ JWT tokens
✔ Role-based access

---

### 4. Protect Against CSRF

✔ Use CSRF tokens
✔ SameSite cookies

---

### 5. Secure API Calls

✔ Use interceptors for tokens

---

### 6. Avoid Storing Sensitive Data in LocalStorage

✔ Prefer secure cookies

---

### 7. Use Content Security Policy (CSP)

✔ Restrict script execution

---

### 8. Keep Dependencies Updated

✔ Avoid vulnerabilities

---

## Key Idea

```text
Secure input + Secure API + Secure storage
```

---

# Short Interview Answer

Angular applications can be secured by preventing XSS/CSRF, using HTTPS, implementing authentication, securing APIs, and following best security practices.




# 196. What is JWT Authentication?

**JWT (JSON Web Token)** authentication is a method where the server issues a **token** after login, and the client uses it for subsequent requests.

---

## Structure of JWT

```text
Header.Payload.Signature
```

---

## Flow

```text
User login → Server validates → Sends JWT → Client stores → Sends with every request
```

---

## Example

```ts
Authorization: Bearer <token>
```

---

## Benefits

* Stateless (no session on server)
* Scalable
* Secure (if used properly)

---

# Short Interview Answer

JWT authentication uses a token issued by the server to authenticate users for subsequent requests.

---

# 197. How to Store Tokens Securely?

## Best Practices

### 1. Use HttpOnly Cookies (Recommended)

✔ Cannot be accessed via JavaScript
✔ Protects from XSS

---

### 2. Avoid LocalStorage (Less Secure)

❌ Vulnerable to XSS attacks

---

### 3. Use Secure & SameSite Cookies

✔ Prevent CSRF

---

### 4. Short Expiry Tokens

✔ Reduce risk if compromised

---

### 5. Refresh Tokens

✔ Maintain session securely

---

## Comparison

| Storage         | Security |
| --------------- | -------- |
| HttpOnly Cookie | High     |
| SessionStorage  | Medium   |
| LocalStorage    | Low      |

---

# Short Interview Answer

Tokens should be stored in HttpOnly secure cookies to protect against XSS and CSRF attacks.

---

# 198. What is Route Protection?

Angular **route protection** restricts access to certain routes based on conditions like authentication.

---

## How It Works

```text
User tries to access route → Guard checks → Allow or deny access
```

---

## Example

```ts
{
  path: 'dashboard',
  canActivate: [AuthGuard],
  component: DashboardComponent
}
```

---

## Use Cases

* Protect admin pages
* Restrict unauthorized users

---

# Short Interview Answer

Route protection uses guards to control access to routes based on conditions like authentication or roles.

---

# 199. What is OAuth?

**OAuth (Open Authorization)** is a protocol that allows users to **login using third-party providers**.

---

## Example

* Login with Google
* Login with Facebook

---

## Flow

```text
User → Google login → Token → App gets access
```

---

## Benefits

* No password handling
* Secure delegation
* Easy integration

---

# Short Interview Answer

OAuth is an authorization protocol that allows users to log in using third-party services without sharing their passwords.

---

# 200. What is CORS?

**CORS (Cross-Origin Resource Sharing)** is a security mechanism that controls **API requests between different domains**.

---

## Example

```text
Frontend: http://localhost:4200  
Backend: http://api.example.com
```

👉 Different origins → CORS required

---

## How It Works

* Server sends headers:

```text
Access-Control-Allow-Origin: *
```

---

## Why Needed?

* Prevent unauthorized access
* Secure cross-domain communication

---

## Key Idea

```text
Browser blocks cross-origin → Server allows via CORS headers
```

---

# Short Interview Answer

CORS is a security mechanism that allows or restricts API requests between different domains using server-defined headers.




# 201. How to Build Angular App for Production?

Angular building for production optimizes your app for **performance, security, and smaller size**.

---

## Command

```bash
ng build --configuration production
```

---

## What Happens in Production Build?

### 1. AOT Compilation

✔ Faster rendering

### 2. Minification

✔ Removes unnecessary code

### 3. Tree Shaking

✔ Removes unused code

### 4. Optimization

✔ Reduces bundle size

### 5. Environment Replacement

✔ Uses `environment.prod.ts`

---

## Output

```text
dist/ folder → Ready for deployment
```

---

## Deployment

* Upload `dist/` files to server
* Use hosting like:

  * Firebase
  * AWS S3
  * Netlify

---

## Key Idea

```text
Development → Optimize → Production build → Deploy
```

---

# Short Interview Answer

Angular production build uses `ng build --configuration production` to optimize the app with AOT, minification, and tree shaking for better performance.

---

# 202. What is angular.json?

**angular.json** is the main configuration file for an Angular project.

---

## Purpose

* Define build settings
* Configure environments
* Manage assets and styles

---

## Example

```json
{
  "projects": {
    "my-app": {
      "architect": {
        "build": {
          "options": {
            "outputPath": "dist/my-app",
            "assets": ["src/assets"],
            "styles": ["src/styles.css"]
          }
        }
      }
    }
  }
}
```

---

## What It Controls?

* Build configuration
* Serve settings
* File paths
* Optimization options

---

## Key Idea

```text
angular.json → Project configuration center
```

---

# Short Interview Answer

angular.json is the configuration file that defines how an Angular project is built, served, and managed.

---

# 203. What is environment.ts?

**environment.ts** is used to store **environment-specific configuration**.

---

## Example

```ts
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'
};
```

---

## Production File

```ts
// environment.prod.ts
export const environment = {
  production: true,
  apiUrl: 'https://api.example.com'
};
```

---

## How It Works

```text
Development → environment.ts  
Production → environment.prod.ts
```

---

## Usage

```ts
import { environment } from '../environments/environment';

console.log(environment.apiUrl);
```

---

## Benefits

* Manage multiple environments
* Avoid hardcoding URLs
* Easy configuration switching

---

## Key Idea

```text
Environment files → Different configs for different stages
```

---

# Short Interview Answer

environment.ts is used to define environment-specific variables like API URLs, which change based on development or production builds.





# 204. How to Manage Multiple Environments?

Angular provides built-in support for managing multiple environments like **development, staging, and production**.

---

## Step 1: Create Environment Files

```text
src/environments/
  environment.ts
  environment.prod.ts
  environment.staging.ts
```

---

## Step 2: Define Configurations

```ts
// environment.ts
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'
};
```

```ts
// environment.prod.ts
export const environment = {
  production: true,
  apiUrl: 'https://api.prod.com'
};
```

---

## Step 3: Configure angular.json

```json
"configurations": {
  "production": {
    "fileReplacements": [
      {
        "replace": "src/environments/environment.ts",
        "with": "src/environments/environment.prod.ts"
      }
    ]
  }
}
```

---

## Step 4: Build for Specific Environment

```bash
ng build --configuration production
ng build --configuration staging
```

---

## Key Idea

```text
Build config → Replace environment file → Use correct settings
```

---

# Short Interview Answer

Multiple environments are managed using environment files and file replacements configured in angular.json.

---

# 205. What is Source Map?

A **source map** maps compiled/minified code back to the **original source code**.

---

## Why Needed?

* Debug production errors
* Understand minified code

---

## Example

```text
main.js (minified) → map → original TypeScript file
```

---

## Enable/Disable

```json
"sourceMap": true
```

---

## In Production

❌ Usually disabled for:

* Security
* Smaller bundle size

---

## Key Idea

```text
Minified code → Source map → Original code
```

---

# Short Interview Answer

Source maps help developers debug by mapping minified code back to the original source code.

---

# 206. What is Bundle Optimization?

Bundle optimization is the process of reducing the **size and load time** of your application bundles.

---

## Techniques

### 1. Tree Shaking

✔ Remove unused code

---

### 2. Lazy Loading

✔ Load modules/components on demand

---

### 3. Minification

✔ Compress code

---

### 4. Code Splitting

✔ Break into smaller chunks

---

### 5. Compression

✔ Use Gzip/Brotli

---

### 6. Remove Unused Dependencies

✔ Keep project lightweight

---

## Example Command

```bash
ng build --configuration production
```

---

## Result

```text
Smaller bundle → Faster load → Better performance
```

---

# Short Interview Answer

Bundle optimization reduces application size and improves performance using techniques like tree shaking, lazy loading, and minification.




# 207. How to Deploy Angular Application?

Angular deployment means hosting your built app so users can access it online.

---

## Step 1: Build for Production

```bash
ng build --configuration production
```

👉 Output goes to:

```text
dist/your-app-name
```

---

## Step 2: Choose Hosting Platform

### Popular Options

* Firebase
* Amazon Web Services (S3 + CloudFront)
* Netlify
* Vercel

---

## Step 3: Upload Files

* Upload `dist/` folder contents
* Configure server to redirect all routes to `index.html`

---

## Example (Firebase)

```bash
npm install -g firebase-tools
firebase login
firebase init
firebase deploy
```

---

## Important Config (for Routing)

```text
All routes → index.html
```

---

## Key Idea

```text
Build → Upload → Host → Live
```

---

# Short Interview Answer

Deploy an Angular app by building it using `ng build --configuration production` and hosting the dist folder on platforms like Firebase, AWS, or Netlify.

---

# 208. What is CI/CD?

**CI/CD (Continuous Integration / Continuous Deployment)** is a process that automates building, testing, and deploying applications.

---

## CI (Continuous Integration)

* Code pushed to repo
* Automatically builds & tests

---

## CD (Continuous Deployment)

* Automatically deploys to server

---

## Flow

```text
Code push → Build → Test → Deploy
```

---

## Tools

* GitHub Actions
* Jenkins
* GitLab

---

## Benefits

* Faster delivery
* Fewer errors
* Automation

---

# Short Interview Answer

CI/CD is a process that automates code integration, testing, and deployment to ensure faster and reliable delivery.

---

# 209. What is Docker in Angular Deployment?

Docker is a tool used to package your Angular app into a **container**.

---

## Why Use Docker?

* Consistent environment
* Easy deployment
* Works anywhere

---

## How It Works

```text
App + Dependencies → Docker Image → Run as Container
```

---

## Example Dockerfile

```dockerfile
# Build stage
FROM node:18 AS build
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# Serve stage
FROM nginx:alpine
COPY --from=build /app/dist/your-app /usr/share/nginx/html
```

---

## Run Container

```bash
docker build -t angular-app .
docker run -p 80:80 angular-app
```

---

## Benefits

* Portable
* Scalable
* Easy CI/CD integration

---

## Key Idea

```text
Package app → Run anywhere
```

---

# Short Interview Answer

Docker is used to containerize Angular applications, ensuring consistent deployment across different environments.




# 210. How to Host Angular App on Nginx?

Nginx is a popular web server used to serve Angular apps efficiently.

---

## Step 1: Build the App

```bash id="z2h9qf"
ng build --configuration production
```

👉 Output in `dist/your-app`

---

## Step 2: Install Nginx

```bash id="h3u8n1"
sudo apt install nginx
```

---

## Step 3: Copy Build Files

```bash id="6s5d9l"
sudo cp -r dist/your-app/* /var/www/html/
```

---

## Step 4: Configure Nginx

Edit config:

```bash id="q8k1w2"
sudo nano /etc/nginx/sites-available/default
```

---

## Add This Configuration

```nginx id="k0l3p9"
server {
  listen 80;
  server_name your-domain.com;

  root /var/www/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
```

👉 Important for Angular routing

---

## Step 5: Restart Nginx

```bash id="x7v2m8"
sudo systemctl restart nginx
```

---

## Key Idea

```text id="c4f6b2"
Build → Copy to server → Configure Nginx → Serve app
```

---

# Short Interview Answer

Host Angular on Nginx by building the app, copying dist files to the server, and configuring Nginx to serve index.html for all routes.

---

# 211. What is Firebase Hosting?

Firebase Hosting is a service to deploy web apps quickly with global CDN.

---

## Features

* Fast CDN
* Free SSL (HTTPS)
* Easy deployment
* Custom domains

---

## Steps to Deploy

```bash id="9t2n3m"
npm install -g firebase-tools
firebase login
firebase init
firebase deploy
```

---

## Configuration

```json id="o8d7f4"
{
  "hosting": {
    "public": "dist/your-app",
    "rewrites": [
      { "source": "**", "destination": "/index.html" }
    ]
  }
}
```

---

## Benefits

* Simple setup
* High performance
* Secure

---

## Key Idea

```text id="r5p8k1"
Build → Deploy → Live instantly
```

---

# Short Interview Answer

Firebase Hosting is a platform to deploy and host web applications with fast CDN, HTTPS, and easy setup.

---

# 212. What is SSR Deployment?

Angular Universal **SSR deployment** means deploying an Angular app with **server-side rendering enabled**.

---

## How It Works

```text id="m7n2q4"
User request → Server renders HTML → Sends to browser → Faster load
```

---

## Steps

### 1. Add SSR

```bash id="p4c6x9"
ng add @nguniversal/express-engine
```

---

### 2. Build SSR App

```bash id="j8k3l2"
npm run build:ssr
```

---

### 3. Serve App

```bash id="y1z5x7"
npm run serve:ssr
```

---

## Deployment Options

* Node.js server
* Docker
* Cloud platforms (AWS, Firebase Functions)

---

## Benefits

* Faster initial load
* Better SEO
* Improved performance

---

## Key Idea

```text id="u9v2w6"
Server renders → Browser displays instantly
```

---

# Short Interview Answer

SSR deployment involves hosting an Angular Universal app on a server (like Node.js) to render pages on the server for better performance and SEO.




# 213. How to Enable Gzip Compression?

**Gzip compression** reduces the size of files sent from server to browser, improving load time.

---

## How It Works

```text
Server compresses → Browser downloads smaller file → Faster load
```

---

## Enable in Nginx

```nginx
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
gzip_min_length 1000;
```

---

## Enable in Node.js (Express)

```ts
import compression from 'compression';

app.use(compression());
```

---

## Benefits

* Faster page load
* Reduced bandwidth
* Better performance

---

## Key Idea

```text
Smaller files → Faster delivery
```

---

# Short Interview Answer

Gzip compression reduces file size by compressing responses on the server, improving load performance.

---

# 214. What is Cache Busting?

**Cache busting** ensures the browser loads the **latest version of files** instead of cached ones.

---

## Problem

```text
Browser caches old JS → New changes not visible
```

---

## Solution

✔ Add unique hash to file names

---

## Example

```text
main.js → main.abc123.js
styles.css → styles.xyz456.css
```

👉 Angular does this automatically in production build

---

## How It Works

```text
File changes → New hash → Browser downloads new file
```

---

## Benefits

* Always latest version
* Avoid stale cache issues

---

# Short Interview Answer

Cache busting uses unique file names (hashes) to ensure browsers load the latest version of files instead of cached ones.

---

# 215. How to Optimize Production Builds?

Angular production optimization improves performance and reduces bundle size.

---

## Best Practices

### 1. Use Production Build

```bash
ng build --configuration production
```

---

### 2. Enable AOT

✔ Faster rendering

---

### 3. Tree Shaking

✔ Remove unused code

---

### 4. Lazy Loading

✔ Load modules/components on demand

---

### 5. Minification

✔ Compress JS/CSS

---

### 6. Use OnPush Strategy

✔ Reduce change detection

---

### 7. Optimize Images & Assets

✔ Use WebP, compress images

---

### 8. Enable Gzip/Brotli

✔ Reduce transfer size

---

### 9. Use CDN

✔ Faster global delivery

---

### 10. Analyze Bundle

```bash
ng build --stats-json
```

---

## Key Idea

```text
Less code + Efficient loading = Faster app
```

---

# Short Interview Answer

Production builds can be optimized using AOT, lazy loading, tree shaking, minification, and compression techniques to improve performance.




# 216. Difference Between Interface and Type

In TypeScript, both **interface** and **type** are used to define shapes of data, but they have some differences.

---

## Example

```ts
// Interface
interface User {
  name: string;
  age: number;
}

// Type
type UserType = {
  name: string;
  age: number;
};
```

---

## Key Differences

| Interface                      | Type                           |
| ------------------------------ | ------------------------------ |
| Used for object structure      | Used for any type              |
| Supports extension (`extends`) | Supports unions, intersections |
| Can be merged                  | Cannot be merged               |
| More OOP-friendly              | More flexible                  |

---

## Example (Type Advanced)

```ts
type Status = 'success' | 'error';
```

---

## When to Use

* Interface → Objects, APIs
* Type → Complex types, unions

---

# Short Interview Answer

Interface is mainly used to define object structures, while type is more flexible and can define unions, primitives, and complex types.

---

# 217. What is Generic in TypeScript?

A **generic** allows you to write reusable and type-safe code.

---

## Example

```ts
function identity<T>(value: T): T {
  return value;
}

identity<string>('Hello');
identity<number>(10);
```

---

## Why Use Generics?

* Reusability
* Type safety
* Avoid duplication

---

## Example with Array

```ts
function getFirst<T>(arr: T[]): T {
  return arr[0];
}
```

---

## Key Idea

```text
Write once → Use with any type
```

---

# Short Interview Answer

Generics allow creating reusable functions and classes that work with different data types while maintaining type safety.

---

# 218. What is Enum?

An **enum** (enumeration) is a way to define a set of named constants.

---

## Example

```ts
enum Role {
  Admin,
  User,
  Guest
}
```

---

## Usage

```ts
let userRole: Role = Role.Admin;
```

---

## String Enum

```ts
enum Status {
  Success = 'SUCCESS',
  Error = 'ERROR'
}
```

---

## Benefits

* Readable code
* Avoid hardcoded values
* Centralized constants

---

## Key Idea

```text
Enum → Named constant values
```

---

# Short Interview Answer

Enum is a TypeScript feature used to define a set of named constant values.

---

# 219. What is Union Type?

A **union type** allows a variable to hold **multiple types**.

---

## Example

```ts
let value: string | number;

value = 'Hello';
value = 10;
```

---

## Function Example

```ts
function printId(id: string | number) {
  console.log(id);
}
```

---

## Benefits

* Flexible typing
* Better type safety

---

## Key Idea

```text
Union → One variable, multiple types
```

---

# Short Interview Answer

A union type allows a variable to have more than one type using the `|` operator.





# 220. What is Intersection Type?

In TypeScript, an **intersection type** combines multiple types into one.

---

## Syntax

```ts id="h2k7s1"
type A = { name: string };
type B = { age: number };

type Person = A & B;
```

---

## Example

```ts id="m4x9c2"
const user: Person = {
  name: 'Sai',
  age: 25
};
```

👉 Must include **all properties** from both types

---

## Key Idea

```text id="t1p8v6"
Intersection → Combine multiple types into one
```

---

## Use Case

* Merge multiple interfaces/types
* Extend objects

---

# Short Interview Answer

An intersection type combines multiple types into one, requiring all properties from each type.

---

# 221. What is Optional Chaining?

**Optional chaining (`?.`)** is used to safely access nested object properties without errors.

---

## Example

```ts id="y6b3n0"
const user = {
  profile: {
    name: 'Sai'
  }
};

console.log(user?.profile?.name);
```

---

## Without Optional Chaining

```ts id="p8k2r1"
user.profile.name // may throw error if undefined
```

---

## With Optional Chaining

```ts id="z9q4x7"
user?.profile?.name // safe access
```

---

## Key Idea

```text id="c5v7u2"
If null/undefined → return undefined (no crash)
```

---

## Use Cases

* API responses
* Nested objects

---

# Short Interview Answer

Optional chaining allows safe access to nested properties using `?.` without causing runtime errors.

---

# 222. What is Type Inference?

**Type inference** means TypeScript automatically **detects the type** without explicitly defining it.

---

## Example

```ts id="d2j8k4"
let name = 'Sai'; // inferred as string
let age = 25;     // inferred as number
```

---

## Function Example

```ts id="f7l3s9"
function add(a: number, b: number) {
  return a + b; // inferred as number
}
```

---

## Benefits

* Less code
* Cleaner syntax
* Automatic typing

---

## Key Idea

```text id="u9n1g5"
No type given → TypeScript guesses type
```

---

# Short Interview Answer

Type inference is when TypeScript automatically determines the type of a variable based on its value.

---

# 223. What is keyof?

**`keyof`** is a TypeScript operator used to get **keys of an object type as a union**.

---

## Example

```ts id="k3m8x1"
type User = {
  name: string;
  age: number;
};

type UserKeys = keyof User; 
// 'name' | 'age'
```

---

## Usage

```ts id="q5r2t7"
function getValue(obj: User, key: keyof User) {
  return obj[key];
}
```

---

## Benefits

* Type-safe property access
* Prevent invalid keys

---

## Key Idea

```text id="w6y3z8"
keyof → Extract keys from object type
```

---

# Short Interview Answer

keyof is a TypeScript operator that returns the keys of an object type as a union of string literals.





# 224. What is `readonly`?

In TypeScript, **`readonly`** makes a property **immutable** (cannot be changed after initialization).

---

## Example

```ts
interface User {
  readonly id: number;
  name: string;
}

const user: User = { id: 1, name: 'Sai' };
user.name = 'Krishna'; // ✅ allowed
user.id = 2; // ❌ Error
```

---

## Use Cases

* Prevent accidental changes
* Define constant properties

---

## Key Idea

```text
readonly → Value cannot be modified
```

---

# Short Interview Answer

`readonly` is used to make properties immutable so they cannot be changed after assignment.

---

# 225. What is Utility Type?

**Utility types** are built-in types that help **transform existing types**.

---

## Common Utility Types

### 1. Partial

```ts
type User = { name: string; age: number };

type PartialUser = Partial<User>; 
// { name?: string; age?: number }
```

---

### 2. Required

```ts
type RequiredUser = Required<User>;
```

---

### 3. Readonly

```ts
type ReadonlyUser = Readonly<User>;
```

---

### 4. Pick

```ts
type NameOnly = Pick<User, 'name'>;
```

---

### 5. Omit

```ts
type WithoutAge = Omit<User, 'age'>;
```

---

## Key Idea

```text
Utility types → Modify existing types easily
```

---

# Short Interview Answer

Utility types are predefined TypeScript types used to transform and manipulate existing types.

---

# 226. Difference Between `any` and `unknown`

| any                       | unknown               |
| ------------------------- | --------------------- |
| No type checking          | Type-safe             |
| Can perform any operation | Must check type first |
| Unsafe                    | Safe                  |

---

## Example

```ts
let value: any = 10;
value.toUpperCase(); // ❌ No error (but runtime issue)

let val: unknown = 10;
val.toUpperCase(); // ❌ Error
```

---

## Safe Usage

```ts
if (typeof val === 'string') {
  val.toUpperCase(); // ✅ safe
}
```

---

## Key Idea

```text
any → No safety  
unknown → Safe handling required
```

---

# Short Interview Answer

`any` disables type checking, while `unknown` is safer and requires type validation before use.

---

# 227. What is `never` Type?

The **`never`** type represents values that **never occur**.

---

## Example

```ts
function throwError(): never {
  throw new Error('Error!');
}
```

---

## Infinite Loop

```ts
function loop(): never {
  while (true) {}
}
```

---

## Use Cases

* Error handling
* Exhaustive checks

---

## Key Idea

```text
never → Function never returns
```

---

# Short Interview Answer

The `never` type represents values that never occur, such as functions that throw errors or run infinitely.

---

# 228. What is Tuple?

A **tuple** is a fixed-length array with **specific types for each position**.

---

## Example

```ts
let user: [string, number];

user = ['Sai', 25]; // ✅
user = [25, 'Sai']; // ❌
```

---

## Access

```ts
console.log(user[0]); // string
```

---

## Use Cases

* Fixed structured data
* Return multiple values

---

## Key Idea

```text
Tuple → Fixed structure + fixed types
```

---

# Short Interview Answer

A tuple is an array with a fixed number of elements and specific types for each position.

---

# 229. What is Mapped Type?

A **mapped type** creates a new type by **transforming properties of an existing type**.

---

## Example

```ts
type User = {
  name: string;
  age: number;
};

type ReadonlyUser = {
  readonly [K in keyof User]: User[K];
};
```

---

## Explanation

* Loop through keys
* Modify properties

---

## Built-in Example

```ts
type PartialUser = Partial<User>;
```

---

## Key Idea

```text
Mapped type → Loop through properties → Transform
```

---

# Short Interview Answer

Mapped types create new types by transforming properties of existing types using `keyof`.

---

# 230. What is Decorator in TypeScript?

A **decorator** is a special function used to **add metadata or modify classes, methods, or properties**.

---

## Example

```ts
function Log(target: any) {
  console.log('Class created');
}

@Log
class User {}
```

---

## Types of Decorators

* Class decorator
* Method decorator
* Property decorator
* Parameter decorator

---

## In Angular

Decorators are heavily used:

* `@Component`
* `@Injectable`
* `@NgModule`

---

## Key Idea

```text
Decorator → Add extra behavior using @
```

---

# Short Interview Answer

Decorators are special functions that add metadata or modify classes, methods, or properties, widely used in Angular.








These two are **very important interview questions**—especially for your experience level. I’ll give you a **real-world, interview-ready answer** you can speak confidently.

---

# 231. Explain Your Angular Project Architecture

Angular project architecture is based on **modular, scalable, and maintainable structure**.

---

## 🏗️ High-Level Architecture

```text
src/
 ├── app/
 │   ├── core/
 │   ├── shared/
 │   ├── features/
 │   ├── services/
 │   ├── models/
 │   ├── state/ (NgRx)
 │   └── app-routing.ts
 ├── assets/
 ├── environments/
```

---

## 🔹 1. Core Module

* Singleton services
* Auth service, interceptors, guards

```text
core/
 ├── services/
 ├── guards/
 ├── interceptors/
```

---

## 🔹 2. Shared Module

* Reusable components, pipes, directives

```text
shared/
 ├── components/
 ├── pipes/
 ├── directives/
```

---

## 🔹 3. Feature Modules

* Business-specific modules

```text
features/
 ├── auth/
 ├── dashboard/
 ├── users/
```

✔ Lazy loaded for performance

---

## 🔹 4. State Management (Optional)

* Using NgRx

```text
state/
 ├── actions/
 ├── reducers/
 ├── effects/
 ├── selectors/
```

---

## 🔹 5. Services Layer

* API calls
* Business logic

---

## 🔹 6. Models

* Interfaces / types

---

## 🔹 7. Routing

* Centralized routing
* Lazy loading modules

---

## 🔹 Data Flow

```text
Component → Service → API → Store → UI
```

---

## 💡 Key Principles

* Separation of concerns
* Reusability
* Scalability
* Lazy loading

---

# 🎯 Short Interview Answer

I follow a modular architecture with core, shared, and feature modules. Core handles singleton services, shared contains reusable components, and features are lazy-loaded modules. I also use services for API handling and optionally NgRx for state management.

---

# 232. How Do You Structure Large Angular Applications?

For large apps, structure is **critical**.

---

## 🧱 Recommended Structure

```text
app/
 ├── core/
 ├── shared/
 ├── features/
 │   ├── module1/
 │   ├── module2/
 ├── layout/
 ├── state/
 ├── utils/
```

---

## 🔥 Best Practices

### 1. Feature-Based Structure

✔ Group by feature, not by type

```text
❌ Bad:
components/, services/, models/

✅ Good:
user/
  ├── user.component.ts
  ├── user.service.ts
```

---

### 2. Lazy Loading

```ts
{
  path: 'users',
  loadChildren: () => import('./features/users/users.module')
}
```

✔ Improves performance

---

### 3. Use Core Module

* Auth
* Interceptors
* Global services

---

### 4. Shared Module

* Buttons
* Tables
* Pipes

---

### 5. State Management

* Use NgRx for complex apps
* Use services for small apps

---

### 6. Folder Naming Convention

* kebab-case
* meaningful names

---

### 7. Smart vs Dumb Components

| Smart         | Dumb            |
| ------------- | --------------- |
| Handles logic | UI only         |
| Uses services | Receives inputs |

---

### 8. Code Splitting

* Lazy load modules
* Dynamic imports

---

### 9. Environment Handling

* Separate configs

---

### 10. Performance Optimization

* OnPush strategy
* trackBy
* memoization

---

## 🔄 Real Example (Your Case)

For your **Fintech / Ecommerce / Healthcare apps**, you can say:

```text
features/
 ├── auth/
 ├── dashboard/
 ├── transactions/
 ├── products/
 ├── orders/
```

---

## 💡 Key Idea

```text
Break large app → Independent modules → Lazy load → Scale easily
```

---

# 🎯 Short Interview Answer

I structure large Angular applications using a feature-based modular approach with core and shared modules. I implement lazy loading, separate business logic into services, and use state management like NgRx when needed to ensure scalability and maintainability.







These are **experience-based questions**, so interviewers expect **real-world answers**, not definitions. I’ll give you **practical + interview-ready answers** you can speak confidently.

---

# 233. How do you manage reusable components?

In Angular, I manage reusable components using a **shared and modular approach**.

---

## 🔹 My Approach

### 1. Shared Module

I create a **Shared Module** to store reusable components:

```text
shared/
 ├── components/
 │   ├── button/
 │   ├── table/
 │   ├── modal/
 ├── pipes/
 ├── directives/
```

---

### 2. Use @Input and @Output

```ts
@Input() data: any;
@Output() action = new EventEmitter();
```

✔ Makes components dynamic and reusable

---

### 3. Config-Driven Components

Example: Reusable table

```ts
columns = [
  { field: 'name', header: 'Name' },
  { field: 'email', header: 'Email' }
];
```

---

### 4. Smart vs Dumb Components

* Smart → Handles logic
* Dumb → UI only (reusable)

---

### 5. Styling Strategy

* Use global styles or Tailwind
* Avoid tight coupling

---

### 6. Reusable Services

* Shared API handling
* Utility functions

---

## 💡 Example (Real)

In my project, I created:

* Reusable table component
* Reusable form input component
* Modal/dialog component

---

## 🎯 Short Interview Answer

I manage reusable components using a shared module, designing them with @Input/@Output for flexibility, and keeping them UI-focused. I also follow a smart/dumb component pattern to maximize reusability.

---

# 234. How did you optimize performance?

Performance optimization is something I actively implemented in my projects.

---

## 🚀 Techniques I Used

### 1. Lazy Loading

```ts
loadChildren: () => import('./features/users/users.module')
```

✔ Reduced initial bundle size

---

### 2. OnPush Change Detection

```ts
changeDetection: ChangeDetectionStrategy.OnPush
```

✔ Avoid unnecessary re-renders

---

### 3. trackBy in ngFor

```ts
trackBy: (index, item) => item.id
```

✔ Prevent DOM re-creation

---

### 4. API Optimization (RxJS)

* Used `debounceTime` for search
* Used `switchMap` to cancel old requests

---

### 5. Code Splitting

* Feature modules
* Dynamic imports

---

### 6. Caching

* Cached API responses using services

---

### 7. Image Optimization

* Used compressed images / WebP

---

### 8. Production Build

```bash
ng build --configuration production
```

---

## 💡 Real Impact

* Faster load time
* Reduced API calls
* Smooth UI

---

## 🎯 Short Interview Answer

I optimized performance using lazy loading, OnPush change detection, trackBy, RxJS operators, and production builds. This reduced bundle size and improved UI responsiveness.

---

# 235. How do you handle authentication?

I implement authentication using **JWT-based authentication with route protection**.

---

## 🔐 Flow

```text
Login → API validates → JWT token → Store → Use in requests
```

---

## 🔹 My Implementation

### 1. Login API

* User sends credentials
* Backend returns JWT token

---

### 2. Token Storage

✔ Prefer HttpOnly cookies (secure)
⚠️ Sometimes localStorage (with precautions)

---

### 3. HTTP Interceptor

```ts
intercept(req, next) {
  const token = this.authService.getToken();

  const cloned = req.clone({
    headers: req.headers.set('Authorization', `Bearer ${token}`)
  });

  return next.handle(cloned);
}
```

---

### 4. Route Guards

```ts
canActivate() {
  return this.authService.isLoggedIn();
}
```

---

### 5. Logout

* Remove token
* Redirect to login

---

### 6. Token Expiry Handling

* Refresh token OR auto logout

---

## 🔒 Security Practices

* Use HTTPS
* Avoid storing sensitive data
* Handle token expiration

---

## 💡 Real Example

In my project:

* Secured dashboard routes
* Used interceptor for all API calls
* Implemented role-based access

---

## 🎯 Short Interview Answer

I handle authentication using JWT tokens with HTTP interceptors for attaching tokens, route guards for protection, and secure storage mechanisms like HttpOnly cookies. I also manage token expiration and logout flows.

---




These are **core real-world questions**—interviewers expect **how you actually worked**, not just theory. Here’s a **strong, practical answer** you can speak confidently.

---

# 236. How do you manage API integration?

In Angular, I manage API integration using a **service-based architecture with HttpClient and RxJS**.

---

## 🔹 My Approach

### 1. Create Dedicated Service

```ts
@Injectable({ providedIn: 'root' })
export class UserService {
  constructor(private http: HttpClient) {}

  getUsers() {
    return this.http.get('/api/users');
  }
}
```

---

### 2. Use Environment Config

```ts
apiUrl = environment.apiUrl;
```

✔ Avoid hardcoding URLs

---

### 3. Handle Errors Globally

Using interceptor:

```ts
catchError((error) => {
  console.error(error);
  return throwError(() => error);
})
```

---

### 4. Use RxJS Operators

* `map()` → transform data
* `switchMap()` → cancel previous requests
* `debounceTime()` → optimize search

---

### 5. HTTP Interceptor

* Add auth token
* Handle errors globally

---

### 6. Strong Typing

```ts
getUsers(): Observable<User[]> {}
```

---

### 7. Loading & Error Handling

* Show loader
* Handle API failures gracefully

---

## 💡 Real Example

* Integrated REST APIs for dashboard
* Implemented search with debounce
* Centralized API calls in services

---

## 🎯 Short Interview Answer

I manage API integration using Angular services with HttpClient, environment-based URLs, RxJS for data handling, and HTTP interceptors for authentication and error handling.

---

# 237. How do you handle State Management?

State management depends on **application size and complexity**.

---

## 🔹 My Approach

### 1. Small Apps → Services + RxJS

```ts
private users$ = new BehaviorSubject<User[]>([]);
```

✔ Simple and lightweight

---

### 2. Medium/Large Apps → NgRx

* Store → Global state
* Actions → Events
* Reducers → State updates
* Effects → API calls

---

## 🔄 Data Flow

```text
Component → Action → Reducer → Store → UI
                ↓
              Effect → API
```

---

## 🔹 Why I Use NgRx?

* Predictable state
* Centralized data
* Easy debugging

---

## 🔹 Example

```ts
this.store.dispatch(loadUsers());
```

---

## 🔹 When I Choose What?

| Scenario    | Approach          |
| ----------- | ----------------- |
| Small app   | Service + Subject |
| Complex app | NgRx              |

---

## 💡 Real Example

* Used BehaviorSubject for simple data sharing
* Used NgRx for large dashboard state

---

## 🎯 Short Interview Answer

I handle state management using RxJS services for smaller apps and NgRx for larger applications to maintain predictable and centralized state.

---

# 238. How do you implement Lazy Loading?

Lazy loading is used to **load modules only when needed**.

---

## 🔹 Step 1: Create Feature Module

```bash
ng generate module users --route users --module app
```

---

## 🔹 Step 2: Configure Routing

```ts
{
  path: 'users',
  loadChildren: () =>
    import('./features/users/users.module').then(m => m.UsersModule)
}
```

---

## 🔹 Step 3: Remove Direct Imports

❌ Don’t import UsersModule in AppModule

---

## 🔹 How It Works

```text
App loads → User navigates → Module loads on demand
```

---

## 🔹 Benefits

* Faster initial load
* Smaller bundle size
* Better performance

---

## 💡 Real Example

* Lazy loaded dashboard, user, and admin modules
* Reduced initial bundle significantly

---

## 🎯 Short Interview Answer

I implement lazy loading by splitting the app into feature modules and loading them using loadChildren in routing, which improves performance by reducing the initial bundle size.

---




These are **practical, real-world Angular interview questions**. Answer them with **what you actually do in projects**—that’s what interviewers care about.

---

# 239. How do you manage forms?

In Angular, I primarily use **Reactive Forms** for better control and scalability.

---

## 🔹 My Approach

### 1. Use Reactive Forms

```ts
this.form = this.fb.group({
  name: ['', Validators.required],
  email: ['', [Validators.required, Validators.email]]
});
```

✔ Better validation
✔ Scalable for complex forms

---

### 2. FormBuilder

* Cleaner and readable code
* Reduces boilerplate

---

### 3. Validation Handling

```html
<div *ngIf="form.controls.email.errors?.required">
  Email is required
</div>
```

---

### 4. Custom Validators

```ts
function passwordValidator(control: AbstractControl) {
  return control.value.length < 6 ? { weak: true } : null;
}
```

---

### 5. Dynamic Forms

* Use `FormArray` for dynamic fields

---

### 6. API Integration

* Submit form → Call service → Handle response

---

## 💡 Real Example

* Built login & registration forms
* Dynamic product forms (ecommerce)
* Validations with error messages

---

## 🎯 Short Interview Answer

I manage forms using Reactive Forms with FormBuilder, validations, and FormArray for dynamic forms. This approach provides better scalability and control.

---

# 240. How do you handle memory leaks?

Memory leaks usually happen due to **unsubscribed observables or long-lived references**.

---

## 🔹 My Approach

### 1. Unsubscribe Observables

```ts
subscription.unsubscribe();
```

---

### 2. Use takeUntil

```ts
private destroy$ = new Subject();

ngOnInit() {
  this.service.getData()
    .pipe(takeUntil(this.destroy$))
    .subscribe();
}

ngOnDestroy() {
  this.destroy$.next();
  this.destroy$.complete();
}
```

---

### 3. Async Pipe (Best Practice)

```html
<div *ngIf="data$ | async as data">
```

✔ Auto unsubscribe

---

### 4. Avoid Global Subscriptions

* Keep subscriptions component-scoped

---

## 💡 Real Impact

* Prevented performance issues
* Avoided unwanted API calls

---

## 🎯 Short Interview Answer

I prevent memory leaks by unsubscribing from observables using takeUntil, async pipe, and cleaning up in ngOnDestroy.

---

# 241. How do you manage subscriptions?

Subscriptions are handled carefully to avoid leaks and improve performance.

---

## 🔹 My Approach

### 1. Async Pipe (Preferred)

```html
{{ data$ | async }}
```

✔ No manual unsubscribe

---

### 2. takeUntil Pattern

```ts
.pipe(takeUntil(this.destroy$))
```

✔ Best for multiple subscriptions

---

### 3. Subscription Object (Rare)

```ts
this.sub = observable.subscribe();
this.sub.unsubscribe();
```

---

### 4. Avoid Nested Subscriptions

❌ Bad:

```ts
obs1.subscribe(() => {
  obs2.subscribe();
});
```

✔ Use RxJS:

```ts
obs1.pipe(switchMap(() => obs2))
```

---

## 💡 Real Example

* Managed API calls with async pipe
* Used takeUntil for lifecycle-based cleanup

---

## 🎯 Short Interview Answer

I manage subscriptions using async pipe, takeUntil for cleanup, and RxJS operators like switchMap to avoid nested subscriptions.

---

# 242. How do you debug Angular applications?

Debugging is a key skill I actively use.

---

## 🔹 My Approach

### 1. Browser DevTools

* Console logs
* Network tab
* Inspect DOM

---

### 2. Angular DevTools

* Component tree
* Change detection tracking

---

### 3. Breakpoints

* Debug TypeScript in browser

---

### 4. Logging

```ts
console.log(data);
```

---

### 5. Error Handling

* Check API errors
* Use interceptors

---

### 6. Source Maps

✔ Debug original code

---

### 7. RxJS Debugging

```ts
.pipe(tap(data => console.log(data)))
```

---

## 💡 Real Example

* Debugged API failures using Network tab
* Fixed change detection issues using DevTools
* Tracked state issues in NgRx

---

## 🎯 Short Interview Answer

I debug Angular applications using browser DevTools, Angular DevTools, breakpoints, logging, and RxJS debugging techniques like tap.

---






These are **real-world scenario questions**—interviewers want to see how you actually design solutions.

---

# 243. How do you handle Role-Based Access?

In Angular, I implement **role-based access control (RBAC)** using **JWT, route guards, and UI-level restrictions**.

---

## 🔐 My Approach

### 1. Store Role After Login

Backend sends role inside JWT or response:

```ts
{
  token: 'abc123',
  role: 'admin'
}
```

---

### 2. Auth Service

```ts
getUserRole(): string {
  return this.user.role;
}
```

---

### 3. Route Guards

```ts
canActivate(route: ActivatedRouteSnapshot): boolean {
  const role = this.authService.getUserRole();
  return route.data['roles'].includes(role);
}
```

---

## Route Config

```ts
{
  path: 'admin',
  component: AdminComponent,
  canActivate: [AuthGuard],
  data: { roles: ['admin'] }
}
```

---

### 4. UI-Level Control

```html
<button *ngIf="role === 'admin'">Delete</button>
```

---

### 5. HTTP Interceptor

* Attach token
* Backend validates role

---

## 🔄 Flow

```text
Login → Get Role → Store → Guard checks → Allow/Deny
```

---

## 💡 Real Example

* Admin → full access
* User → limited access
* Protected routes like dashboard, settings

---

## ⚠️ Important

✔ Always validate on backend also
❌ Don’t rely only on frontend

---

## 🎯 Short Interview Answer

I implement role-based access using JWT tokens, route guards with role checks, and conditional UI rendering, while ensuring backend validation for security.

---

# 244. How do you implement Dynamic Forms?

Dynamic forms are forms where fields are **generated at runtime**.

---

## 🔹 My Approach

### 1. Define Form Configuration

```ts
fields = [
  { name: 'name', type: 'text', validators: [Validators.required] },
  { name: 'email', type: 'email', validators: [Validators.required] }
];
```

---

### 2. Create Form Dynamically

```ts
this.form = this.fb.group({});

this.fields.forEach(field => {
  this.form.addControl(
    field.name,
    this.fb.control('', field.validators)
  );
});
```

---

### 3. Dynamic Template

```html
<div *ngFor="let field of fields">
  <input [type]="field.type" [formControlName]="field.name" />
</div>
```

---

### 4. Use FormArray (Advanced)

```ts
this.form = this.fb.group({
  items: this.fb.array([])
});
```

---

### 5. Add/Remove Fields

```ts
(this.form.get('items') as FormArray).push(this.fb.control(''));
```

---

## 🔄 Flow

```text
Config → Generate Form → Render UI → Submit Data
```

---

## 💡 Real Example

* Dynamic survey forms
* Product configuration forms (ecommerce)
* Admin form builder

---

## Benefits

* Flexible
* Reusable
* API-driven

---

## 🎯 Short Interview Answer

I implement dynamic forms using configuration-driven Reactive Forms, dynamically creating controls with FormBuilder and rendering them using ngFor, often using FormArray for dynamic fields.

---








These are **behavioral + real-world technical questions**. The best answers mix **problem → solution → impact**. Here’s how you can answer confidently:

---

# 245. What challenges did you face in Angular projects?

In Angular projects, I faced multiple real-world challenges and resolved them systematically.

---

## 🔹 1. Performance Issues

**Problem:** Slow loading due to large bundle size
**Solution:**

* Implemented lazy loading
* Used `OnPush` change detection
* Added `trackBy` in lists

**Impact:** Improved initial load time significantly

---

## 🔹 2. API Handling & Multiple Calls

**Problem:** Duplicate or unnecessary API calls
**Solution:**

* Used `switchMap` to cancel previous requests
* Implemented caching using services

**Impact:** Reduced server load and improved UX

---

## 🔹 3. State Management Complexity

**Problem:** Managing shared data across components
**Solution:**

* Used RxJS (`BehaviorSubject`) for small cases
* Implemented NgRx for complex flows

---

## 🔹 4. Memory Leaks

**Problem:** Subscriptions not cleaned up
**Solution:**

* Used `takeUntil` pattern
* Used async pipe

---

## 🔹 5. Form Complexity

**Problem:** Handling large dynamic forms
**Solution:**

* Used Reactive Forms + FormArray
* Created reusable form components

---

## 🔹 6. Authentication & Security

**Problem:** Securing routes and APIs
**Solution:**

* Implemented JWT auth
* Used guards + interceptors

---

## 🎯 Short Interview Answer

I faced challenges like performance issues, API optimization, state management complexity, and memory leaks. I solved them using lazy loading, RxJS operators, NgRx, and proper subscription management, which improved performance and maintainability.

---

# 246. How do you improve application scalability?

Scalability means the app can **grow without becoming hard to manage**.

---

## 🔹 My Approach

### 1. Modular Architecture

```text
core / shared / feature modules
```

✔ Easy to scale and maintain

---

### 2. Lazy Loading

* Load features on demand
* Reduce initial load

---

### 3. Reusable Components

* Shared components
* Config-driven UI

---

### 4. State Management

* Use NgRx for large apps
* Centralized state

---

### 5. API Layer Separation

* Services for API calls
* Clean separation of logic

---

### 6. Code Standards

* Folder structure
* Naming conventions
* Clean architecture

---

### 7. Micro Frontend (Advanced)

* Split large apps into smaller apps

---

### 8. Performance Optimization

* OnPush
* trackBy
* caching

---

## 💡 Real Example

* Broke app into feature modules
* Lazy loaded dashboard, users
* Scaled from small app → multi-module system

---

## 🎯 Short Interview Answer

I improve scalability by using modular architecture, lazy loading, reusable components, and proper state management like NgRx, ensuring the application remains maintainable as it grows.

---

# 247. How do you handle error logging?

Error logging is critical for debugging and monitoring.

---

## 🔹 My Approach

### 1. Global Error Handler

```ts
import { ErrorHandler } from '@angular/core';

export class GlobalErrorHandler implements ErrorHandler {
  handleError(error: any) {
    console.error(error);
  }
}
```

---

### 2. HTTP Error Handling (Interceptor)

```ts
catchError((error) => {
  console.error('API Error:', error);
  return throwError(() => error);
})
```

---

### 3. Logging Service

* Centralized logging
* Send logs to backend

---

### 4. Third-Party Tools

* Sentry
* LogRocket

---

### 5. User-Friendly Messages

* Show proper error messages in UI

---

### 6. Environment-Based Logging

```ts
if (!environment.production) {
  console.log(error);
}
```

---

## 🔄 Flow

```text
Error occurs → Capture → Log → Monitor → Fix
```

---

## 💡 Real Example

* Logged API failures
* Captured UI errors globally
* Used interceptor for centralized error handling

---

## 🎯 Short Interview Answer

I handle error logging using a global error handler, HTTP interceptors, and centralized logging services. I also integrate tools like Sentry for production monitoring.

---





These are **practical, experience-driven questions**—answer them with **what you actually do in projects**.

---

# 248. How do you implement Responsive UI?

In Angular, I ensure responsiveness using a combination of **CSS techniques, frameworks, and Angular tools**.

---

## 🔹 My Approach

### 1. CSS Flexbox & Grid

```css
.container {
  display: flex;
  flex-wrap: wrap;
}
```

✔ Handles layouts easily across screen sizes

---

### 2. Media Queries

```css
@media (max-width: 768px) {
  .container {
    flex-direction: column;
  }
}
```

---

### 3. Responsive Frameworks

* Tailwind CSS
* Bootstrap

✔ Faster UI development

---

### 4. Angular CDK Layout

```ts
import { BreakpointObserver } from '@angular/cdk/layout';
```

✔ Detect screen size dynamically

---

### 5. Responsive Components

* Tables → scroll / card view
* Forms → stacked layout

---

### 6. Mobile-First Approach

✔ Design for mobile first, then scale up

---

## 💡 Real Example

* Dashboard responsive for mobile & desktop
* Converted tables → cards on small screens

---

## 🎯 Short Interview Answer

I implement responsive UI using Flexbox/Grid, media queries, and frameworks like Tailwind or Bootstrap, along with Angular CDK for dynamic responsiveness.

---

# 249. How do you manage Shared Modules?

Shared modules are used to manage **reusable components, pipes, and directives**.

---

## 🔹 My Approach

### 1. Create Shared Module

```text
shared/
 ├── components/
 ├── pipes/
 ├── directives/
 └── shared.module.ts
```

---

### 2. Declare & Export

```ts
@NgModule({
  declarations: [ButtonComponent],
  exports: [ButtonComponent]
})
export class SharedModule {}
```

---

### 3. Import Where Needed

```ts
imports: [SharedModule]
```

---

### 4. What I Keep in Shared

✔ Reusable UI components
✔ Custom pipes
✔ Utility directives

---

### 5. What I Avoid

❌ Services with state (keep in Core module)

---

### 6. Keep It Lightweight

✔ Only reusable items
✔ No business logic

---

## 💡 Real Example

* Shared table component
* Reusable form inputs
* Common pipes (date, currency)

---

## 🎯 Short Interview Answer

I manage shared modules by placing reusable components, pipes, and directives in a shared module and exporting them for use across the application, keeping it lightweight and reusable.

---

# 250. How do you deploy Angular applications?

I follow a **production-ready deployment process**.

---

## 🔹 My Approach

### 1. Build Application

```bash
ng build --configuration production
```

---

### 2. Output

```text
dist/your-app
```

---

### 3. Hosting Platforms

* Firebase
* Amazon Web Services (S3 + CloudFront)
* Netlify
* Vercel

---

### 4. Server Configuration

✔ Redirect all routes to `index.html` (for SPA)

---

### 5. CI/CD Integration

* Automate build & deploy
* Use GitHub Actions / Jenkins

---

### 6. Optional (Advanced)

* Docker containerization
* Nginx hosting

---

## 🔄 Flow

```text
Build → Upload → Configure → Deploy → Live
```

---

## 💡 Real Example

* Deployed app on Firebase
* Used AWS S3 for static hosting
* Configured CI/CD for auto deployment

---

## 🎯 Short Interview Answer

I deploy Angular applications by building them using production configuration and hosting the dist folder on platforms like Firebase or AWS, often integrating CI/CD for automation.

---






These are **HR + behavioral questions**—your answers should feel **natural, confident, and experience-based**. I’ll give you **ready-to-speak answers tailored to your profile (Frontend Developer, Angular, ~2–3 yrs experience)**.

---

# 251. Tell me about yourself

Hi, my name is Sai Krishna Achary. I have around 2.8 years of experience as a Frontend Developer, specializing in Angular, along with React and modern JavaScript technologies.

I have worked on building scalable and responsive web applications in domains like fintech, e-commerce, and healthcare. My expertise includes developing reusable components, integrating APIs, optimizing performance, and implementing state management using RxJS and NgRx.

I am passionate about writing clean and maintainable code, improving user experience, and continuously learning new technologies. Currently, I am looking for an opportunity where I can contribute to a strong development team and further enhance my skills in Angular and frontend architecture.

---

# 252. Why Angular?

I prefer Angular because it provides a complete framework with built-in features like dependency injection, routing, and form handling, which helps in building scalable applications.

I also like its strong use of TypeScript, which improves code quality and maintainability. Features like RxJS for reactive programming and modular architecture make it ideal for enterprise-level applications.

In my experience, Angular helps in maintaining a clean structure and is very effective for large-scale projects.

---

# 253. Why should we hire you?

You should hire me because I have hands-on experience in building real-world Angular applications with a strong focus on performance, scalability, and clean architecture.

I have worked on reusable components, API integrations, authentication, and state management, and I understand best practices like lazy loading and optimization techniques.

I am also a quick learner, a problem solver, and a team player who is always ready to take ownership and deliver quality work.

---

# 254. What are your strengths?

My key strengths are problem-solving, strong understanding of Angular and frontend concepts, and writing clean, maintainable code.

I am also good at debugging issues, optimizing performance, and building reusable components. Additionally, I am a fast learner and adapt quickly to new technologies and project requirements.

---

# 255. What are your weaknesses?

One of my weaknesses is that earlier I used to spend extra time perfecting the UI and code structure. While it improved quality, it sometimes affected timelines.

However, I have improved by balancing quality with deadlines and prioritizing tasks effectively.

---

# 256. Why are you changing jobs?

I am looking for new opportunities to grow professionally and work on more challenging projects.

While I have gained valuable experience in my current role, I want to expand my skills further, especially in large-scale applications, advanced Angular architecture, and modern frontend practices.

I am looking for an organization where I can contribute more and continue learning.

---

# 257. Describe a difficult issue you solved

In one of my projects, we faced a performance issue where the application was slow due to multiple unnecessary API calls and frequent re-rendering.

I analyzed the issue and implemented RxJS operators like switchMap to cancel previous API requests and used debounceTime for search functionality. I also applied OnPush change detection and trackBy in ngFor to reduce unnecessary DOM updates.

As a result, the application performance improved significantly, API calls were reduced, and the user experience became much smoother.

---

## 💡 Pro Tip

In interview:

* Speak **slow and clear**
* Add **real examples**
* Keep answers **1–2 minutes max**

---




These are **final-round HR questions**. Keep answers **honest, structured, and confident**. I’ll give you **ready-to-speak answers tailored to your profile**.

---

# 258. How do you handle deadlines?

I handle deadlines by planning my tasks properly and breaking them into smaller milestones. I prioritize critical features first and track progress regularly.

If I see any risk of delay, I communicate early with my team and try to find solutions, like re-prioritizing tasks or optimizing the approach. I also avoid over-engineering and focus on delivering quality work within the timeline.

---

# 259. Have you worked in Agile/Scrum?

Yes, I have worked in Agile environments using Scrum methodology. I have been part of sprint planning, daily stand-ups, sprint reviews, and retrospectives.

I regularly use tools like Jira to track tasks and progress. Agile helped me collaborate better with my team, adapt to changing requirements, and deliver features incrementally.

---

# 260. How do you learn new technologies?

I follow a structured approach to learning new technologies. First, I understand the fundamentals through documentation and tutorials. Then I build small hands-on projects to apply what I learned.

I also refer to official docs, watch tutorials, and explore real-world use cases. If possible, I try to implement the new technology in my current or side projects to gain practical experience.

---

# 261. How do you handle team conflicts?

I handle team conflicts by focusing on clear communication and understanding different perspectives. I try to listen to others and discuss the issue calmly.

If needed, I suggest a solution based on project goals and best practices. My focus is always on resolving conflicts professionally and ensuring team productivity is not affected.

---

# 262. What motivates you?

I am motivated by solving challenging problems and building impactful applications. Seeing my work improve user experience and performance gives me satisfaction.

I am also motivated by continuous learning and improving my skills, especially in Angular and frontend architecture.

---

# 263. Where do you see yourself in 5 years?

In the next 5 years, I see myself as a senior frontend developer or a frontend architect, working on large-scale applications.

I want to deepen my expertise in Angular, performance optimization, and system design, and also contribute to mentoring junior developers and improving team practices.

---

# 264. What is your expected salary?

I am open to a competitive salary based on my skills, experience, and industry standards. I would prefer to discuss this based on the role and responsibilities.

However, I am flexible and more focused on the right opportunity and growth.

---

# 265. Do you have questions for us?

Yes, I do have a few questions:

1. What kind of projects or technologies will I be working on in this role?
2. How does the team handle code reviews and collaboration?
3. What opportunities are there for learning and growth?
4. How do you measure success in this role?

These will help me better understand the role and how I can contribute effectively.

---

## 💡 Final Tip

In HR round:

* Be **confident but not overconfident**
* Keep answers **natural (don’t sound memorized)**
* Always connect answers to **your experience**

---

------------------------------------------------------------


// 71. Create a counter app
Here’s a simple **Counter App** built with **Angular 19**. It demonstrates the basics of component state management and event handling.

---

## 📂 File Structure
```
src/
 ├── app/
 │    ├── app.component.ts
 │    ├── app.component.html
 │    └── app.component.css
 ├── main.ts
```

---

## 📝 Code

### `app.component.ts`
```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  counter = 0;

  increment() {
    this.counter++;
  }

  decrement() {
    this.counter--;
  }

  reset() {
    this.counter = 0;
  }
}
```

---

### `app.component.html`
```html
<div class="container">
  <h1>Angular 19 Counter App</h1>
  <h2>{{ counter }}</h2>

  <div class="buttons">
    <button (click)="increment()">Increment</button>
    <button (click)="decrement()">Decrement</button>
    <button (click)="reset()">Reset</button>
  </div>
</div>
```

---

### `app.component.css`
```css
.container {
  text-align: center;
  margin-top: 50px;
  font-family: Arial, sans-serif;
}

h1 {
  color: #2c3e50;
}

h2 {
  font-size: 2.5rem;
  margin: 20px 0;
}

.buttons button {
  margin: 5px;
  padding: 10px 20px;
  font-size: 1rem;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

.buttons button:hover {
  opacity: 0.8;
}

button:nth-child(1) {
  background-color: #27ae60;
  color: white;
}

button:nth-child(2) {
  background-color: #c0392b;
  color: white;
}

button:nth-child(3) {
  background-color: #2980b9;
  color: white;
}
```

---

### `main.ts`
```ts
import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent)
  .catch(err => console.error(err));
```

---

## 🚀 Features
- **Increment** the counter  
- **Decrement** the counter  
- **Reset** back to zero  
- Styled buttons with hover effects  





---

Would you like me to extend this into a more advanced version with
 **state persistence** (saving the counter value in `localStorage`) 
 or keep it as a simple demo? You could explore Angular components, 
 event binding, or state management next.

-----------------------------------------------------



// 72. Build a Todo App (CRUD)

Here’s a complete walkthrough for building a **Todo App (CRUD)** with **field validation** and persistence using a **JSON Server** in **Angular 19**.  

---

## 🛠️ Steps to Build

### 1. **Setup Angular Project**
```bash
ng new todo-app --style=css --routing=true
cd todo-app
npm install json-server
```

---

### 2. **Configure JSON Server**
Create a `db.json` file in the root:
```json
{
  "todos": []
}
```
Run JSON Server:
```bash
npx json-server --watch db.json --port 3000
```

---

### 3. **Create Todo Model**
`src/app/models/todo.model.ts`
```ts
export interface Todo {
  id?: number;
  title: string;
  description: string;
  completed: boolean;
}
```

---

### 4. **Build Todo Service**
`src/app/services/todo.service.ts`
```ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Todo } from '../models/todo.model';

@Injectable({ providedIn: 'root' })
export class TodoService {
  private apiUrl = 'http://localhost:3000/todos';

  constructor(private http: HttpClient) {}

  getTodos(): Observable<Todo[]> {
    return this.http.get<Todo[]>(this.apiUrl);
  }

  addTodo(todo: Todo): Observable<Todo> {
    return this.http.post<Todo>(this.apiUrl, todo);
  }

  updateTodo(todo: Todo): Observable<Todo> {
    return this.http.put<Todo>(`${this.apiUrl}/${todo.id}`, todo);
  }

  deleteTodo(id: number): Observable<void> {
    return this.http.delete<void>(`${this.apiUrl}/${id}`);
  }
}
```

---

### 5. **Create Todo Component**
Generate:
```bash
ng generate component components/todo
```

`todo.component.ts`
```ts
import { Component, OnInit } from '@angular/core';
import { FormBuilder, Validators } from '@angular/forms';
import { TodoService } from '../../services/todo.service';
import { Todo } from '../../models/todo.model';

@Component({
  selector: 'app-todo',
  templateUrl: './todo.component.html',
})
export class TodoComponent implements OnInit {
  todos: Todo[] = [];

  todoForm = this.fb.group({
    title: ['', [Validators.required, Validators.minLength(3)]],
    description: ['', Validators.required],
    completed: [false]
  });

  constructor(private fb: FormBuilder, private todoService: TodoService) {}

  ngOnInit() {
    this.loadTodos();
  }

  loadTodos() {
    this.todoService.getTodos().subscribe(data => this.todos = data);
  }

  addTodo() {
    if (this.todoForm.valid) {
      this.todoService.addTodo(this.todoForm.value).subscribe(() => {
        this.loadTodos();
        this.todoForm.reset({ completed: false });
      });
    }
  }

  updateTodo(todo: Todo) {
    this.todoService.updateTodo(todo).subscribe(() => this.loadTodos());
  }

  deleteTodo(id: number) {
    this.todoService.deleteTodo(id).subscribe(() => this.loadTodos());
  }
}
```

---

### 6. **Todo Template with Validation**
`todo.component.html`
```html
<div class="p-4">
  <form [formGroup]="todoForm" (ngSubmit)="addTodo()" class="space-y-4">
    <input formControlName="title" placeholder="Title"
           class="border p-2 w-full" />
    <div *ngIf="todoForm.controls['title'].invalid && todoForm.controls['title'].touched" class="text-red-500">
      Title is required (min 3 chars).
    </div>

    <textarea formControlName="description" placeholder="Description"
              class="border p-2 w-full"></textarea>
    <div *ngIf="todoForm.controls['description'].invalid && todoForm.controls['description'].touched" class="text-red-500">
      Description is required.
    </div>

    <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add Todo</button>
  </form>

  <ul class="mt-6">
    <li *ngFor="let todo of todos" class="flex justify-between items-center border-b py-2">
      <div>
        <h3 [class.line-through]="todo.completed">{{ todo.title }}</h3>
        <p>{{ todo.description }}</p>
      </div>
      <div class="space-x-2">
        <button (click)="updateTodo({...todo, completed: !todo.completed})"
                class="bg-green-500 text-white px-2 py-1 rounded">
          {{ todo.completed ? 'Undo' : 'Complete' }}
        </button>
        <button (click)="deleteTodo(todo.id!)"
                class="bg-red-500 text-white px-2 py-1 rounded">Delete</button>
      </div>
    </li>
  </ul>
</div>
```


------------------------------------------------------------
// 73. Create a reusable component


Here’s a clean example of how to build a **reusable Angular 19 component**. Let’s make a **ContactCardComponent** that can display a person’s **name, phone, and email**, and be reused anywhere in your app.

---

## 📂 File Structure
```
src/
 ├── app/
 │    ├── contact-card/
 │    │    ├── contact-card.component.ts
 │    │    ├── contact-card.component.html
 │    │    └── contact-card.component.css
 │    ├── app.component.ts
 │    ├── app.component.html
 │    └── app.component.css
 ├── main.ts
```

---

## 📝 Code

### `contact-card.component.ts`
```ts
import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-contact-card',
  templateUrl: './contact-card.component.html',
  styleUrls: ['./contact-card.component.css']
})
export class ContactCardComponent {
  @Input() name: string = '';
  @Input() phone: string = '';
  @Input() email: string = '';
}
```

---

### `contact-card.component.html`
```html
<div class="card">
  <h3>{{ name }}</h3>
  <p>📞 {{ phone }}</p>
  <p>✉️ {{ email }}</p>
</div>
```

---

### `contact-card.component.css`
```css
.card {
  border: 1px solid #ddd;
  border-radius: 8px;
  padding: 15px;
  margin: 10px;
  background-color: #f9f9f9;
  text-align: left;
}

h3 {
  margin: 0 0 10px;
  color: #2c3e50;
}

p {
  margin: 5px 0;
}
```

---

### `app.component.ts`
```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  contacts = [
    { name: 'Alice Johnson', phone: '123-456-7890', email: 'alice@example.com' },
    { name: 'Bob Smith', phone: '987-654-3210', email: 'bob@example.com' }
  ];
}
```

---

### `app.component.html`
```html
<div class="container">
  <h1>Reusable Contact Card Demo</h1>

  <app-contact-card
    *ngFor="let contact of contacts"
    [name]="contact.name"
    [phone]="contact.phone"
    [email]="contact.email">
  </app-contact-card>
</div>
```

---

### `main.ts`
```ts
import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent)
  .catch(err => console.error(err));
```

---

## 🚀 Features
- **Reusable component**: `ContactCardComponent` can be dropped anywhere with different inputs.  
- **Dynamic rendering**: Pass data via `@Input()` bindings.  
- **Separation of concerns**: Clean structure for scalability.  

---

--------------------------------------------------------------------------------


// 74. Build a search with debounce
// 75. Implement pagination

Here’s the **full Angular 19 code** for a **Debounced Search with Pagination** using the **Fake API (JSONPlaceholder)**. This is a complete working example you can drop into a fresh Angular project.

---

## 📂 Project Structure
```
src/
 ├── app/
 │    ├── services/
 │    │    └── post.service.ts
 │    ├── search-bar/
 │    │    ├── search-bar.component.ts
 │    │    ├── search-bar.component.html
 │    │    └── search-bar.component.css
 │    ├── app.component.ts
 │    ├── app.component.html
 │    └── app.component.css
 ├── main.ts
```

---

## 📝 Code

### 1. **Post Service**
`src/app/services/post.service.ts`
```ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({ providedIn: 'root' })
export class PostService {
  private apiUrl = 'https://jsonplaceholder.typicode.com/posts';

  constructor(private http: HttpClient) {}

  searchPosts(query: string, page: number, limit: number): Observable<any[]> {
    return this.http.get<any[]>(
      `${this.apiUrl}?q=${query}&_page=${page}&_limit=${limit}`
    );
  }
}
```

---

### 2. **Search Bar Component**
`src/app/search-bar/search-bar.component.ts`
```ts
import { Component, EventEmitter, Output } from '@angular/core';
import { Subject } from 'rxjs';
import { debounceTime, distinctUntilChanged } from 'rxjs/operators';

@Component({
  selector: 'app-search-bar',
  templateUrl: './search-bar.component.html',
  styleUrls: ['./search-bar.component.css']
})
export class SearchBarComponent {
  private searchSubject = new Subject<string>();
  @Output() search = new EventEmitter<string>();

  constructor() {
    this.searchSubject
      .pipe(debounceTime(500), distinctUntilChanged())
      .subscribe(value => this.search.emit(value));
  }

  onSearchChange(value: string) {
    this.searchSubject.next(value);
  }
}
```

`src/app/search-bar/search-bar.component.html`
```html
<input 
  type="text" 
  placeholder="Search posts..." 
  (input)="onSearchChange($event.target.value)" 
  class="border p-2 w-full" />
```

`src/app/search-bar/search-bar.component.css`
```css
input {
  border: 1px solid #ccc;
  border-radius: 6px;
  padding: 8px;
  width: 100%;
}
```

---

### 3. **App Component**
`src/app/app.component.ts`
```ts
import { Component } from '@angular/core';
import { PostService } from './services/post.service';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  results: any[] = [];
  currentPage = 1;
  itemsPerPage = 5;
  lastQuery = '';

  constructor(private postService: PostService) {}

  onSearch(query: string) {
    this.lastQuery = query;
    this.currentPage = 1;
    this.fetchResults();
  }

  fetchResults() {
    if (this.lastQuery.trim()) {
      this.postService.searchPosts(this.lastQuery, this.currentPage, this.itemsPerPage)
        .subscribe(data => this.results = data);
    } else {
      this.results = [];
    }
  }

  nextPage() {
    this.currentPage++;
    this.fetchResults();
  }

  prevPage() {
    if (this.currentPage > 1) {
      this.currentPage--;
      this.fetchResults();
    }
  }
}
```

`src/app/app.component.html`
```html
<div class="container p-4">
  <h1>Debounced Search with Pagination</h1>

  <app-search-bar (search)="onSearch($event)"></app-search-bar>

  <ul class="mt-4">
    <li *ngFor="let post of results" class="border-b py-2">
      <strong>{{ post.title }}</strong>
      <p>{{ post.body }}</p>
    </li>
  </ul>

  <div class="flex justify-center space-x-4 mt-4">
    <button (click)="prevPage()" [disabled]="currentPage === 1"
            class="bg-gray-500 text-white px-3 py-1 rounded">Prev</button>
    <span>Page {{ currentPage }}</span>
    <button (click)="nextPage()" class="bg-blue-500 text-white px-3 py-1 rounded">Next</button>
  </div>
</div>
```

`src/app/app.component.css`
```css
.container {
  max-width: 600px;
  margin: auto;
}

ul {
  list-style: none;
  padding: 0;
}

li {
  border-bottom: 1px solid #ddd;
  padding: 10px 0;
}
```

---

### 4. **Bootstrap**
`src/main.ts`
```ts
import { bootstrapApplication } from '@angular/platform-browser';
import { provideHttpClient } from '@angular/common/http';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [provideHttpClient()]
}).catch(err => console.error(err));
```

---

## 🚀 Features
- **Debounced search**: waits 500ms after typing before firing API calls.  
- **Pagination**: fetches results page by page using `_page` and `_limit`.  
- **Reusable search bar**: can be plugged into any component.  
- **Clean UI**: simple styling with responsive layout.  

---

This is a **full working Angular 19 app**. You can run it with:

```bash
ng serve
```


----------------------------------------------------------------------------



Create a custom pipe

Here’s a full example of creating a **custom pipe in Angular 19**. Pipes are perfect for transforming data in templates. Let’s build a **`truncate` pipe** that shortens long text to a specified length and appends `...`.

---

## 📂 File Structure
```
src/
 ├── app/
 │    ├── truncate.pipe.ts
 │    ├── app.component.ts
 │    ├── app.component.html
 ├── main.ts
```

---

## 📝 Code

### `truncate.pipe.ts`
```ts
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'truncate'
})
export class TruncatePipe implements PipeTransform {
  transform(value: string, limit: number = 20, trail: string = '...'): string {
    if (!value) return '';
    return value.length > limit ? value.substring(0, limit) + trail : value;
  }
}
```

---

### `app.component.ts`
```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {
  longText = 'Angular custom pipes are powerful tools for transforming data in templates.';
}
```

---

### `app.component.html`
```html
<div class="container">
  <h1>Custom Pipe Demo</h1>
  <p><strong>Original:</strong> {{ longText }}</p>
  <p><strong>Truncated:</strong> {{ longText | truncate:30 }}</p>
</div>
```

---

### `main.ts`
```ts
import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app.component';
import { TruncatePipe } from './app/truncate.pipe';

bootstrapApplication(AppComponent, {
  providers: [],
  imports: [TruncatePipe] // Register pipe here
}).catch(err => console.error(err));
```

---

## 🚀 Features
- **Reusable**: Apply `| truncate` to any string in templates.  
- **Parameterized**: Pass a custom length and trailing characters.  
- **Declarative**: No need to manually slice strings in components.  

---



-----------------------------------------------------------------------



Here’s a full working example of an **Angular 19 application** that combines a **modal popup** with a **multi‑step form wizard** (with validation at each step) and persists data into a **JSON Server**.  

---

## 🛠️ Setup

1. **Create Angular project**  
   ```bash
   ng new modal-form-app --routing=true --style=css
   cd modal-form-app
   npm install json-server
   ```

2. **Configure JSON Server**  
   Create `db.json` in project root:
   ```json
   {
     "users": []
   }
   ```
   Run server:
   ```bash
   npx json-server --watch db.json --port 3000
   ```

---

## 📂 File Structure
```
src/app/
 ├── services/user.service.ts
 ├── components/modal/modal.component.ts
 ├── components/modal/modal.component.html
 ├── components/modal/modal.component.css
 ├── components/form-wizard/form-wizard.component.ts
 ├── components/form-wizard/form-wizard.component.html
 ├── components/form-wizard/form-wizard.component.css
 ├── app.component.ts
 ├── app.component.html
 └── app.component.css
```

---

## 📝 Code

### 1. **User Service**
`services/user.service.ts`
```ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({ providedIn: 'root' })
export class UserService {
  private apiUrl = 'http://localhost:3000/users';

  constructor(private http: HttpClient) {}

  addUser(user: any): Observable<any> {
    return this.http.post<any>(this.apiUrl, user);
  }

  getUsers(): Observable<any[]> {
    return this.http.get<any[]>(this.apiUrl);
  }
}
```

---

### 2. **Modal Component**
`modal.component.ts`
```ts
import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'app-modal',
  templateUrl: './modal.component.html',
  styleUrls: ['./modal.component.css']
})
export class ModalComponent {
  @Input() isOpen = false;
  @Output() close = new EventEmitter<void>();

  onClose() {
    this.close.emit();
  }
}
```

`modal.component.html`
```html
<div class="modal" *ngIf="isOpen">
  <div class="modal-content">
    <span class="close" (click)="onClose()">&times;</span>
    <ng-content></ng-content>
  </div>
</div>
```

`modal.component.css`
```css
.modal {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0,0,0,0.5);
  display: flex; justify-content: center; align-items: center;
}
.modal-content {
  background: #fff;
  padding: 20px;
  border-radius: 8px;
  width: 400px;
}
.close {
  float: right;
  cursor: pointer;
  font-size: 20px;
}
```

---

### 3. **Multi‑Step Form Component**
`form-wizard.component.ts`
```ts
import { Component } from '@angular/core';
import { FormBuilder, Validators } from '@angular/forms';
import { UserService } from '../../services/user.service';

@Component({
  selector: 'app-form-wizard',
  templateUrl: './form-wizard.component.html',
  styleUrls: ['./form-wizard.component.css']
})
export class FormWizardComponent {
  step = 1;

  form = this.fb.group({
    personal: this.fb.group({
      name: ['', Validators.required],
      age: ['', [Validators.required, Validators.min(18)]]
    }),
    contact: this.fb.group({
      email: ['', [Validators.required, Validators.email]],
      phone: ['', [Validators.required, Validators.pattern(/^[0-9]{10}$/)]]
    }),
    account: this.fb.group({
      username: ['', Validators.required],
      password: ['', [Validators.required, Validators.minLength(6)]]
    })
  });

  constructor(private fb: FormBuilder, private userService: UserService) {}

  nextStep() {
    const group = this.getCurrentGroup();
    if (group.valid) this.step++;
    else group.markAllAsTouched();
  }

  prevStep() {
    if (this.step > 1) this.step--;
  }

  getCurrentGroup() {
    if (this.step === 1) return this.form.get('personal')!;
    if (this.step === 2) return this.form.get('contact')!;
    return this.form.get('account')!;
  }

  submit() {
    if (this.form.valid) {
      this.userService.addUser(this.form.value).subscribe(() => {
        alert('User saved successfully!');
        this.form.reset();
        this.step = 1;
      });
    }
  }
}
```

`form-wizard.component.html`
```html
<div>
  <div *ngIf="step === 1">
    <h3>Step 1: Personal Info</h3>
    <input formControlName="name" placeholder="Name" />
    <input formControlName="age" type="number" placeholder="Age" />
  </div>

  <div *ngIf="step === 2">
    <h3>Step 2: Contact Info</h3>
    <input formControlName="email" placeholder="Email" />
    <input formControlName="phone" placeholder="Phone" />
  </div>

  <div *ngIf="step === 3">
    <h3>Step 3: Account Setup</h3>
    <input formControlName="username" placeholder="Username" />
    <input formControlName="password" type="password" placeholder="Password" />
  </div>

  <div class="buttons">
    <button (click)="prevStep()" [disabled]="step === 1">Back</button>
    <button *ngIf="step < 3" (click)="nextStep()">Next</button>
    <button *ngIf="step === 3" (click)="submit()">Submit</button>
  </div>
</div>
```

`form-wizard.component.css`
```css
input {
  display: block;
  margin: 8px 0;
  padding: 6px;
  width: 100%;
  border: 1px solid #ccc;
  border-radius: 4px;
}
.buttons {
  margin-top: 12px;
  display: flex;
  justify-content: space-between;
}
```

---

### 4. **App Component**
`app.component.ts`
```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  isModalOpen = false;

  openModal() { this.isModalOpen = true; }
  closeModal() { this.isModalOpen = false; }
}
```

`app.component.html`
```html
<div class="container">
  <h1>Modal + Multi-Step Form</h1>
  <button (click)="openModal()">Open Form</button>

  <app-modal [isOpen]="isModalOpen" (close)="closeModal()">
    <app-form-wizard></app-form-wizard>
  </app-modal>
</div>
```

---

## 🚀 Features
- **Reusable modal popup** with `ng-content`.  
- **Multi‑step form wizard** with validation at each step.  
- **Data persistence** into JSON Server (`db.json`).  
- **Clean separation** of modal and form logic.  

---



/-------------------------------------------------------------------






